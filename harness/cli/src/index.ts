#!/usr/bin/env node
// Phase 1 scaffold placeholder — proves the bin + prepare build + npx wiring.
// Replaced by the real commander entrypoint in T009.
export function main(): void {
  process.stdout.write('harness CLI — scaffold\n');
}

main();
