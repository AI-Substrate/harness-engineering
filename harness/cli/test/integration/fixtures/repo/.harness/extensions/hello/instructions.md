# `harness hello` — agent briefing

Fixture briefing: proves `has_instructions: true` and `harness instructions
hello` end-to-end. The verb computes a greeting; you (the calling agent) just
read it — no judgment required, it is a fixture.
