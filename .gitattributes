# Checkout hygiene for cross-platform work (plan 017): every text file is LF
# in the working tree on every OS, so a Windows clone (core.autocrlf=true)
# can't poison heredocs, snapshots, or byte-equality tests at the next re-port.
# Zero-churn at introduction: all tracked files were already index-LF.
* text=auto eol=lf
