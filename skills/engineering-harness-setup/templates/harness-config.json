{
  "version": "0.1",
  "harness": {
    "name": "{{HARNESS_NAME}}",
    "cli_language": "{{CLI_LANGUAGE}}",
    "cli_path": "{{CLI_PATH}}",
    "created_by": "engineering-harness-setup"
  },
  "commands": {
    "install": "{{INSTALL_COMMAND}}",
    "boot": "{{BOOT_COMMAND}}",
    "health": "{{HEALTH_COMMAND}}",
    "build": "{{BUILD_COMMAND}}",
    "test": "{{TEST_COMMAND}}",
    "lint": "{{LINT_COMMAND}}",
    "typecheck": "{{TYPECHECK_COMMAND}}",
    "format_check": "{{FORMAT_CHECK_COMMAND}}",
    "run": "{{RUN_COMMAND}}",
    "observe": "{{OBSERVE_COMMAND}}",
    "smoke": "{{SMOKE_COMMAND}}",
    "arch": "{{ARCH_COMMAND}}",
    "security": "{{SECURITY_COMMAND}}",
    "schema": "{{SCHEMA_COMMAND}}",
    "codeql": "{{CODEQL_COMMAND}}",
    "seed": "{{SEED_COMMAND}}"
  },
  "health": {
    "url": "{{HEALTH_URL}}",
    "expected_status": 200,
    "timeout_seconds": 30
  },
  "validation": {
    "fast": ["test"],
    "quick": ["doctor", "build", "test"],
    "proof": ["doctor", "build", "test", "health", "smoke"]
  },
  "paths": {
    "proofs": "harness/proofs",
    "harness_docs": "docs/harness",
    "buffers": "docs/harness/_buffers",
    "retros": "docs/harness/agents",
    "templates": "harness/templates"
  },
  "permissions": {
    "allow_run": false,
    "allow_health_probe": true
  },
  "notes": {
    "unset_command_policy": "An empty command string is an improvement opportunity, not proof of failure unless required for the selected validation tier.",
    "signal_policy": "Empty observe, smoke, arch, security, schema, typecheck, or codeql commands are deterministic-sensor gaps to consider when the agent or reviewer had to infer what the harness should prove. Backpressure Check is advisory over this inventory; do not add a core backpressure command.",
    "safety": "Long-running boot is locked down by default: 'run' requires --execute to actually spawn the process; 'validate' never invokes 'run' under any tier."
  }
}
