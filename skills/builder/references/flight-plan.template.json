{
  "_comment": "Full 12-node source seed: research → plan → impl-guide → phase-1 → review-1 → post-flight → ship, plus five mandatory/human-declinable harness chores. Product intent and implementation architecture are separate. The canonical flow is the only lifecycle; builder advance checks its departure. Guide/decomposition exit does not require future contract code. Phase gates read bare-ordinal tasks. Whole-plan plan-validate is armed only at post-flight EXIT after closeout evidence, never at review. Expansion preserves that placement. Pass --plan-dir to anchor relative DD addresses. Chore shape is mirrored in the doctrine-parity:039 block; source schema/template overrides support isolated tests before packaging.",
  "cursor": "research",
  "nodes": [
    { "id": "research", "type": "research", "label": "Research", "status": "in_progress",
      "command": "/builder 1a explore", "next": ["plan"],
      "instructions": [
        "Explore how the relevant code works before you plan — start cheap, climb only when the evidence demands it; never jump straight to a plan.",
        "Run /builder 1a explore \"<intent>\" — it produces research-dossier.md, a decision packet (not a transcript) with evidence links.",
        "Done when the dossier names the core decision the plan must resolve and the findings that bound it."
      ] },

    { "id": "plan", "type": "plan", "label": "Plan", "status": "assumed",
      "command": "/builder 1b plan", "next": ["impl-guide"],
      "instructions": [
        "Specify WHAT/WHY, observable acceptance criteria and scope in plan.dd.json; architecture, contracts, owners and composition belong to the separate implementation guide.",
        "Run /builder 1b plan \"<intent>\" (add --simple for a one-phase flow); it front-loads the clarifying questions, then auto-runs /validate-v2.",
        "Product intent is ready when plan.dd.json has resolved claims and constraints; this is not baseline readiness or proof. Never pre-check future work.",
        "Scaffold and fill through the CLI: harness plan new <slug> --title \"<title>\" --phase \"<title>\", then node_modules/.bin/ddocs set / node_modules/.bin/ddocs add (add --mint ac for criteria). Validation runs BEFORE every write and the sibling rebuilds in the same operation — never hand-edit either file.",
        "Create the flow with harness flow create flight-plan --plan-dir \"<plan dir>\" — without it the baked gate addresses stay plan-relative and every departure refuses E441."
      ] },

    { "id": "impl-guide", "type": "impl-guide", "label": "Implementation guide", "status": "known", "zone": "preflight",
      "command": "/builder 4 guide", "next": ["phase-1"],
      "instructions": [
        "Run /builder 4 guide --plan <plan path>; initialize assets/impl-guide.dd.json with harness builder guide <plan> --init and author through node_modules/.bin/ddocs.",
        "Declare injected contracts, write/read maps, capability-to-AC proof, dependency waves, explicit composition and a justified solo-pm or coders decision. Maps guide coder and PM work, not source-access permission.",
        "Run harness builder guide <plan> --check and obtain independent decomposition review. Ownership-only findings are visible warnings, not guide invalidity or readiness vetoes. Real malformed data, ambiguous identities and invalid executable checks remain errors. Structural validity is not architectural judgement; do not claim unavailable cross-model review ran.",
        "Exit after guide/decomposition approval, not after the future code baseline. The PM implements, commits and seals the shared contract baseline during implementation before any dispatch.",
        "Use the canonical assets/backpressure.dd.json recipe: actual RUN/EXTEND/BUILD/ABSENT selection, AC and assertion pressure links, observed proven_by links; certainty Partial|Confident|Proven."
      ] },

    { "id": "phase-1", "type": "phase", "phase": 1, "label": "P1: Implementation", "status": "known",
      "command": "/builder 6 implement", "next": ["review-1"],
      "dd_link": { "address": "assets/tasks/phase-1/tasks.dd.json#tasks" },
      "instructions": [
        "You own the PM composition and canonical task/receipt paths named in the reviewed guide; each worker owns its assigned source/test paths. Read the product ACs, unit read paths/owners, shared contracts and selected proof before implementation.",
        "Your job is exactly ONE approved phase. Run /builder 6 implement --plan \"<plan path>\"; log discoveries, tagging any Deferred/Noteworthy a human must see before ship.",
        "Done means the phase's acceptance criteria are met, its selected checks actually ran, the composition is verified at its committed SHA, and the execution log links the observed proof.",
        "Before new dispatch, seal committed contracts with harness builder contracts <plan> --seal --review <receipt>, inspect settings and ready, then send a work packet led by owned paths, read paths, job and done conditions. Follow with its actual pointer/digest. Receiving it starts the unit without a separate release. Optional harness builder self-check <packet> --sha256 <digest> gives warning-only root/source/digest orientation and writes no state.",
        "For an already-running worker, dispatch --adopt-peer <id> with the actual --parent and existing --workspace, retaining kind/role controls; current native OMP requires a full clone. Preserve original adopted ownership/authority, sealed source ancestry, progressed HEAD and WIP. Seed only missing metadata; no spawn, reset, replay or new work grant. Existing durable dispatches are inspected/reused, not resent. Native observations are not provider identity attestation.",
        "Import committed deliveries with harness builder compose <plan> --import <deliveries>; it verifies current evidence bindings, actual tree/branch/commit, distinct peers and sealed ancestry, not startup receipts or transport outcomes. The PM wires and commits, then uses --verify <sha>. Guide, coder and PM map deviations remain file/owning_unit/stage warnings for independent review; out-of-map edits need no approval or justification. Real Git, structural and product-check failures remain failures. Import alone is not proof. Preserve historical receipts without replay or reclassification. Solo execution never substitutes for requested independent review.",
        "If every supplied unit is already integrated, compose --import <deliveries> --already-integrated proves frozen map projections equal current committed PM HEAD, falling back to delivery-touched paths only when no concrete mapped paths exist. Empty scope or missing/mismatched evidence is failure. Compare modes/types/Git objects and absence; preserve original worker SHAs without reapplying changes. This is scoped equality, not product proof: compose --verify and independent review still follow. --already-integrated requires --import and conflicts with --verify.",
        "For already-integrated work followed by PM edits, pass --integration-sha <ref> with --import --already-integrated. Resolve a commit between sealed source and current HEAD, compare projections at that point, and retain it as integration_sha without checking it out. Current --verify measures the later PM delta with warnings; do not use detached HEAD or rewrite temporary-checkout receipt roots.",
        "Optional inspection: harness builder on-track <plan> [--unit <id>] [--from <ref>] [--to <ref>] [--untracked]. No readiness, seal, review or receipt prerequisite, no writes, exit 0; show compared, warnings, issues and basis/full SHAs. Default includes tracked staged/unstaged work; --untracked explicitly adds new paths; explicit --to is committed-only. Unit history includes all touched paths, even reverted writes; PM uses endpoint changes. Without --from, PM prefers imported integration, then sealed source, then HEAD; a unit uses sealed source or HEAD. Malformed basis is an issue, not fallback; unavailable comparison returns compared:false.",
        "Advisory maps do not authorize global/deployed changes, unrelated workspaces, changes on main, pushes, PRs, merges or destructive actions. Obtain the user's authorization; keep canonical lifecycle writes with the PM.",
        "Use node_modules/.bin/ddocs to maintain assets/backpressure.dd.json, assertion pressure and AC proven_by links to actual execution-log entries; never mark an unexecuted test as proof.",
        "Mutate task state through the CLI, never an editor: node_modules/.bin/ddocs set \"<plan dir>/assets/tasks/phase-N/tasks.dd.json#tasks/tk-XXXX/state\" checked (and #done_when/tk-XXXX/dw-XXXX/state for each assertion). The .dd.md sibling is GENERATED — editing it is drift.",
        "Rehearse this node's gate before you try to leave: harness plan validate \"<plan dir>/plan.dd.json\" --address \"<plan dir>/assets/tasks/phase-N/tasks.dd.json#tasks\". Gate-terminal states are checked, human-skipped, na; unchecked and blocked hold it.",
        "Spend tokens where they can change the outcome; use `--quiet` for routine flow mutations.",
        "Match model tier to work: chores -> cheap subagent, analysis -> Opus-class, judgement -> lead; context may override (§ Shared conventions).",
        "Environment friction is work, not an apology: fix small things, otherwise `harness observe` it — is this environment working for us? (builder SKILL.md invariant #14)"
      ] },

    { "id": "review-1", "type": "review", "label": "Review: P1", "status": "assumed",
      "command": "/builder 7 review", "next": ["post-flight"], "zone": "flight",
      "instructions": [
        "Review exactly ONE phase's diff against its acceptance criteria — confirm the tests prove behaviour and nothing risky slipped in.",
        "Run /builder 7 review — it inspects the newest assets/reviews/*.md and surfaces a verdict; REQUEST_CHANGES loops back to fix, clean continues to the next phase or post-flight.",
        "Done when the verdict is recorded (one finding line) and either accepted or routed to a fix-loop.",
        "Review the exact composed artifact and current plan/guide basis. Record an independent composition ReviewReceipt with harness builder review <plan> --receipt <path>; unavailable review stays unfulfilled.",
        "Close only criteria proven by observed receipts through node_modules/.bin/ddocs. Whole-plan --complete is not a review-exit gate: closeout work is still future work."
      ] },

    { "id": "post-flight", "type": "post-flight", "label": "Post-flight", "status": "assumed",
      "command": "/builder 7b post-flight", "next": ["ship"], "zone": "postflight",
      "dd_link": { "address": "plan.dd.json", "check": "plan-validate" },
      "instructions": [
        "Close out the flight BEFORE any ship: every phase and review is done; the terminal harness harvest fires at this edge, then the plan folder is archived — this stage runs whether or not the work ever ships.",
        "Run /builder 7b post-flight --plan <plan path>: write factual closeout progress and evidence, then harness builder close <plan> --survivor <path> --allocations <path> --evidence <path> preserves and archives. Land seam receipts before relocation.",
        "Whole-plan completion gates this node's EXIT after closeout evidence: harness plan validate <archived plan> --complete must be green. Never pre-check closeout to leave an earlier review.",
        "If the gate refuses and departing anyway is the HUMAN's decision, they pass --force — it records a defended override. An agent may not force a dd gate on its own judgment.",
        "Tidy is separate: harness builder tidy <allocation> --preservation <receipt> re-verifies ownership, runtime release and surviving bytes/refs; external ownership or idle-only evidence cannot authorize removal."
      ] },

    { "id": "ship", "type": "ship", "label": "Ship", "status": "assumed",
      "command": "/builder 8 ship", "next": [], "zone": "postflight",
      "instructions": [
        "Get the work out — push the branch, open a PR (using the repo's PR guidance), watch its CI checks, and report problems; the merge itself is optional.",
        "Run /builder 8 ship --plan \"<plan path>\" — the plan folder was archived at post-flight, so resolve --plan under docs/plans/archive/<ord>-<slug>/; push and PR-open each pause for a separate confirm, and any merge waits for a typed PROCEED.",
        "Done when the PR is open and its checks are green (or the failures are reported with a fix-loop offer); a diverged base hands off to 8c reconcile."
      ] },

    { "id": "backpressure", "type": "backpressure", "label": "Backpressure survey", "status": "known",
      "branch_of": "plan", "next": ["plan"],
      "command": "run /eng-harness-flow --hook pre-coding --json",
      "chore": { "kind": "command", "importance": "optional" },
      "instructions": [
        "Pre-coding seam: survey what the harness can PROVE about the plan's coverage versus what was only eyeballed, then re-plan informed by it.",
        "Run /eng-harness-flow --hook pre-coding --json — a REAL invocation. Canonical survey assets/backpressure.dd.json uses local node_modules/.bin/ddocs and feeds both product intent and the implementation guide.",
        "Select RUN/EXTEND/BUILD/ABSENT explicitly; link every AC and task assertion by pressure, and actual execution evidence by proven_by. Certainty is Partial|Confident|Proven, never inferred success.",
        "On done, receipt first, status second: harness flow comment --path <flow-path> --node backpressure --kind validation --source agent --text \"decision:<…> verdict:<…> time:<…> basis_sha256:<full SHA-256 of the plan file>\" THEN harness flow status --path <flow-path> --node backpressure --to done. Mandatory unless the human declines — a decline is: harness flow comment --path <flow-path> --node backpressure --kind decision --source user --text \"<their verbatim words>\" THEN harness flow status --path <flow-path> --node backpressure --to skipped; never a narrated skip.",
        "Basis is the LATEST plan: on each guided entry recompute the plan file's SHA-256 — a mismatch with (or absence of) the recorded basis_sha256 re-opens this seam via a deterministic new chore, id backpressure-<first 12 hex of that SHA-256> (full hash in its receipt; same bytes re-use this node + receipt, changed bytes create exactly one new node; never resurrect this one — D5).",
        "Router missing, or envelope noop/UNAVAILABLE → same two calls with the detection receipt/envelope as the text: harness flow comment --path <flow-path> --node backpressure --kind validation --source agent --text \"decision:unavailable reason:<…> time:<…>\" THEN harness flow status --path <flow-path> --node backpressure --to done — a completed attempt, not a skip."
      ] },

    { "id": "boot-1", "type": "harness-boot", "label": "Boot check", "status": "known",
      "branch_of": "phase-1", "next": ["phase-1"],
      "command": "run /eng-harness-flow --hook pre-flight --json",
      "chore": { "kind": "command", "importance": "recommended" },
      "instructions": [
        "Pre-flight seam: confirm the harness is healthy before you start coding this phase.",
        "Run /eng-harness-flow --hook pre-flight --json — a REAL invocation; narrate its envelope verbatim, never fabricate one. A manual `harness boot` (or any prior evidence) never satisfies this seam.",
        "Spend tokens where they can change the outcome; use `--quiet` for routine flow mutations.",
        "Match model tier to work: chores -> cheap subagent, analysis -> Opus-class, judgement -> lead; context may override (§ Shared conventions).",
        "On done, receipt first, status second: harness flow comment --path <flow-path> --node boot-1 --kind validation --source agent --text \"verdict:<…> time:<…>\" (router missing / envelope noop/UNAVAILABLE → text \"decision:unavailable reason:<…> time:<…>\" — a completed attempt, not a skip) THEN harness flow status --path <flow-path> --node boot-1 --to done. Mandatory unless the human declines — a decline is: harness flow comment --path <flow-path> --node boot-1 --kind decision --source user --text \"<their verbatim words>\" THEN harness flow status --path <flow-path> --node boot-1 --to skipped."
      ] },

    { "id": "observe-1", "type": "observe", "label": "Observe: P1", "status": "known",
      "branch_of": "phase-1", "next": ["phase-1"],
      "command": "harness observe \"<what>\" --kind <kind>",
      "chore": { "kind": "command", "importance": "recommended" },
      "instructions": [
        "Mid-phase capture: record observations (decisions, gotchas, surprises) AS you code, so they don't evaporate as remembered prose.",
        "Run harness observe \"<what>\" --kind <kind> — a real capture, never a narrated one, repeated through the phase; it stays due the whole time the cursor sits on this phase.",
        "Spend tokens where they can change the outcome; use `--quiet` for routine flow mutations.",
        "Match model tier to work: chores -> cheap subagent, analysis -> Opus-class, judgement -> lead; context may override (§ Shared conventions).",
        "Terminalize at phase end BEFORE the retro drain, receipt first, status second: harness flow comment --path <flow-path> --node observe-1 --kind validation --source agent --text \"captures:<count> pointer:<real capture record> time:<…>\" THEN harness flow status --path <flow-path> --node observe-1 --to done — done only after at least one real capture. Human decline instead: harness flow comment --path <flow-path> --node observe-1 --kind decision --source user --text \"<their verbatim words>\" THEN harness flow status --path <flow-path> --node observe-1 --to skipped.",
        "Environment friction is work, not an apology: fix small things, otherwise `harness observe` it — is this environment working for us? (builder SKILL.md invariant #14)"
      ] },

    { "id": "retro-1", "type": "harness-retro", "label": "Retro: P1 (drain)", "status": "known",
      "branch_of": "phase-1", "next": ["phase-1"],
      "command": "run /eng-harness-flow --hook post-coding --json",
      "chore": { "kind": "command", "importance": "recommended" },
      "instructions": [
        "Post-coding seam: drain this phase's observe buffer into durable retro notes before moving on.",
        "Run /eng-harness-flow --hook post-coding --json — a REAL invocation; narrate its envelope verbatim.",
        "Runs after this phase's observe chore is terminalized (real capture receipt, or human decline). On done, receipt first, status second: harness flow comment --path <flow-path> --node retro-1 --kind validation --source agent --text \"verdict:<…> time:<…>\" (router missing / envelope noop/UNAVAILABLE → text \"decision:unavailable reason:<…> time:<…>\" — a completed attempt, not a skip) THEN harness flow status --path <flow-path> --node retro-1 --to done. Mandatory unless the human declines — a decline is: harness flow comment --path <flow-path> --node retro-1 --kind decision --source user --text \"<their verbatim words>\" THEN harness flow status --path <flow-path> --node retro-1 --to skipped."
      ] },

    { "id": "retro-harvest", "type": "harness-retro", "label": "Retro: post-flight (harvest)", "status": "known",
      "branch_of": "post-flight", "next": ["post-flight"],
      "command": "run /eng-harness-flow --hook post-flight --json",
      "chore": { "kind": "command", "importance": "recommended" },
      "instructions": [
        "Post-flight seam: the terminal long-horizon harvest — fold this flight's learnings into durable memory at the pre-ship close-out, BEFORE the plan folder archives (so it runs even on work that never ships).",
        "Run /eng-harness-flow --hook post-flight --json — a REAL invocation; narrate its envelope verbatim.",
        "On done, receipt first, status second: harness flow comment --path <flow-path> --node retro-harvest --kind validation --source agent --text \"verdict:<…> time:<…>\" (router missing / envelope noop/UNAVAILABLE → text \"decision:unavailable reason:<…> time:<…>\" — a completed attempt, not a skip) THEN harness flow status --path <flow-path> --node retro-harvest --to done. Mandatory unless the human declines — a decline is: harness flow comment --path <flow-path> --node retro-harvest --kind decision --source user --text \"<their verbatim words>\" THEN harness flow status --path <flow-path> --node retro-harvest --to skipped."
      ] }
  ]
}
