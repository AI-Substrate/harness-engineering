# Backlog — numbered defect/idea ledger (prime-written, single file)

One row per item; numbers never reused. Status: open | packet | done | wontfix.

| # | opened | what | status |
|---|---|---|---|
| 48 | 2026-09-07 | GUIDE-NOT-POLICE (builder compose integrationFence, gibbon): the fence allows only plan-dir paths or PM-unit `paths` between the last imported lane and the candidate (composition-service.ts ~490), so a PM's MECHANICAL formatting pass over coder files (cargo fmt) is refused E477 'Undeclared PM integration changes' — hit on plan 098 (17 coder paths) and again on Unisphere Plan001 (kotallo, candidate 0b119041). Supported today: coders format inside their own fence before delivery (the lanes are live; re-import), and a PM doc like AGENTS.md is declared in the PM unit's paths via a reviewed guide edit. Ask: accept a DECLARED mechanical pass — the PM names the formatter, compose re-runs it on the candidate and accepts the delta only if the formatter is then a no-op (format-only diff), recorded in the composition receipt; and E477 already names paths (good) — add 'format-only?' to its details so the agent can tell RUNTIME GAP CONFIRMED (kotallo): after import there is NO supported amendment — imports are single-shot (composition-service.ts:622-626) and any guide edit trips verifyBuilderBasis drift (:175-186) — so a PM who formatted worker files post-import can only REVERT those hunks and defer formatting/docs to a post-lifecycle follow-up commit. The declared-mechanical-pass feature is the only future route; until then the stage text must warn: never edit worker paths after import PROOF CORRECTION (gibbon, accepted): 're-run the formatter to a no-op' proves the candidate is FORMATTED, not that the delta is format-ONLY (an already-formatted semantic edit passes). Correct proof: apply the declared formatter+config to the authorized PRE-pass tree in isolation and require the resulting tree to equal the candidate exactly; only then label format-only FIX IN PROGRESS (mine, branch feat/builder-integration-amendment): new verb `harness builder amend <plan> --sha <head> --path <p>… --reason <why>` records an IntegrationAmendment {sha, paths, reason, declared_by} on the composition receipt (no schema change — dd tolerates the extra field, so consumers' staged schemas stay valid); verify's fence accepts exactly those paths for exactly that candidate; declaration refuses non-HEAD SHA, frozen contract paths, paths outside the import→candidate delta, empty reason. Scope authorisation, not proof (gibbon's distinction); the independent composition review reads the real bytes with the declaration. Tests: lifecycle suite covers accept / wrong-SHA still refused / frozen refused / unchanged refused → PR #200 (feat/builder-integration-amendment, ca874f41; CI dispatched 2026-09-07). Merge and rebuild are separate Jordan authorizations | packet |
| 47 | 2026-09-07 | GUIDE-NOT-POLICE (builder prework ack, gibbon): E474 'checkout changed' fired on all 3 Unisphere lanes because OMP's Serena tooling writes `.serena/.gitignore` + `.serena/project.yml` into a fresh clone at STARTUP — native-harness bootstrap metadata, not product work (kotallo, prework-ack-dirty-diagnosis.json). The exact-seed untracked rule is right for product paths; it should not fail on a known harness's own startup files. Supported path today: clone-local `.git/info/exclude` for `/.serena/` (no root/global ignore change) then re-ack — kotallo did exactly that. Ask: dispatch/ack learn per-harness bootstrap metadata (omp: .serena/) as an allowlist, OR the clone provisioner writes that exclude itself for omp seats, and the E474 message names the offending paths so the agent can tell noise from a real change | open |
| 46 | 2026-09-07 | DEFECT (builder dispatch/allocation, blocking Unisphere): every unit dispatch from a builder-allocated plan workspace refuses E470 'Unit allocation requires its live parent…' — dispatch names the governing PEER (--parent <seat>) and acts/builder.ts:257 forwards the provision input unchanged, so reserveAllocation (allocation-store.ts ~489) sees input.parent undefined, even though allocationAuthority already knows how to find the workspace's own allocation through <git-dir>/builder/allocation-ref. Masked in tests: builder-dispatch mocks deps.provision; builder-workspace passes parent explicitly. Fix (mine): the store resolves the parent through the checkout's locator when a unit reservation has none, and refuses E470 naming the locator when the checkout has none; test through the real store + real git (kotallo/scout corroborated the same three sites) FIX: PR #199 (fix/builder-unit-allocation-parent, de7fee4c; CI dispatched 2026-09-07); independent of #198 (different files, both from main). Merge is Jordan's FOLLOW-UP (varl, accepted): add an END-TO-END regression through the act-wired real provision adapter (dispatch → provisionBuilderWorkspace → reserveAllocation) from a managed plan authority, asserting the resolved parent record / canonical plan / unit / base, INCLUDING creator ≠ executing PM (varl allocated, kotallo dispatches) — the store test in #199 proves the allocator, not the previously mocked route; Unisphere's live dispatch after deploy is the first such proof MERGED 45ac8b2b 2026-09-07 (Jordan's 'okay' to '#199?' relayed by varl, scoped to #199); activation (FF + build) still held pending Jordan; open until Unisphere's live dispatch passes the former E470 line LIVE RECEIPT 2026-09-07 07:06:32Z (kotallo): all 3 dispatches exit 0 / status ok on runtime 45ac8b2b — tk-0002→pij-partial-koi, tk-0003→pij-standard-impala, tk-0005→pij-brave-cahir, real parent allocations resolved, distinct clones, omp gpt-6-astra high observed; envelopes assets/dispatch-fixed-tk-000{2,3,5}.json in the Unisphere plan worktree; seats live in pij-rs list. Former E470 line passed end-to-end (creator varl ≠ dispatching PM kotallo) | done |
| 45 | 2026-09-07 | DEFECT (builder dispatch, gibbon): `builder dispatch` requires the PM repo's HEAD to EQUAL the seal's `source_sha` (dispatch-service.ts:1020 verifyRoot with exact HEAD; also :1062 for the clone) while `ready` never checks HEAD — so the moment the PM COMMITS the seal receipt / review evidence (which the workflow demands as 'committed proof'), every dispatch refuses E473 'Native Git root or HEAD does not match the frozen workspace baseline' (Unisphere/kotallo, seal 412842e → evidence commit 4a6fdfe → 3× E473). verifyRef already proves every frozen file digest on disk equals the seal, so HEAD-equality adds nothing for source integrity and forces evidence to stay UNCOMMITTED across the whole fan-out (plan 098 ran with 148 uncommitted evidence files — same cause). Ask: allow HEAD to be a DESCENDANT of source_sha when all frozen digests still match (verifyRef) and record the dispatch HEAD in the dispatch record; until then the supported sequence is seal → dispatch all units → commit evidence, and the stage text must say so FIX: PR #198 (fix/builder-dispatch-ancestor-head, cf05657f; CI dispatched 2026-09-07) — plan-root check = same root + sealed source is HEAD or an ancestor; coder clone stays exact; dispatch record names the observed plan-root HEAD; docs state the commit order. Merge is Jordan's MERGED cd533bb4 2026-09-07 (Jordan's YES to '#198?' relayed by varl, scoped to #198 only); global harness NOT rebuilt yet — runtime activation held pending Jordan; row stays open until Unisphere's live dispatch passes the former E473 line LIVE RECEIPT 2026-09-07: the same 3 dispatches passed the plan-root check on runtime 45ac8b2b; NOTE the PM had held HEAD == sealed source (1ec72134), so the DESCENDANT branch of the new predicate was not exercised live — it is proven by the unit test only; the strict-equality refusal that blocked earlier no longer exists in the shipped code | done |
| 44 | 2026-09-07 | DOCS/RECIPE (builder, gibbon): a re-baseline after an approved review refuses E472 'Existing baseline receipt is immutable and cannot be reused' — correct behaviour (receipts are immutable) — but the recipe implies overwrite, so the PM reaches the refusal AFTER obtaining an independent review, then must pick a fresh guide.baseline.receipt identity, commit, and get ANOTHER focused review (Unisphere/kotallo: r4 approved → E472 → baseline-v6 → r5). Ask: the reseal recipe states the order explicitly — select the fresh receipt identity in the guide FIRST, then inputs/review/seal — and the E472 next_action already says so; put it in the stage text too REFINED ASK (varl): make it deterministic, not just documented — the read-only contracts/guide PREFLIGHT should diagnose an already-occupied immutable receipt destination BEFORE the PM solicits review of a changed seal set; avoids one whole review round per reseal DESIGN RULE: preflight diagnostic before the expensive review; never a new gate | open |
| 43 | 2026-09-07 | UX (builder ready, gibbon): E471 'Dependencies lack committed proof: <unit>' names the unit but NOT which `paths` entry is missing from the seal or which `proof` address is not a guide check id (contracts-service.ts dependencyEvidence, ≈494–540) — so a PM cannot tell 'seal is incomplete' from 'guide paths/proof are mis-shaped' from 'composition really is required'. Unisphere/kotallo hit it on tk-0002/3/5 with a sealed, approved baseline. Ask: list the failing path(s)/proof(s) per dependency in details and say which of the two predicates failed; keep the composition fallback message only when both pass CONCRETE CASE (resolved on their side): tk-0001 paths listed `.gitignore` while the guide's authored `baseline.files` (what `contracts --seal` freezes) omitted it — one path, and the refusal said 'needs composition'. Diagnosis converged independently by prime (read-only, 20 sealed paths vs 10 unit paths) and PM; fix = add the unchanged file to baseline.files, focused re-approval, reseal, retry ready. The gap is exactly the missing detail in the message DESIGN RULE: the refusal names cause + repair; where safe, prefer a warning that lets ready proceed | open |
| 42 | 2026-09-07 | IMPROVEMENT (builder seal/ready, gibbon): a checked done_when assertion can keep pointing at an execution-log entry (lg-0001) whose frozen SOURCE has since changed; only an independent human/AI review caught 3 such stale proofs on Unisphere Plan001 (varl/kotallo; repaired to lg-0002 in 8eafef5). Deterministic ask: at seal/readiness, refuse (or warn) when a proof link's recorded basis no longer matches the source it proved — dd ALREADY has the mechanism (basis ledger + `ddocs link verify-basis`, E434 stale citation); builder should record a basis for each proof link and read it back, with an explicit allowance for evidence-only commits (plain HEAD equality would self-reference and loop). Not a defect; the review gate provided the backpressure this time DESIGN RULE (Jordan 2026-09-07, 'we are not the police'): ship as an EARLY WARNING with a repair path, not an approval gate | open |
| 41 | 2026-09-07 | EXTERNAL (pij skill vs rs daemon): the /pij skill's completion-compact discipline (C3) tells a parent to compact a finished peer via a pij send with command:compact; pij-rs v1 send REFUSES — 'pij-rs does not expose remote-control commands in the v1 send request' (Unisphere/varl after their Opus reviewer finished; no typed /compact fallback, no fake success — correct). So on rs the C3 instruction cannot be executed as written: either rs grows a remote-control verb or the skill's C3 text must name the rs-era action (a plain '/compact' message body for harnesses that honour it, or leave the peer uncompacted and record it). pij owns both halves; routed to the pij prime via gurgeh | open |
| 40 | 2026-09-07 | DEFECT (builder, #195; same family as row 37): the packaged impl-guide template keys its capability rows `cp-0001..3`, but the dd writer's mint registry has NO `cp` prefix — `ddocs add …#capabilities --mint cp` refuses E454 (registered: ph,tk,ac,bp,lg,dw,fn,fd,vd). So the template shows an id shape the tooling cannot mint; a consumer must hand-roll ids (forbidden by the skills' own rule) or key rows by product AC ids (what Unisphere/varl did — one-to-one with ac-XXXX, schema-valid). Fix (gibbon owns): either register `cp` in the mint registry (dd package, needs a bump) or change the template + guide stage to key capabilities by their AC id and say so. Routed to gibbon 2026-09-07 | open |
| 39 | 2026-09-07 | EXTERNAL (OMP, same package as row 38): OMP task (sub)agents SHARE one persistent Python eval kernel — parallel agents overwrite each other's globals (Unisphere/varl: a `fixture_bytes` written by one reader agent was clobbered by another's call mid-test). Any fleet running parallel OMP subagents with Python evaluation must namespace variables / keep transforms local-scope and verify fixture provenance before accepting a test result; a green from a shared kernel is not the agent's own green. Not a Builder or native-reader defect | open |
| 38 | 2026-09-07 | EXTERNAL (not ours — OMP = @oh-my-pi/pi-coding-agent, installed from npm, no local repo/prime): OMP's bash tool in async mode persists the command's output to <n>.async.log and TRUNCATES any line at 768 chars — a long JSON line comes back as invalid JSON before any summariser touches it (Unisphere/varl, pplx_research.py --json, 2026-09-07, JSONDecodeError on direct file read). Standing workaround for every omp seat: redirect the command's own stdout to a file INSIDE the command and read the file; never treat the async log as the artifact. Same family as row 34's lesson (exit 0 + silence ≠ complete) | open |
| 37 | 2026-09-07 | DEFECT (builder, #195): `harness builder new` seeds assets/impl-guide.dd.json from skills/builder/templates/impl-guide.template.json, which is a fully POPULATED teaching example ('Text conversion: injected services and explicit composition', fan_out.decision=coders, skills/builder/examples paths) — so a fresh plan carries someone else's architecture that reads as chosen product architecture unless the reader already knows it is a template. Found by the first external consumer (Unisphere, varl) during requirements-only intake; they left it unapproved rather than dispatch example scope. Source: guide-service.ts:40 copies the template verbatim. Fix (gibbon owns): `new` writes an EMPTY guide with a draft/template marker in meta (or no guide until `guide --init`, which the bundled docs already say owns that file); keep the worked example under examples/, not as the seed. Routed to gibbon 2026-09-07 EVIDENCE 2: `harness builder ready` correctly REFUSES the seeded plan (E471: empty AC contract + guide references to unknown ac-0001…) — the gate holds, so the template cannot be dispatched by accident; the seed is still wrong | open |
| 36 | 2026-09-07 | FIRST EXTERNAL CONSUMER of `harness builder new` (#195): Unisphere, seat pij-female-varl, Jordan-directed, requirements-only scope. Evidence: (a) `--workspace ../sibling` refused E477 'escaping path', absolute sibling path succeeded — BY DESIGN (allocation-store.ts:440 refuses any `..` segment lexically; next_action already names the absolute-sibling contract) → not a defect, a UX datapoint for gibbon's evaluation; (b) local ddocs absent + npm proxy 404 / registry ENOTCONN (this machine's MSFT-proxy-only npm, not harness) → the harness-bundled @ai-substrate/dd 0.1.0 fallback engaged and `--help` verified — the fallback design held on its first real miss. Allocation al-001-4437fdce; no pushes, no global changes. Unisphere reports further gaps here DATAPOINT 3 (kotallo, PM): `harness builder advance --now phase-1` from an eval subprocess → E475; the caller resolves from PIJ_SESSION_ID, else `pij whoami` (review-service.ts:84), and whoami is ambiguous from a shared folder; exact retry with PIJ_SESSION_ID set succeeded. UX: the refusal's next_action does not name PIJ_SESSION_ID — it should | open |
| 35 | 2026-09-05 | telemetry capture-service cannot name an omp/pi run: harness detection is an ENV chain (COPILOT_AGENT_SESSION_ID / CURSOR_CONVERSATION_ID / CLAUDE_CODE_SESSION_ID, capture-service.ts:98) and omp/pi export nothing — so `harness commit` telemetry on an omp coder (gibbon's 098 canary, audit 2026-09-05) has no harness/session. The data now EXISTS: rs rows carry `session` for omp (req-0033 rs half landed; verified `pij-rs list --json` → pij-natural-gant.session, and explicit `harness convo sync --harness omp --session` delivered 79 turns). Fix = the same registry seam as row 27 (resolve pane/seat → session through pij), behind the union-verb version floor; do NOT read ~/.pij-rs/pij.sqlite | open |
| 34 | 2026-09-05 | CORE DEFECT: every JSON envelope over 64 KiB piped to a reader was cut at EXACTLY 65536 bytes with exit 0 (gibbon, plan 098 `plan validate --complete --json`; known as a workaround note since 07-31, never tracked). Cause: `exitWithEnvelope` calls process.exit straight after emit and pipe stdout is ASYNC on macOS/Windows (sync on Linux → CI blind to it; a CI runner is not a user machine). 130 call sites, many non-tail, so returning-instead-of-exiting (F002) would double-emit. Fix: bin entry makes pipe stdio blocking (`_handle.setBlocking(true)`) before main(); unit test + real-pipe integration test with a 250-AC plan (negative control on main's dist: 65536; fixed: 257782). PR #194 (CI dispatched 2026-09-05) MERGED b4c45d2e 2026-09-06 (Jordan: 'yeah merge it', executed by gibbon with an exact-head guard); global harness 0.14.0 rebuilt from it and proved through a pipe (257782 bytes). pij CLI has the same shape and is NOT fixed | done |
| 33 | 2026-09-05 | `builder/backpressure` schema `certainty` enum is `Partial|Confident|Proven` — NO Weak rung, while the stage's derivation has three (Strong/Partial/Weak); PR #193 maps Weak→Partial (the ABSENT count stays legible in rows). Widening the enum is cheap in-repo but `~/.dd/schemas` (the home root every other repo resolves against) has NO paved refresh path — a stage writing `Weak` would be REFUSED (E451) on every machine until someone hand-copies the schema. Ship: a `harness`/`just` verb that installs `.dd/schemas` to `~/.dd` (like install-skills), THEN widen the enum. Evidence: `ddocs schema list --json` from a no-.dd repo → root=home; E451 reproduced in scratch 2026-09-05 | open |
| 32 | 2026-09-05 | Backpressure survey was Markdown-only in the stage while every consumer (tasks stage, plan schema `pressure`, exemplar, plans 080/081/137) expected `assets/backpressure.dd.json` — plan 071 dogfood catch #4 said 'schema won; reconcile in ph-7002' and it never landed; a PM on plan 137 followed the stale stage and marked assertions `not-applicable` (Jordan caught it; gurgeh routed here). Fixed in PR #193 (stage → dd recipe + same-stroke `meta.backpressure`/AC `pressure` links; tasks path `../../` not `../../../`, which resolved to a missing target). NOT DONE until deployed: `just install-skills-from-source` after merge — the deployed tasks stage was ALREADY stale (said `dd` where main says `ddocs` since #184) RESOLVED via #195 (s098 builder delivery, merged 2026-09-07 at 7194a379) which folded #193's contract into one shared skills/builder/references/backpressure-recipe.md; #193 closed as superseded. Deploy still pending (gibbon holds the install baton) | done |
| 31 | 2026-09-02 | POST-BASE-MOVE materialisation gap (voxel o-prime boa; s8-clouds merge + WxKit #52). INVARIANT: a base move brings files referencing derived artefacts the tree has NEVER materialised, and NO ladder rebuilds them — invisible to both the unit suite and `harness checks --quick`, so a green ladder is not evidence for this class. **The class SPLITS, and a fix built on the loud half alone catches only half (boa's own correction to their evidence, verbatim):** **LOUD** — the artefact is required at boot, so its absence ABORTS (s8-clouds: `.godot/imported/` 0 entries → boot aborted naming `B612-Regular.ttf`, misattributing the fault to the branch just merged; `godot --headless --import` → 12 entries, clean). One boot detects it, and detects unenumerated future members. **SILENT** — the artefact feeds a SCAN or COUNT, so its absence reads as a legitimate zero (WxKit: an unrestored project yielded ZERO engine-boundary matches, indistinguishable from a clean boundary; it did not crash, it went BLIND). No boot detects this; the only defence is the consuming check REFUSING to conclude on an empty input — an allowlist needs a non-emptiness partner. The silent half is the expensive one. ACCEPTANCE CRITERION: covers BOTH halves — a boot AND an inventory of derived-artefact caches compared against what the new base references. Interim: boa holds the ritual (`dotnet restore weather/WxKit.sln`, `godot --headless --import`, one boot) and RULED not to escalate — Jordan's ship order today, exposure is a small known set of worktrees, stream-merge calls are boa's. Do not escalate; keep queued. | open |
| 30 | 2026-09-02 | `harness commit` on a CONNECTED ingress named attribution as MISSED (fs3 coder DL-009, fs3-jobs-retention, commit c5242ea, 14:20; fs3 row 156): collector accepted the commit, `refs/notes/ai` never written. The verified-or-named contract held; the CAUSE (accepted-but-no-note) is ours — same seam as rows 13 and 2 (commit-service.ts:496 overclaim; report-note-as-data). Chase: does the collector ack before the note write, and can the note fail after ack? EVIDENCE 2 (2026-09-05, gurgeh, fs3-copilot-native-extension worktree): commits ffa81a63 + 29965794 — ingress connected, refs/notes/ai absent after 5000ms, buffer:null; PM kept 'degraded', did not claim attributed. | open |
| 29 | 2026-09-02 | Backfill the last 3 days of pij work into fs3 once lynx row 117 lands: two waves — pilot of 10 rs seats read back by BOTH governments via lynx's `conversation verify`, then the rest serially off-peak. Claude seats only until req-0033 | open |
| 28 | 2026-09-02 | Review of #190 (deferred, pre-existing #188 code): (a) node-background.ts attaches the 'error' listener AFTER the `pid == null` throw → a spawn failure (ENOENT/EACCES) leaves an unhandled 'error' event; masked today only by same-tick process.exit — attach listener before the throw; (b) the losing `clock.sleep(250)` race branch is never aborted (AbortController pattern exists in sensors/runner.ts); (c) `exitCode` became REQUIRED on the published `ctx.background.spawnDetached` type with no API bump/CHANGELOG — make optional or document feature-detect; (d) FakeBackground `exitCode` never settles and has no seed, so DOA tests hand-roll stubs; (e) node-background.ts JSDoc I5 still says 'returns child.pid' | open |
| 27 | 2026-09-02 | rs-generation pij seats have NO route to conversation ingest for pi/omp (271 of 287 seats on lynx's count): rs rows carry no inner session id and pi/omp export no env var. Blocked on pij req-0033 (persist session at adopt/spawn/register; `pij sessions --json` unions both stores with `generation`; whoami answers under rs). When it lands: consume `pij sessions --json` behind a version floor — NEVER read `~/.pij-rs/pij.sqlite`. Owner weasel; ETA days (2026-09-02) EVIDENCE (2026-09-05, gurgeh, omp seat on rs): #190's stderr line fired — 'PIJ_SESSION_ID unresolvable in registry' despite a live rs seat — exactly the pi/omp gap; still blocked on req-0033. | blocked |
| 26 | 2026-09-02 | fs3 `--pij` intake route is blind the same way (shells legacy-only `pij sessions`; 250/252 unresolvable) — lynx row 121, ruled fs3 owns intake identity; version floor once req-0033 lands, standing instruction NOT to build a second store reader in fs3 | external |
| 25 | 2026-09-02 | `PIJ_DAEMON_GENERATION=rs pij list` SILENTLY serves legacy (1198 rows, exit 0) — a caller trusting the nav note's rule 4 gets the wrong store with no signal; reported to weasel as F1. `pij-rs list` is the only rs view and prints one ~150KB JSON line (F2) | external |
| 24 | 2026-09-02 | Row 22 (`--verify` read-back) now waits on lynx's `conversation verify --harness <h> --session <id>` (binding deliverable of their row 119): repo-unscoped by construction. Do NOT ship a `--verify` on bare `conv:` get — it is silently worktree-scoped and its miss lies (`--repo all` trap, measured by lynx) | blocked |
| 23 | 2026-09-02 | Row 21 PARTIAL via #190: convo identity now env-first (`CLAUDE_CODE_SESSION_ID`) so claude seats resolve with no pij; seam prints ONE stderr line on consent-on/identity-unresolvable instead of discarding. Cause found: 245/248 rs seats absent from `~/.pij`, seam discarded every miss for 3 days with no signal (2026-08-30→09-02). pi/omp → row 27 | partial |
| 1 | 2026-08-30 | Port `harness team new`/`team tidy` from fs3 (#37/#40 there); include tidy stash-rescue of dirty files to a named rescue dir (lynx D3 scar + our s090 trap) | open |
| 2 | 2026-08-28 | attribution: `direct-verified` overclaims — report note CONTENT as data (design: scratch/attribution-verify-fix-design.md; 6 samples, 0 known-good — needs synthesized positive control) | open |
| 3 | 2026-08-28 | eval-modality telemetry blindness: omp `eval` FILES-dict writes invisible to write/edit surveys (2nd instance of xd:// class) | open |
| 4 | 2026-08-29 | s077 branch family: 157 unshipped commits, 0 upstream-equivalent; needs content-equivalence audit before keep-or-reap (fan-out candidate) | open |
| 5 | 2026-08-28 | bun U1 blocker: bun 1.4.0 cannot install github-pinned @ai-substrate/dd (IntegrityCheckFailed); dissolves if dd publishes to npm | open |
| 6 | 2026-08-30 | fs3 dogfood: ask silently answers the nearest answerable question (team new → harness new) with citations; search archive-over-source ranking (FS-1 x4) — relayed to lynx | open |
| 7 | 2026-08-27 | ci-verdict ruleset pin (not ci-required) on ruleset 20537412 — awaiting Jordan | open |
| 8 | 2026-08-30 | plan 091 phase 2 proving packet: services/settings + harness convo sync | packet |
| 9 | 2026-08-30 | pij spawn cwd: RESOLVED mechanically (`cd <wt> && pij spawn` — pane takes shell cwd); --cwd flag now optional polish | done |
| 10 | 2026-08-30 | settings.local.json self-gitignore WRITE path — deferred from phase 2 by ruling (no owner among frozen units); interim: U3 ships the .gitignore ENTRY only | open |
| 16 | 2026-08-30 | fs3 ingest envelope echoes the CALLER'S INTENT (dedupe_key carries requested folder) not the RESOLVED STORE — coral's fixture 'worked' while ingesting the real 46k-line session; daemon resolves via ITS env, client HOME controls nothing. Relayed to lynx: envelope should name the store path it read | open |
| 22 | 2026-08-30 | convo sync: optional --verify (or verify-on-explicit-command) doing the read-back delta and RAISING the envelope claim to delivered — today the message honestly stops at dispatch (DOA proves survived-grace, not delivered; the +delta receipts were out-of-band). Lynx requested; keep seams fire-and-forget. SECOND INSTANCE (dajeil): fs3 ACCEPTS an ingest for a nonexistent session (ok:true, accepted) and fails silently at the drain (ingest_session failed x3, no conversation created) — validated-at-accept is not validated at all; a --verify zero-delta is exactly the missing signal. fs3 half routed to lynx by dajeil | open |
| 21 | 2026-08-30 | convo sync auto-identity: resolver demands PIJ_SESSION_ID env, which fresh non-pij shells never carry — while `pij whoami` resolves the same seat WITHOUT it, so the information exists in a form the resolver does not ask for (dajeil). Fix shape: resolve the way whoami does | open |
| 20 | 2026-08-30 | CORE DEFECT, high: a repo extension whose name collides with a core verb ABORTS THE ENTIRE CLI (E100, even doctor dies — reproduced in fs3 after convo shipped). Loader must SKIP the duplicate with a degraded envelope naming BOTH registrations. Recurs every time core grows a verb; current failure mode is total outage naming neither file | open |
| 19 | 2026-08-30 | test/services/hooks/live-daemon-note.int.test.ts is LOAD-SENSITIVE (passed isolated x2, failed under 353-file concurrency at 11.4s; live daemon socket + git notes, timing-bound) and lives in the scope ONLY CI runs — a flake nobody sees until it blocks a merge. Discriminated from regression by coral (base-vs-branch + isolation). If red on a merge: rerun the file isolated before believing it | open |
| 18 | 2026-08-30 | ONE public contract (composition-root command list) asserted in TWO fixtures that do not know about each other (app.test.ts + test/index.test.ts) — drift trap; asymmetric failure: focused-tests seat ships green, full suite/CI breaks (vulture, found at 091 integration; coral MEASURED: both are ORDER-SENSITIVE toEqual over program.commands at index.test.ts:62 + app.test.ts:175, and the duplication is COPY-PASTE — the same plan-074 comment verbatim at the same list position in both. The ORDER is partly deliberate (commit-after-doctor documented), the duplication is not; preserve the first, kill the second when unifying) | open |
| 17 | 2026-08-30 | sandbox DB leak is a KILL consequence, not a sandbox one (coral): graceful stop drops the minted DB (verified — its two gone, one stranger row left untouched); encode 'stop gracefully' in smoke harnesses, retire the drop-by-hand chore | open |
| 15 | 2026-08-30 | Fresh linked worktree has no node_modules → 8 sensors exit 127; ~20min tax per fresh coder seat, every fleet (vicuna, 4 obs, 2 waves, 2 independent coders; evidence: pij s108/rust-port done-wave-{4,5}.md). Fix candidates: team-new port runs npm ci; or sensors detect absent node_modules and say so in ONE honest line | open |
| 14 | 2026-08-30 | harness boot hides its diagnostic on failure (same evidence set). Spot-check: boot's gateNote = last 4 stderr lines when checks stdout unparseable — a 127 from missing deps renders empty/cryptic rather than naming the cause. Needs the failing-layer NAME in the envelope (which parser refused — the lesson from lynx F4) | open |
| 13 | 2026-08-30 | commit-service.ts:496 `detail` overclaims: '...landed, SO the collector recorded this commit's authorship' — inference dressed as observation; JSON fields honest, human string not (two audiences, two epistemic grades). Fix: end at 'landed.' ONE LINE. Deferred past W2 close (U3 owns that seam file); fold into row 2's fix | open |
| 12 | 2026-08-30 | Lesson (coral, self-reported): a name in two flag positions is a TAXONOMY not a synonym (pi = harness AND bin); read `<verb> --help` before theorising about the verb | open |
| 11 | 2026-08-30 | pij models vs omp models CONTRADICT on sol-fast thinking levels (pij: none..max; omp: '-'); -1m rows are omp-runtime-only and a bare pi spawn 400s on them (coral, Seat A DOA) — report upstream | open |
