# Orchestrator prompt — md-to-pdf

Drive the-flow over pij for the subject session: explore → plan → validate →
compact → implement → review → fix → validate. Capture the resolved
`pij_session_id` for the `flow-eval score` run. (Fixture — Phase-2 test bundle.)
