---
schema_version: "1.2"
retro_id: "2026-08-09T08:33:48Z-agent-080p3"
agent: agent
plan_id: "080-dd-consume-upgrade"
started_at: "2026-08-09T06:05:09.546Z"
ended_at: "2026-08-09T08:35:00Z"
summary: "retro --drain at the 080 phase-3 boundary (6 entries) - the deletion phase: the fork died, and most of the yield is measurement/authoring discipline caught live"
entries:
  - id: DL-001
    kind: difficulty
    description: "Stale-render corpus: a dd schema field added mid-plan (57d8bd1f added satisfies_toward to .dd/schemas/builder/plan) regenerates ONLY the sibling of the document its author touched; every other document already rendered against that schema stays silently stale. 10 of 32 tracked .dd.md were stale for the rest of plan 080 and nothing detected it, because no gate sweeps 'every .dd.md still matches the render of its .dd.json' - dd build --check is per-file and only ran when someone happened to invoke it. Encodable fix: a corpus-wide render-drift check (the dd-docs drift guard's shape, aimed at *.dd.md) so a schema change surfaces every document it invalidates at the moment it lands, not phases later."
    target: project-sensor
    severity: degrading
    workaround: "phase-3's full regeneration surfaced all 10; the 22/10 split commit recorded cause vs consequence"
    suggested_encoding: "corpus-wide render-drift check aimed at *.dd.md (prime named the class: a schema change has a REGENERATION BLAST RADIUS)"
    fp: "44884447ae12"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T06:05:09.546Z"
  - id: DL-002
    kind: difficulty
    description: "which('dd') false-green trap: doctor's uniform toolchain pattern (proc.which) would report our CLI PRESENT on every POSIX host via /bin/dd - a permanent false green inside the very warning built to prevent that confusion. Fixed by probing node_modules/.bin/dd; a test pins it against future 'simplification' back to which()"
    target: project-sensor
    severity: degrading
    suggested_encoding: "pinned by test in 22c89534 - already encoded; kept for the record"
    fp: "8445a872f0d7"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-09T08:33:40.586Z"
        resolved_by: "22c89534 (probe + pinning test)"
  - id: DL-003
    kind: difficulty
    description: "lint-leg-vs-CI-leg gap: npm run lint is 'biome check harness/cli' which does NOT surface the organizeImports ASSIST class - only 'biome ci' does. The leg kept running clean was not the leg CI runs; a blocking gate broke invisibly until the ci form ran"
    target: tooling
    severity: degrading
    workaround: "coder baselined via a throwaway git worktree at d7daf94c and repaired to baseline-identical"
    suggested_encoding: "align npm run lint to the biome ci form, or a checks leg running it locally"
    fp: "253f989b9a55"
    disposition: task
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T08:33:40.908Z"
  - id: DL-004
    kind: difficulty
    description: "commit-pathspec discipline: in a shared multi-seat worktree, 'git add <paths> && git commit' commits the WHOLE INDEX - pathspec discipline on add does not protect the commit; 261 staged deletions rode a ledger commit. Cure adopted: git commit -- <paths>, always, in shared trees"
    target: tooling
    severity: degrading
    workaround: "accept-not-rewrite ruled; mapping recorded in ledger + PR body; practice adopted and fired usefully on first use"
    suggested_encoding: "AGENTS.md rule; candidate harness commit wrapper requiring pathspecs"
    fp: "9962830640d9"
    disposition: task
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T08:33:41.239Z"
  - id: DL-005
    kind: difficulty
    description: "a stated done_when test can be VACUOUS: dw-001f's 'dd validate shows zero declared-but-absent sections' would have passed untouched (dd validate does not report absent sections at all) - the coder drove the real E450 instead. A done_when whose named probe cannot fail is a fake gate authored in good faith"
    target: harness-itself
    severity: annoying
    workaround: "coder substituted the real probe at execution time and logged the vacuity"
    suggested_encoding: "authoring rule: every done_when probe names the RED it would produce; validate-v2 checks probe falsifiability"
    fp: "ea3efae1fc61"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T08:33:41.531Z"
  - id: INS-001
    kind: insight
    description: "prose lacks a forcing function: the coder's CODE resolved node_modules/.bin/dd correctly (it had to run) while their PROSE recommended unsafe npx dd in the same report - three seats then endorsed the prose unchecked through two wrong remedies. Executable statements self-verify; prose claims need the same verification as defects, with a positive control"
    fp: "d823b8ee1173"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T08:33:41.830Z"
system:
  compound:
    bubble_action: "all-save"
---

# Retro — 080 phase 3 (fork drain, deletion, cleanup)

Six entries at the deletion boundary. The phase's structural yield: DL-001 (the
regeneration blast radius — prime owns the instance, the corpus-sweep check is the
encodable fix) and DL-004 (the commit-pathspec rule, learned at the cost of 261
deletions riding a ledger commit). DL-002 is already encoded (test-pinned in-phase).
The authoring-discipline pair — DL-005 (vacuous stated probes) and INS-001 (prose has
no forcing function) — both feed the same upgrade: claims and probes need falsifiers
named at authoring time, whether they live in a done_when or a report paragraph.

Also on the record from this phase, ruled rather than drained: the 22/10 regeneration
split, the provenance accept-not-rewrite, three npx remedy rounds converging on
no-change-today, the stash fleet rule, and Jordan routing the dd CLI naming question
(crab's shadow sharpening) to the dd team. Durable homes: dogfood-ledger rows 5/6 +
Materiality/provenance sections; execution log; ledger drift-surface section.
