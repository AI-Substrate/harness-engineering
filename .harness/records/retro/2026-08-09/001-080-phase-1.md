---
schema_version: "1.2"
retro_id: "2026-08-09T03:25:00Z-agent-080p1"
agent: agent
plan_id: "080-dd-consume-upgrade"
started_at: "2026-08-09T01:49:33.304Z"
ended_at: "2026-08-09T03:25:00Z"
summary: "retro --drain at the 080 phase-1 boundary (3 entries) — dd-native builder dogfood frictions, all three routed to durable homes before the drain"
entries:
  - id: DL-001
    kind: difficulty
    description: "dd-native WRITE gap: harness dd set/add both E450 on any schema-declared section missing from the document, and plan new seeds only 6 of 22 declared sections - so the writer verbs cannot author most of builder/plan without hand-seeding the JSON. rm can remove a section; nothing can create one."
    target: harness-itself
    severity: degrading
    workaround: "seed-then-validate: hand-seed empty sections in plan.dd.json, then dd build + plan validate to prove, then writer verbs for content"
    suggested_encoding: "dd: implicit-create on declared-but-absent sections (ratified upstream, unshipped); harness: plan new seeds ALL declared sections (fr-0002 harness half, phase 3)"
    fp: "5bca3da0dbe1"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T01:49:33.304Z"
  - id: DL-002
    kind: difficulty
    description: "No scaffold verb exists for non-plan builder documents (backpressure/execution-log/review dd docs) - plan new scaffolds only the plan+tasks pair, so a backpressure.dd.json must be hand-authored then validated. Same family as the E450 section-create gap: the write path assumes documents into existence."
    target: harness-itself
    severity: annoying
    workaround: "hand-authored backpressure.dd.json against the resolved schema, proved with dd validate + dd build"
    suggested_encoding: "a generic dd scaffold verb (dd new <schema> or plan new --with backpressure) seeding required sections from the resolved schema"
    fp: "870d17b24c0e"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T02:08:38.919Z"
  - id: DL-003
    kind: difficulty
    description: "plan validate reports a checked task satisfying a deliberately multi-phase acceptance criterion as a 'contradiction' WARN - satisfies is binary, so a phase-1 task that legitimately only PARTIALLY earns an AC has no way to say so; plan validate is permanently degraded mid-plan, eroding the signal exactly when a phase boundary wants a clean read."
    target: harness-itself
    severity: degrading
    workaround: "none absorbed - surfaced to dd same-day"
    suggested_encoding: "adopted: open-namespace satisfies-toward relation (ref-behaving) via optional builder/plan tasks[].satisfies_toward field; dd builtin partial-relation ruling still open upstream"
    fp: "d50355dadb4d"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-09T03:01:27.460Z"
        resolved_by: "commit satisfies-toward adoption on s080/dd-consume-upgrade (schema field + 5 edges moved; plan validate ok 0/0)"
system:
  compound:
    bubble_action: "all-save"
---

# Retro — 080 phase 1 (dd consume-upgrade, dd-native builder dogfood)

Three frictions, all with the harness product itself (this IS the harness repo, so all
route to local fixes / the 080 dogfood ledger rather than upstream issues). DL-001 and
DL-002 are one family — the dd-native write path assumes documents/sections into
existence — and their fix rides phase 3's plan-new bundle plus dd's ratified
implicit-create. DL-003 was fixed in-flight via the satisfies-toward convention (crab's
control-armed measurement), with the builtin ruling still open at dd. Durable homes:
assets/dogfood-ledger.md entries 1/2/4.
