---
schema_version: "1.2"
retro_id: "2026-08-09T05:20:37Z-agent-080p2"
agent: agent
plan_id: "080-dd-consume-upgrade"
started_at: "2026-08-09T03:13:40.456Z"
ended_at: "2026-08-09T05:22:00Z"
summary: "retro --drain at the 080 phase-2 boundary (7 entries) - the OQ-2 trial phase: one win (gate refused in anger), six difficulties, all routed"
entries:
  - id: WIN-001
    kind: win
    description: "WIN: a dd gate refused a real premature departure - review-1 carried the whole-plan plan-validate gate and blocked nav to phase-2 at 41 findings mid-plan; the fix was the doctrine-prescribed gate relocation (disarm review-1, arm review-3), not an override. First live refusal of the dd-native flow in anger, and it caught a real expander omission of mine."
    fp: "b8a0a88fba71"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T03:13:40.456Z"
  - id: DL-001
    kind: difficulty
    description: "Concurrent committers in one worktree: an uncommitted tracked-file edit can be swept into another seat's 'harness commit -- <paths>' before its author commits it. My dogfood-ledger F001 edit landed inside prime's 57d8bd1f. Nothing was lost here because prime built on it, but the authoring seat's own commit then staged nothing. Mitigation used: write-then-commit immediately, and stage throwaway measurements under gitignored scratch/. Recurred (benignly) at the phase-2 drain boundary: the coder held off buffer appends mid-drain citing exactly this race."
    target: tooling
    severity: degrading
    workaround: "write-then-commit immediately; explicit pathspecs; gitignored scratch for measurements; no buffer appends mid-drain"
    fp: "ada5eb77d872"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T03:14:03.598Z"
  - id: DL-002
    kind: difficulty
    description: "I wrote a boot-2 seam receipt from the PREVIOUS boot's pattern before reading the envelope - the real verdict was ERROR, not degraded. Caught one command later and corrected via append-only comment, but the anti-fake rule exists for exactly this: a receipt is a transcription of the envelope, never a prediction of it."
    target: harness-itself
    severity: annoying
    workaround: "append-only correction comment on the same node, same session"
    fp: "c2fbbcf4f596"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T03:21:42.895Z"
  - id: DL-003
    kind: difficulty
    description: "Type errors in harness/cli/test/** are invisible to every gate: the only tsconfig is harness/cli/tsconfig.json with include:['src'], and harness checks' typecheck step runs tsc --noEmit against exactly that project (.harness/extensions/checks/extension.ts:153). Vitest transpiles tests without typechecking. So any claim a test makes at the TYPE level is inert - it cannot fail. SHARPENED AT THE DRAIN (coder's ask): no longer 'a gap' - a gap that has SHIPPED A HIGH FINDING (review F002: forkReadPlanReadiness in a typeof query, never imported, silently no-oped; both required gates were silent on it and an ad-hoc probe tsconfig had to prove the fix both ways)."
    target: project-sensor
    severity: degrading
    workaround: "ad-hoc probe tsconfig covering the one test file, run both ways, recipe in the phase-2 execution log (deliberately NOT committed - a one-file typecheck would read as coverage the repo lacks)"
    suggested_encoding: "a tests tsconfig (or vitest typecheck) wired into harness checks' typecheck leg so test-level type claims can fail; placement decision for phase 3 or a follow-up plan"
    fp: "40be059b5c55"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T04:35:47.148Z"
  - id: DL-004
    kind: difficulty
    description: "rm -f silently succeeds on a WRONG path, so a mis-resolved cleanup looks identical to a successful one. Bit in phase-2: a chained cd put cwd at harness/ (not repo root), the rm targeted harness/harness/cli/..., printed nothing, and the deliberate control file survived into the full suite run. The plan-semantics boundary test caught it - the guard working as designed."
    target: tooling
    severity: annoying
    workaround: "chain cd per command; absolute paths for destructive cleanup so a wrong path FAILS instead of no-oping"
    fp: "c4a33f7c90a1"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T04:53:57.399Z"
  - id: DL-005
    kind: difficulty
    description: "plan validate E400 conflates 'no plan document' with 'plan present but invalid' - same conflation shape as dd's E450 locate.ts split (ledger #1, dd 012b6bd) one layer up, in a verb whose plan layer harness now owns post-promotion; live consumer evidence: s081 reachability carries an fs.exists probe to work around exactly this merge. 080 deliberately preserves the merge (phase-2 bar = behavioural identity, goldens-pinned); a split is a post-080 design decision and s081's probe matrix + detail strings must be named consumers in any plan that makes it. CORRECTED same-day (prime relay of s081's three-probe re-measure against the real binary; original claim above kept for the audit trail): the merge is NARROWER than first recorded - absent doc = E400 'no plan document', present-but-unreadable-as-dd = E400 'is not a dd document', but a READABLE dd doc failing the plan schema = E407 with a specific message. So the conflation is 'nothing to read' vs 'unreadable', NOT 'nothing to read' vs 'everything invalid'. Blast radius of a future split is smaller; E407 proves the layer can already be specific, which STRENGTHENS the split case; the no-split-in-080 answer and s081's fs.exists workaround are both unaffected."
    target: harness-itself
    severity: annoying
    workaround: "consumers probe fs.exists themselves (s081's live workaround)"
    suggested_encoding: "post-080 design decision: split the codes with s081 named as consumer; NOT phase-3 scope"
    fp: "77cffdd91ab8"
    disposition: deferred
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-09T05:06:07.604Z"
  - id: DL-006
    kind: difficulty
    description: "Stale-measurable claims: a fact observed once gets carried forward as current across later commits without re-measuring. FOUR instances in phase 2 alone, in two distinct sub-classes (folded at the drain, coder's refinement): (A) stale NUMBERS carried across commits - ledger entry 4's contradiction count, review F001, the 'just checks biome red' line - cure: cite the command + sha that produced the number, or re-measure at claim time; (B) stale READ OF ANOTHER SEAT'S TREE - the coder's uncommitted-flips warning, measured true at compose time, false by send time (~90s after filing the rule itself) - citation does NOT cure this one; concurrent-worktree claims need the measurement at SEND time or an explicit 'as of <sha>'. The discipline does not survive as a habit, it needs an instrument."
    target: project-sensor
    severity: degrading
    workaround: "manual re-measure on receipt (both directions: my re-grep of the coder's biome claim, the coder's re-status of my flips)"
    suggested_encoding: "two affordances, one per sub-class: (A) a log/report lint flagging gate-status words (green/red/failing/passing) without a cited command+sha; (B) a pij-send preflight that re-runs git status when a message body mentions uncommitted/dirty tree state"
    fp: "b911b3e68389"
    disposition: task
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-09T05:17:06.932Z"
system:
  compound:
    bubble_action: "all-save"
---

# Retro — 080 phase 2 (the OQ-2 trial: keep-and-promote)

Seven entries at the phase-2 boundary. The headline pair: **DL-006** (four
stale-measurable-claim instances in one phase, two sub-classes with different cures —
the phase's own review REJECT was one of them) and **DL-003** (the untypechecked-tests
gap, no longer theoretical — it shipped review F002, a HIGH). WIN-001 records the dd
gate machinery refusing a real premature departure for the first time in anger.

Dispositions: DL-003 → plan (typecheck-the-tests placement, phase 3 or follow-up);
DL-006 → task (two encodable affordances, one per sub-class); DL-005 → deferred
(post-080 design decision with s081 pre-registered as consumer); WIN-001/DL-001/DL-002/
DL-004 → kept. Durable homes already carrying pieces: dogfood-ledger (E450 family),
phase-2 execution log (probe-tsconfig recipe, DL-004 postmortem), s081's ac-0006
probe matrix (DL-005 consumer evidence).
