---
record_kind: "retro"
harness_version: "0.13.0"
branch: "s065/deterministic-documents"
repo: "https://github.com/AI-Substrate/harness-engineering"
created_at: "2026-08-03T15:06:12.752Z"
agent: agent
plan_id: 065-deterministic-documents
schema_version: "1.2"
retro_id: "2026-08-03T15:08:00Z-agent-p34dd065"
started_at: "2026-08-03T12:32:00Z"
ended_at: "2026-08-03T15:08:00Z"
summary: "P3∥P4 fan-out phase-end drain: two concurrent pairs (Opus5-xhigh coders, Terra-xhigh reviewers) in ONE shared worktree under a custody-window protocol. P3: FIX (2, honesty-class) -> PASS; P4: FIX (2 HIGH radius/severity gaps) -> APPROVE. Full suite 3532/3532. 10 buffered observations drained (3 re-captured after a buffer race ate them)."
entries:
  - id: DL-001
    kind: difficulty
    description: "Cited a nonexistent ratification: P1's depcruise rules landed in commits the prime never read, and I described them as 'ratified in your P1 sweep' to justify a new root-file grant. Silence is not ratification — an unnoticed change cited as precedent for the next compounds without bound, converting the supervisor's blindness into the subordinate's authority. Rule adopted both directions: a grant exists only where the granter SAID something (cite the making message); otherwise the true state is 'not ruled'."
    target: project
    severity: degrading
    workaround: "prime granted anyway (amendment 4) and issued the correction; citation discipline adopted immediately"
    suggested_encoding: "grants cite their making message; 'X has not ruled' is the only honest description of silence"
    fp: "5db849669284"
    disposition: kept
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T12:32:43.998Z"
  - id: DL-002
    kind: difficulty
    description: "Custody-protocol gap: 'git commit --only, never git add' cannot commit UNTRACKED files (pathspec did not match) — every phase whose first commit introduces new files hits it. Fix preserving --only atomicity: git add -N <paths> (intent-to-add, no content staged) then commit --only. Belongs in the packet template, not rediscovered per coder."
    target: skill
    severity: annoying
    workaround: "git add -N then commit --only"
    suggested_encoding: "pair packet git-discipline block gains the add -N line (applied from P5 onward)"
    fp: "302ecd163f65"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T13:05:00Z"
  - id: DL-003
    kind: difficulty
    description: "Custody protocol covered the git index and the two shared files but NOT harness/cli/coverage/.tmp — two coders running just test in one worktree collide there. Worked around with --coverage.reportsDirectory; shared-worktree packets must enumerate ALL shared mutable dirs (coverage, dist, temp) not just source files."
    target: skill
    severity: annoying
    workaround: "--coverage.reportsDirectory per seat"
    suggested_encoding: "custody section template: coverage/dist/tmp dirs listed as shared surfaces"
    fp: "3320a038faad"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T13:40:00Z"
  - id: CONF-001
    kind: confusion
    description: "'Pin cwd at the describe level (CLI_ROOT off import.meta.url)' is necessary but NOT sufficient for multi-root fixture corpora: a dd document resolves its schema from ITS OWN repo root, so live rows over real repo-shaped fixtures must chdir per-row into the fixture root. P2's rows dodged this only because their fixtures carried doc-folder-local schemas."
    target: doc
    fp: "b21debbf9565"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-03T14:05:00Z"
  - id: DL-004
    kind: difficulty
    description: "just test is not env-proof: agent-shell GIT_CONFIG_* injection breaks exec-remote-telemetry-git.int.test.ts (third phase in a row — P1 DL-003, P2 DL-004 recurrences); green requires clearing the injected vars. The fixture should self-skip or neutralize injected env."
    target: tooling
    severity: annoying
    workaround: "env -u the injected GIT_CONFIG_* vars"
    suggested_encoding: "test setup neutralizes injected GIT_CONFIG_* (recurring 3 phases — promote to a real fix in P5 checks work)"
    fp: "e7aef1435351"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T14:20:00Z"
  - id: DL-005
    kind: difficulty
    description: "Shared observe buffer loses entries under multi-agent concurrency: PM + two coders captured to .harness/temp/agent/session-buffer.md simultaneously; the PM's two spawn-limbo entries vanished and the id sequence restarted — read-modify-write clobbering. Multi-seat worktrees need per-seat buffers or append-only capture."
    target: tooling
    severity: degrading
    workaround: "re-captured the lost entries from conversation history before the drain"
    suggested_encoding: "harness observe: append-only writes or per-seat buffer files keyed by PIJ_SESSION_ID"
    fp: "c0ffee5eabuf"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T15:06:04.063Z"
  - id: DL-006
    kind: difficulty
    description: "Spawn-limbo class: pij's default stack layout squeezed peers to 26x5 panes — below the copilot TUI's boot floor — so seats sat 'pending' forever with zero spine events and queued sends never delivering. Two full reviewer waves lost before root-cause via raw tmux pane capture. Remedy: --layout window once the stack is crowded. Canary corollary: a bind probe must assert BOUND (reject DEGRADED/pending/zero-events); grep for 'idle|working' matches a pre-bind seat — a probe that cannot see the opposite."
    target: tooling
    severity: blocking
    workaround: "--layout window respawns; hardened canary predicate"
    suggested_encoding: "pij spawn: warn or refuse when the target pane is below the harness TUI's minimum; canary doctrine gains assert-BOUND"
    fp: "d00d5pawn11m"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T15:06:04.350Z"
  - id: WIN-001
    kind: win
    description: "Shape-directed vs positional segment classification (RESERVED ruling T007d) paid off immediately: P1's positional kind hints misread #phase/brief-class addresses; classifying against the resolved schema shape + data made --resolve correct on the first corpus run."
    target: project
    fp: "1d9176674a4d"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T14:30:00Z"
  - id: WIN-002
    kind: win
    description: "Wedged live seat recovery path proven end-to-end: pane-kill the hung CLI -> pij revive --print for the recipe -> new full-size window -> pij revive --attach + copilot --resume restores the full session context (296k tokens) with zero loss; re-dispatch explicitly rather than trusting the pre-wedge queue."
    target: tooling
    fp: "5eedrev1ve0k"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T15:06:04.955Z"
  - id: INS-001
    kind: insight
    description: "Review honesty-class findings are as valuable as code HIGHs: P3's reviewer caught a universal provenance claim history contradicts (goldens-before-renderer) and four adjudications missing from the log; the fix produced a per-file truth table, RECONSTRUCTED labels on derived evidence, an explicitly-declared unrecoverable, and the coder correcting its own live claim (D4 was its test, not the renderer). An absolute provenance claim is cheap to write and expensive to trust later."
    target: project
    fp: "90ldenh0ne5t"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T14:55:00Z"
---

# Retro — 065 dd Phases 3∥4 (render/adapters ∥ links/ledger/doctor)

First true fan-out: two pairs, one shared worktree, custody windows over the two
shared files (dd.test.ts, .dependency-cruiser.cjs) plus commit --only discipline
— four window rotations, zero collisions, zero index sweeps. P4's review found
two real radius/severity HIGHs (tripwire denominated on loaded-not-scheduled
paths; --path scoping leaking into the interior pass); P3's review found the
rarer thing: an execution log whose provenance claim overclaimed, fixed with a
per-file truth table and a coder correcting its own live report.

Fleet lessons dominate the drain: the 26x5 spawn-limbo class (two reviewer
waves lost), the assert-BOUND canary rule, the proven revive path, and the
observe-buffer race that ate the PM's own captures. Process rulings that should
outlive the plan: grants cite their making message; silence is never
ratification.
