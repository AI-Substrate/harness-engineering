---
record_kind: "retro"
harness_version: "0.13.0"
branch: "s065/deterministic-documents"
repo: "https://github.com/AI-Substrate/harness-engineering"
created_at: "2026-08-03T19:23:32.460Z"
agent: agent
plan_id: 065-deterministic-documents
schema_version: "1.2"
retro_id: "2026-08-03T19:25:00Z-agent-p6dd065"
started_at: "2026-08-03T17:30:00Z"
ended_at: "2026-08-03T19:25:00Z"
summary: "P6 terminal (flow-spine gating) drain: single pair (Opus5-xhigh coder, Terra-xhigh reviewer), three fix rounds (6+2+1 findings, all MEDIUM), APPROVE at 5/5+8/8+6/6 mutations. Task 6.6's inference lens found 5 UX defects no suite could see; the review loop found a repo-wide depcruise type-only blindness and unified the self-defeating-probe class. 8 observations drained."
entries:
  - id: DL-001
    kind: difficulty
    description: "Containment debt (prime-flagged, both bumps as ONE item): two tests in test/integration/docs.test.ts now carry 120_000 timeouts solely because of telemetry capture cost (d531383c byte-for-byte; 68d62225 early-close, measured ~5.16s spawned-CLI vs 5000ms default = ~50% flake; idle ~0.2s so load-dependent path). The suite is being reshaped around an unfixed defect — two is containment, a third means adaptation out-competed the fix. Open with Jordan: synchronous post-commit sync (3.5-4.5s, swallows failures) + per-invocation capture cost under concurrency."
    target: infra
    severity: degrading
    workaround: "granted timeout bumps, counted together as debt"
    suggested_encoding: "fix telemetry capture cost; until then every new bump cites this item"
    fp: "c0nta1nment2"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T17:45:20.105Z"
  - id: DL-002
    kind: difficulty
    description: "test/adapters/git/exec-remote-telemetry-git.int.test.ts asserts on credentialTempDirectories(), which readdirSyncs the SHARED OS tmpdir by prefix — global mutable state that cannot hold while peers run concurrently on one box (this repo's actual operating model). 91/91 in isolation, failed twice inside one checks run. Fix shape: scope the enumeration to a per-run temp root. Second structural defect in the telemetry/test-isolation seam this phase."
    target: tooling
    severity: degrading
    workaround: "none — intermittent; reported not reached-over (outside phase fence)"
    suggested_encoding: "per-run temp root for credential temp enumeration"
    fp: "5haredtmpd1r"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T18:10:00Z"
  - id: DL-003
    kind: difficulty
    description: "Self-defeating probes — THREE instances in one phase, unified: (1) the opt-in byte-identical pin was expect(renderFlow(x)).toBe(renderFlow(x)) — true for every implementation while five goldens changed; (2) the nested-scan mutation passed because no nested files exist today — a walker proven only on the current layout passes BY NOT LOOKING; (3) the config tie test substring-matched raw .cjs text, satisfied by its own commented-out corpse. THE RULE: a probe must be evaluated in the same FORM its consumer evaluates it — require/parse configs (never grep), compare against STORED expected values (never recomputations), prove walkers on BUILT trees. Reviewer detector: any assertion whose subject merely CONTAINS the thing rather than being it."
    target: skill
    severity: degrading
    workaround: "three fix rounds; behavioral tie tests"
    suggested_encoding: "review doctrine + lint heuristic (expect(f(x)).toBe(f(x)) shape); merge into probe-must-see-the-opposite doctrine"
    fp: "5elfdefprobe"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-03T19:00:00Z"
  - id: DL-004
    kind: difficulty
    description: "Static-analysis DEFAULTS are where enforcement bypasses hide: dependency-cruiser's tsPreCompilationDeps defaults to false, so it reads the TRANSPILED graph — EVERY depcruise rule in this repo was silently blind to type-only import edges until P6's boundary work turned it on (free: violation count unchanged). When adding an architecture rule, mutation-prove it in both import forms (value AND type-only) and at a nested path."
    target: tooling
    severity: degrading
    workaround: "tsPreCompilationDeps: true + both-ways mutation proof"
    suggested_encoding: "landed (P6); the lesson generalizes to any analyzer config"
    fp: "t5precompdep"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T18:50:00Z"
  - id: CONF-001
    kind: confusion
    description: "Flow-side gate resolution initially anchored dd addresses at process.cwd() instead of the flow document's repository root (review F005) — the cwd-anchoring class from P2/P3 recurring in its third distinct home. docRepoRoot(flowPath) walking to the nearest .git (including a worktree's .git FILE) is the durable idiom; any persisted repo-relative address must be resolved against the artifact that persisted it, never the invoker's cwd."
    target: project
    fp: "cwdanch0r3rd"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T18:30:00Z"
  - id: MW-001
    kind: magic-wand
    description: "HARNESS_NO_TELEMETRY=1 in a spawned child env is a 43x difference (5.16s -> 0.107s) and NOTHING signposts it — every test author rediscovers it or ships a flake. Wants a documented 'spawning the CLI in tests' recipe (env flag + cwd pinning + both-cwds proof) in the contributor docs."
    target: doc
    fp: "n0telemetryd"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-03T18:05:00Z"
  - id: WIN-001
    kind: win
    description: "Task 6.6(b) — the inference lens — earned its place: operating the built gate as a user found FIVE defects the fully-green suite could not see (stale 'not yet evaluated' above a fresh reading; refusal ids where blocked and unchecked looked alike; --force baking an absolute machine path incl. username into a COMMITTED flow doc; two 64-char shas on one row; E303 naming one side of a two-sided comparison). Deterministic tests prove behavior; operation finds meaning. The two-lens shape is worth repeating for any human-facing surface."
    target: project
    fp: "1nferencelen"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T18:00:00Z"
  - id: WIN-002
    kind: win
    description: "The gate's structural-agreement seam: deriveSchemaItems and deriveState now project the same collector, so the refusal list and its n/m count are incapable of disagreeing by construction — agreement by shared source, not by parallel maintenance. The same principle closed P5's duplicated E-code maps. 'Two artifacts that must agree should be projections of one source' is dd's own thesis, applied to dd's implementation."
    target: project
    fp: "5tructagree1"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T18:45:00Z"
---

# Retro — 065 dd Phase 6 (flow-spine gating, terminal)

The terminal phase: the flow spine became dd's first gating consumer, opt-in by
construction (a throwing evaluator proves absence), refusing departures with
E44x, recording --force as the human's defended decision, and surfacing gate
state everywhere position is read. Three fix rounds, all findings MEDIUM, all
real: an SDK-seam violation that produced a proper public seam plus a
repo-wide depcruise fix; an injection vector from hand-editable flow JSON; the
cwd-anchoring class in its third home; and the self-defeating-probe class
caught three times and unified into one rule.

Jordan's 6.6 two-lens dog-food was the phase's distinctive win: the
deterministic lens locked the journey into just test; the inference lens found
five human-surface defects green tests cannot see. The pair loop's culture is
now visibly self-propagating — the coder mutation-tested its own first-run
green, confessed its own redundant guard, and reported fence-external defects
with measurements instead of reaching over.
