---
record_kind: "retro"
harness_version: "0.13.0"
branch: "s065/deterministic-documents"
repo: "https://github.com/AI-Substrate/harness-engineering"
created_at: "2026-08-03T12:18:19.441Z"
agent: agent
plan_id: 065-deterministic-documents
schema_version: "1.2"
retro_id: "2026-08-03T12:20:00Z-agent-p2dd065"
started_at: "2026-08-03T11:00:50.690Z"
ended_at: "2026-08-03T12:20:00Z"
summary: "P2 (schema layer & baked docs) phase-end drain: pair ran Opus5-xhigh coder + Terra-xhigh reviewer, two fix rounds (F001 undocumented scan-depth cap vs D14; F002 error-swallowing shared adapter defeating the loud-failure trade, with a second phantom-E412 face), APPROVE at 5/5 mutations. 11 buffered observations consolidated to 8 entries."
entries:
  - id: DL-001
    kind: difficulty
    description: "Fence-drafting miss recurred one phase later (P1 retro DL-002 class): the P2 fence named src paths but not test/acts/dd.test.ts, whose five stub-expectation rows necessarily redden when the P2 command bodies go live — needed a mid-run fence amendment again. Rule: a fence for 'fill the stub bodies' work must include the stub-expectation/enumeration tests for any stub going live that phase, replacement-only. Corollary ruled during the round: a row-level freeze pins the ASSERTIONS, not the file's setup scaffolding."
    target: skill
    severity: annoying
    workaround: "mid-phase fence amendment 1 (five rows replacement-only, frozen rows byte-identical)"
    suggested_encoding: "pair packet template: stub-goes-live => its expectation tests in-fence; row-freeze != file-freeze"
    fp: "ddeed98836f1"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T11:00:50.690Z"
  - id: DL-002
    kind: difficulty
    description: "Two peers sharing one worktree share one git index: coder staged 38 files by explicit pathspec, PM's 'git add <one file> && git commit' swept the entire index under an unrelated one-line message — and PM reported intent, not artifact, until the prime checked. Explicit-pathspec ADD does not protect the add->commit window; 'git commit --only <pathspec>' (atomic stage+commit) does, plus 'git diff --cached --stat' before every shared-index commit. Prime ordered a pre-push split of the unpushed collision commit (reversing its own blanket no-rewrite call: unpushed history is not shared history)."
    target: tooling
    severity: degrading
    workaround: "soft-reset split into honest commits pre-push; --only adopted for all subsequent commits"
    suggested_encoding: "pair packet git-discipline block: git commit --only + pre-commit staged-diff check"
    fp: "24a63056ac41"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T11:47:00.000Z"
  - id: DL-003
    kind: difficulty
    description: "Telemetry per-invocation cost is LOAD-DEPENDENT, not a flat tax: idle, harness docs --list is 0.26-0.28s with telemetry vs 0.22-0.24s without (~0.2s), but ~3.9-4.1s was measured for the same invocation class under full-suite concurrency — 40x spread. Direct cause of test/integration/docs.test.ts flaking under vitest's default 5s ceiling (sibling test was already half-fixed with 120_000). Separately, the post-commit hook runs 'harness telemetry sync' synchronously (~3.5-4.5s network push per commit, failures swallowed). Both with prime/Jordan."
    target: infra
    severity: degrading
    workaround: "granted one-line timeout on the flaking test; HARNESS_NO_TELEMETRY=1 for tight measurement loops"
    suggested_encoding: "profile capture path under concurrency; async/deferred telemetry write"
    fp: "b69ce49afcff"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T11:36:29.534Z"
  - id: DL-004
    kind: difficulty
    description: "P1 retro DL-003 recurred verbatim: agent-shell-injected GIT_CONFIG_COUNT/KEY_*/VALUE_* (safe.bareRepository=explicit) breaks exec-remote-telemetry-git.int.test.ts at git config; 91/91 green under env -u. Second phase in a row an agent harness's injected git env broke the full-suite proof line."
    target: tooling
    severity: annoying
    workaround: "env -u GIT_CONFIG_COUNT (unset only injected entries) for full-suite runs"
    suggested_encoding: "test setup neutralizes injected GIT_CONFIG_* so just test is env-proof"
    fp: "8f30327e02b1"
    disposition: kept
    system:
      compound:
        status: open
        source: agent-self
        first_seen_at: "2026-08-03T11:37:00.000Z"
  - id: DL-005
    kind: difficulty
    description: "Two sanctioned suite invocations disagreed and nothing catches it: the repo ships a root vitest.config.ts (root:'harness/cli' — relocates discovery, does NOT chdir), so repo-root invocation is supported, yet 7 dd act tests were red from root and green from harness/cli (fixture paths off process.cwd()). Proof ceiling, not just a test bug: either checks must run the suite from repo root too, or the root config should be deleted. Product resolver proven cwd-independent by same-absolute-path probe from three cwds."
    target: project
    severity: degrading
    workaround: "describe-level cwd pin in the two test files; both-cwds proof made a fix-round requirement"
    suggested_encoding: "P5 checks join: add root-invocation suite run XOR drop root vitest.config.ts"
    fp: "2f00629dd070"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-03T11:52:00.000Z"
  - id: DL-006
    kind: difficulty
    description: "A semantic decision shipped as a bare constant: MAX_SCAN_DEPTH=8 silently bounded dd's schema deep-scan although D14 ruled 'scan until found' with no bound — it read like a tuning knob while changing which schemas resolve, and no fixture crossed the cap so nothing could catch it (review F001, HIGH). Fix removed the cap and added a beyond-cap fixture whose teeth were mutation-proven. Lesson: every invented limit needs a fixture that crosses it."
    target: project
    severity: degrading
    workaround: "review mutation gate caught it; cap removed per D14"
    suggested_encoding: "P5 exemplar/checks: fixtures deliberately cross every invented numeric limit"
    fp: "e15c4a53a06c"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T12:05:00.000Z"
  - id: DL-007
    kind: difficulty
    description: "Reasoned from a port's CONTRACT instead of its IMPLEMENTATION and shipped a defence that could never fire (review F002, HIGH): NodeFs.readdir catches every fs exception and returns [], so the promised loud symlink-loop failure was silently an empty branch — worse, a loop CONTAINING the requested package manufactured phantom E412 duplicates, a hard error blaming the user's tree. An error-swallowing shared adapter is invisible at the type level (string[] either way); every consumer needing 'nothing there' vs 'could not look' inherits the trap silently. A fallback nobody has seen fire is a hypothesis, not a defence. Fixed via dd-owned NodeSchemaFs (ENOENT/ENOTDIR benign, all else -> E416) + real symlink-loop fs test + act-level composition-wiring regression row."
    target: project
    severity: degrading
    workaround: "reviewer's live probe caught it; dd-owned adapter with honest errno split"
    suggested_encoding: "audit shared adapters for error-to-empty conversions; type-level distinction for 'empty vs unreadable'"
    fp: "77f4275a5c2e"
    disposition: fixed-now
    system:
      compound:
        status: encoded
        source: agent-self
        first_seen_at: "2026-08-03T12:14:00.000Z"
  - id: MW-001
    kind: magic-wand
    description: "DdShape.valuesShape for dynamic-key maps: workshop-002's per-task evidence is a map keyed by owning task id (array form collides with E404 id-uniqueness, live-proven), but DdShape has only fields/items — no 'every value of this map has shape X', so dd-core cannot validate map interiors (entry states not enum-checked; deriveState unaffected). Additive valuesShape would close it. Parked as residual A3 for P5 adjudication; P3's render walk may want it too."
    target: project
    fp: "95049c46c915"
    disposition: plan
    system:
      compound:
        status: suggested
        source: agent-self
        first_seen_at: "2026-08-03T11:25:42.231Z"
---

# Retro — 065 dd Phase 2 (schema layer & baked docs)

Phase ran as a pij pair (Claude Opus 5 xhigh coder, GPT-5.6 Terra xhigh
reviewer) under PM pij-related-koala. Two fix rounds, both HIGHs real and
mutation-proven: F001 (an unruled depth cap silently narrowing D14's
deep-scan) and F002 (an error-swallowing shared adapter defeating the
loud-failure trade the coder AND PM had both signed — with a second,
nastier phantom-duplicate face the coder found while testing its own fix).
APPROVE at 5/5 mutations caught.

Process signal this phase: the shared-worktree/shared-index commit collision
(split pre-push under a prime reversal), the fence-drafting class recurring
from P1 (now encoded in the packet template: stub-goes-live tests in-fence,
row-freeze ≠ file-freeze), and the two-sanctioned-invocations proof ceiling
(root vitest config vs cd-harness/cli norm) headed to the P5 checks
conversation alongside valuesShape (A3) and cross-every-invented-limit
fixtures.
