# Builder team delivery — evaluator only

Do not copy this file, assertions.json, evaluation notes or report artifacts into any subject or implementation root. Deliver only subject.md plus native-root and reply-channel instructions. No rubric, worker quota, decomposition boundaries, stage choreography or score hints.

Use the existing pij peer/Builder orchestration protocol. The subject derives its team from its own architecture. Answer task questions without suggesting scored behavior. A method approval may be given when requested; do not nudge skipped stages. Do not repair the subject artifact as evaluator.

Prepare with the locally packaged, committed and reviewed product using `prepare-consumer.mjs`; supply the tarball and the matching installed dependency tree. It does not fetch packages or install skills globally. Preserve preparation.json outside the consumer; its base_sha is the only score base. Pin the package SHA and run this cohort separately from historical md-to-pdf/dd-native-builder cohorts.

Before dispatch, use a separate live OMP positive-control seat: score its native source, inspect the persisted rs row.session, successful Flowspace verification, complete bounded windows and unsupported-field gaps. Missing evidence blocks acceptance, not because it proves failure but because it proves nothing. Never set a vendor capture variable or relabel turns as segments.

After delivery, keep every implementation/reviewer root available. Score from the evaluator checkout with `--evidence-source native --subject-plan <selected NEW plan.dd.json> --base-ref <preparation base_sha> --worktree <consumer>`. `--session` names the subject PM, not the evaluator. Install PyMuPDF into the evaluator's Python environment before the independent PDF lane; missing tooling is unknown, not a capability failure. Read the report's proof-source and capability lanes separately. No numeric score alone is acceptance.

Inspect the generated PDF page PNGs, source nonce documents, parsed text and invocation receipts. Confirm diagram readability and that code matches the claimed unit responsibilities: deterministic path ownership cannot decide architectural merit. Acceptance requires every B-row pass, clean independent composition review and that visual/architecture judgement; no PASS_WITH_NOTES shortcut.

Before any teardown, run preserve-run.mjs against the report.json into a fresh retained directory outside all roots. It archives the subject and workers, Git histories, dependencies and the entire report/evidence directory. Verify that preservation completed; then close only peers you own and remove only recorded disposable allocations. Idle is not closed. Capture evaluator friction and the next encodable check; do not silently discard evidence.
