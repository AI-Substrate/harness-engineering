# Original ask — observe-retro-merge
**Captured**: 2026-06-10T01:53:24Z  ·  **By**: /the-flow

> We're going to look at how to combine observe and retro skills into one. Make sure that we're talking about concepts in them to look out for and extend, for example, we wanna really task it with things like what did the agent have to infer that the harness should have proved deterministically, for example. I wanna make sure that's a strong signal.
> I also want to discuss how presently the observe step, which should be running somewhat in the agent's mind at least, or at least prompted. How it collects evidence as it goes of where it spots frictions and things to make sure that that's not lost as we go along. Do we need a new record type in the harness itself? Or you know, how do we go about that? Probably a record type that we track frictions and things as we go, and then we can harvest our retro from them. We need to be careful we dont lose information if the agent harness does a compaction or otherwise looses data fidelity. Also these inflight "logs" of things should probably ot be committed (not sure how to communicate this kind of thing in to the harness setup prices, maybe prompted in the setup skills what to gitignore and not?).  It's what we used to have, but I wasn't quite sure how to do it in this new world, so we might look to add it back in or adjust it to better operate here. Let's just have a look at what we've got there, look at how we might combine them to become a more generally usable skill that can both observe and record as we go along the various frictions with examples of the friction that we're looking for. And also how it harvests them to create the retros and obviously continues on doing the retro stuff that it's already got. All along the way we should also be the system should be prompted in a way that basically, you know, if something's being inferred and not being done deterministically, that could be token wastage. Or we want to make sure that if we had to figure something out, then you know, that and we do encode that into the environment, then that becomes sort of like a team memory and also you know could reduce token usage. So part of this process is about token usage, it's not the primary thing, but it's also one of our goals, I guess. So let's just talk through this, have a look at what we've got in there, look at how we might combine them to become a more simple concept without watering them down too much to address these new goals and such.

> *(then, after the alignment read of `docs/harness-basics/intro-to-harness.md` + `harness-foundations/simple-mode.md`)*: fire up a flow and pgoress to a vlidated spec stage

## Pre-flow discussion outcome (context for the spec)

Agreed direction from the in-session design discussion (2026-06-10):

1. **Merge is canon-restoring**: the loop is *boot → backpressure check → do-work-and-observe → retro → encode*; observe was never its own stage. Fold `eng-harness-3-observe` into the retro skill — one skill owning the friction lifecycle: notice → hold safely → present at the seam → route to encoding.
2. **The strong signal**: the canonical question pair from simple-mode Rule 5 — the magic wand AND "What did you have to infer that the harness should have proved?" — asked verbatim at retro time and carried as the headline in-flight capture trigger (currently buried as trigger #7 in observe; absent from the drain menu).
3. **CLI-owned capture** (Rule 2 — encode the fix, not the memory): a `harness observe` verb so the CLI owns path, ID counter, timestamp, schema validation, and the `.harness/temp/` + gitignore guarantee. The current observe skill is "code in markdown" — ~190 lines of mechanics the agent re-infers each session/post-compaction. Drift found: the skill *claims* the CLI ensures `.harness/temp/` exists + gitignored; the CLI does not (yet).
4. **Storage classes**: committed records (`.harness/records/`) = team memory; transient observations (`.harness/temp/`) = session scratch, never committed. The record contract's reserved `placement?` hook is the natural extension point. Doctor gains a convention check that `.harness/temp/` is gitignored — answers the "what do we prompt setup to gitignore" question deterministically.
5. **Token framing is already canon**, not scope creep: an inference is a token cost paid per session per machine until encoded; recurrence in the harvest view is the leak detector.
6. **Simplicity bar**: the merged skill must be *shorter* than the two it replaces — mechanics move to the CLI; prose keeps only judgment.
