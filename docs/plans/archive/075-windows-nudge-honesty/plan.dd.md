<!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. -->
# Windows named-pipe honesty: never call an ingress a buffer

**Schema**: builder/plan · **Source**: plan.dd.json · **Sections**: 6

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Windows named-pipe honesty: never call an ingress a buffer |
| slug | 075-windows-nudge-honesty |
| ordinal | — |
| status | ready |
| complexity | CS-2 |
| summary | — |
| backpressure | — |
| log | — |
| mode | Simple |
| plan_version | — |
| created | — |
| confidence | — |
| complexity_breakdown | — |

<a id="summary"></a>

## Summary

Plan 074 shipped telemetry-nudge and harness commit with no platform guard. On Windows, git has no af_unix trace2 target, so the nudge's working branch is unreachable by construction — and worse, ABSOLUTE_TARGET (/^([A-Za-z]:)?[\\/]/) matches a Windows named pipe, so a LIVE INGRESS is classified as a drainable FILE BUFFER. harness commit would then tell a user their events are buffered to a pipe, write a .shas sidecar beside it, and point at a nudge that refuses with a misleading 'is a plain file'. Plan 074's own non-goal claims a 'platform-guarded no-op' that does not exist (win32/process.platform appears zero times in ingress.ts, nudge.ts and commit-service.ts on merged main). This plan does NOT make Windows work — that needs the transport established first. It makes the tools stop lying: a named_pipe target kind, honest unsupported-on-this-platform reporting from both verbs, a real platform guard so the non-goal becomes true, and tests that pin both UNC directions.

<a id="goals"></a>

## Goals

- A Windows named pipe is never classified as a drainable file buffer
- Both verbs report unsupported-on-this-platform honestly rather than issuing an instruction that cannot work
- Plan 074s claimed platform-guarded no-op becomes a fact in the code
- Legitimate UNC file targets keep working exactly as today

<a id="non-goals"></a>

## Non-goals

- Making replay actually work on Windows — that requires establishing what transport git-ai uses there (scratch/gitai/10-cursor-testing-playbook.md section 5) and is a separate, larger piece
- Any change to POSIX af_unix behaviour: the existing suites must pass untouched
- Any git-ai patch or upstream filing (standing instruction)
- DEFERRED, OWNED, NOT A NOTE — the managed AGENTS.md block (commitGuidanceBlock() in services/instructions/commit-guidance.ts) still promises TWO commit outcomes; this plan adds a third (ingress-unverified). Deliberately NOT edited here. Prime ruled option (c) on 2026-08-07: the staleness test is byte-exact (existing === commitGuidanceBlock() ? current : stale), so ANY edit flips every consumer repo's block to stale — and that warning is the ONLY mechanism telling a consumer to re-sync, so firing it is a message, not a side effect. Riding it on a Windows platform fix would give consumers a warning whose cause is undiscoverable from their side. FOLLOW-UP IDENTITY: a separate change, batched with any other pending edit to the same block (ask prime what is queued before filing), whose commit message is written to be findable by a consumer searching why their block went stale. Affordable because doctor only WARNS and never edits (074 ac-0008) — the cost is noise, not breakage.

<a id="acceptance-criteria"></a>

## Acceptance criteria

| id | claim | state | note | receipt | pressure | proven_by |
| --- | --- | --- | --- | --- | --- | --- |
| ac-0001 | resolveTrace2Target gains a named_pipe kind, classified BEFORE the ABSOLUTE_TARGET test: a Windows named pipe (\\.\pipe\x and \\?\pipe\x, case-insensitive) returns {kind:named_pipe} and never {kind:file}. | [x] checked | — | harness/cli/test/services/doctor/collector/ingress.test.ts — 'plan 075 · ac-0001 — a Windows named pipe is an INGRESS, never a buffer' | — | — |
| ac-0002 | PRIME CONSTRAINT, both directions pinned: this SPLITS an intentional case rather than filling a gap. A legitimate UNC file target (\\server\share\trace.jsonl) MUST still classify file. Asserted by a table test covering: UNC file, pipe (both prefixes), drive-rooted path, POSIX absolute, relative, keyword and fd forms. | [x] checked | — | harness/cli/test/services/doctor/collector/ingress.test.ts — the one-table both-directions case; plus commit-service.test.ts 'a legitimate UNC FILE target still takes the file branch' | — | — |
| ac-0003 | PRIME CONSTRAINT, negative testing: the classifier is proven with a KNOWN-BAD fixture, not only inputs it handles — a classifier only run against inputs it handles has been demonstrated, not tested. At least one input that SHOULD be refused/unconfigured is asserted, and a deliberately malformed pipe-like string (e.g. \\.\pipexyz) is asserted NOT to classify as named_pipe. | [x] checked | — | harness/cli/test/services/doctor/collector/ingress.test.ts — 'plan 075 · ac-0003 — the classifier is proven by what it REFUSES' (6 near-miss + 4 refused fixtures) | — | — |
| ac-0004 | telemetry-nudge on a named_pipe target skips with a reason naming the transport and the platform — never no-buffer, never nothing-to-do — performing ZERO renames and ZERO deletes (reuse the existing containment-guard assertions). | [x] checked | — | harness/cli/test/services/doctor/collector/nudge.test.ts — 'plan 075 · ac-0004 — a named-pipe ingress is refused, never drained' (zero renames, zero deletes, zero sends) | — | — |
| ac-0005 | harness commit on a named_pipe target does NOT take the file branch: no GIT_TRACE2_EVENT override, no .shas sidecar written beside the pipe path, and the envelope never claims the events were buffered. It states plainly that attribution was not verified on this platform. | [x] checked | — | harness/cli/test/services/commit/commit-service.test.ts — 'plan 075 · ac-0005 — a named-pipe ingress is never called a buffer' | — | — |
| ac-0006 | A real platform guard exists so plan 074s non-goal becomes true: process.platform is consulted in the drain path and the behaviour is deliberate rather than accidental. The guard lives in src (typecheck include is [src]), and any switch over Trace2Target kind is exhaustive so the compiler refuses a future unhandled kind (the F011 lesson from plan 074). | [x] checked | — | src guard: TRACE2_TARGET_POLICY satisfies Record&lt;Trace2TargetKind,…&gt; in ingress.ts; platform read via IS_WIN32 in nudge.ts + process.platform at the doctor act. Compiler refusal demonstrated (TS1360/TS7053) and reverted. | — | — |
| ac-0007 | Plan 074s non_goals wording is corrected if the guard does not fully satisfy it, and docs/how/gitai-collector.md states the Windows position honestly. No new dependency; every test CI-safe with no real pipe, socket, daemon or network. | [x] checked | — | docs/how/gitai-collector.md § Windows rewritten; plan 074 non_goals corrected + rebuilt; commit-guidance.ts states the third branch. No package.json/lock changes; all new tests use FakeFs/FakeSocketRelay only. | — | — |

<a id="phases"></a>

## Phases

| id | title | brief | state | note | receipt | depends_on | tasks | objective | delivers | key_risks |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ph-3258 | Phase 1: Implementation | Split the named_pipe case out of file, make both verbs report honestly, add the real platform guard, pin both UNC directions with a known-bad fixture. | [x] checked | — | — | — | [tasks](assets/tasks/phase-1/tasks.dd.md#tasks) | — | — | — |
