# Original ask — flow-conformance-eval
**Captured**: 2026-06-29T02:58:01Z  ·  **By**: /the-flow

> We're going to take the concept of /Users/jordanknight/substrate/harness-engineering/agents/validate-harness-flow and make it so we can use FLOW Pair to have an orchestrator guide another agent through an entire flow. This will enable us to check different agents as well as LLMs, so for example Opus or GPT through the entire flow end to end. We will tell it that we are evaluating the flow, but we will not give it any more information as to how it should complete the work, only on how it should report the work to the orchestrator. We will be picking a checkpoint in one of our own repos and we will be adding the same feature every time. We will have an evaluation template that we fill in on every aspect. So for example, did it follow the flow? Did it do the checkpoints right? Did it run various skills? Because remember, we can collect telemetry, so we can see exactly what happened in the session. We will actually review not only the agent's output and its effect on the file system, but we will also review its hotel telemetry. So we'll be parsing that to see exactly what was done. Deterministically, of course, to be able to prove the work was done properly. For example, did it run engineering harness skills? Did it create back pressure checks? Did it run planning and then did it drain the retros and produce a retro report and all those sorts of things. All the stuff that we're talking about with engineering harness flow, everything that we've been doing in our last couple of plans has been leading to this moment of us being able to automatically test this. We will need input prompts to the flow pair agent, we will need evaluation guide for the agent, we will need harness extensions to allow us to grab the Otel output of the "pij" session (we store pij session id in our telemetry, so we should be able to fin the session in local telemetry). We need first class way to check off everything against the list. We need a system so that we can have json that is parsed, then we can have stuff like "called x skill", and "cerated file x, created file y". So its config driven eval of what the agent did. We can also include some lLM interpretation and reporting as part of the report process. Reports will be generated in .harness/live-testing/<slug>/<report>. Later we will be adding more.
>
> The task that we will have it do is create a worktree of this repo (harness engineering) and then add a new extension that can convert a markdown to PDF, including with mermaids and validated outputs.
> The orchestrator will drive the process. The orchestrator and the agent will talk via pij - if there are questions from the agent then it will use pij to ask them. The orchestrator will compact the agent at the end of planning before coding. We will use the-flow, it will set it up, it will run explore, plan, then validate, then compact, then implement, the code review, fix, and validate.
> It will be assessed on its proper use of deterministic back pressure (the host orchestrator llm will nee to assess this and fill in this report field). e.g. did it make a tool to check back pressure.
> During the planning phase, select simple. During back pressure checks, it might offer up some things that the orchestrator can choose what to do (e.g. does it offer up a proper back pressure checker for the pdf?).
