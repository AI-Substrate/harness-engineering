# Original ask — sandbox-attribution
**Captured**: 2026-08-07T01:10Z  ·  **By**: /the-flow (PM seat, session ac636c44)

> you will be pm, get a new plan in for this stuff, but in same branch / PR / worktree please. can just have a second plan. get builder spine in, then run explore which is basicaly just moving our research and couemtntion from our research process from scratch in to that folder, then run the plan please. remember we are using builder flow dd version now!

**"this stuff" — the agreed lean feature set (same conversation, minutes earlier):**

1. `harness commit` — single allowlisted entrypoint: stage, commit, verify-note-landed, fail loud (proven by `commit.mjs`, run 6-E).
2. Verify-after-commit built into it (the braces).
3. Doctor ingress probe — `SocketProbePort` on `node:net`, classify reachable/denied/dead/absent/unconfigured; `ingress-blocked` verdict.
4. Doctor at-risk list — unattributed commits, bounded window, honest wording; plus the `capture-liveness` green-forever fix.
5. `harness doctor telemetry-nudge` — recovery verb called opportunistically by doctor/checks (mechanism pending run-7 lever isolation).
6. One line of agent guidance — commit via `harness commit`, never chained git.

Constraints ruled in-conversation: same branch/PR/worktree as plan 073 (`s073/gitai-collector-v1`, PR #104); lean — no ocean-boiling; dd-native plan (plan.dd.json).
