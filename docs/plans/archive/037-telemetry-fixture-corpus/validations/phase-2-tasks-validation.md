# Validation — Phase 2 tasks dossier

**Target**: `tasks/phase-2-fan-out-copilot-cli-copilot-vscode-cursor/tasks.md`
**Date**: 2026-06-25 · **By**: /validate-v2 (adaptive: lead deterministic proof + 1 independent critic) · **Revision**: dossier as generated by `/the-flow 5 tasks`

## ✅ VALIDATED — no material issues

- **Proof (deterministic, lead)**: every adapter entry point the tasks name resolves in source — `copilot-adapter.ts` (`copilotEventsPath`/`copilotLogsDir`, `assistant_usage` token correlation), `copilot-vscode-adapter.ts` (`copilotVscodeStoreDbPaths`/`resolveCopilotVscodeSessionId` + turns SQL via `DbPort`), `cursor-adapter.ts` (`cursorTranscriptPath`/`cursorStateDbPaths`/`cursorDiskKV` join). `fixture-extract.ts` is new + listed in the plan Domain Manifest. AC-03/04/05 each map to tasks; synthetic fixtures marked immutable (AC-10).
- **Thesis**: advances Phase 2's purpose — reuses Phase-1's proven scrub/extension/e2e/byte-scan patterns across the three remaining capturable surfaces; target proof = actual proof (the goldens+invariants and the SQL round-trip are the oracles).
- **Consumers**: STANDALONE — the dossier is consumed only by the implement verb; no exported shape, no next-phase prerequisite beyond Phase 1 (done).

## Independent critic (Explore, read-only) — disprove attempt

Returned **`no_material_findings`**, each pinned to source:
1. Adapter signatures exist as described (all three adapters).
2. AC-03 token correlation real — `copilot-adapter.ts:384-427` (interaction-id windowed `assistant_usage`).
3. **Cursor `t_precision: 'anchored'` confirmed** — `cursor-adapter.ts:296,306` (untimed transcript → anchored, not exact). T011's assertion is correct; this is the exact class of bug that bit claude's AC-01 in Phase 1, now pre-empted.
4. AC-04 throwaway-`node:sqlite` → read-only `NodeDb` pattern real + reusable (`sqlite-spike.test.ts`); `TURNS_SQL` computes counts/flags in-SQL (no raw message text crosses the boundary).
5. No plan 2.1–2.5 deliverable without a task; no Done-When too weak to verify; no task forces an AC-10 synthetic edit.
6. G6 TDD ordering honoured — `fixture-extract.ts` is test-first (T005 RED → T006 GREEN).

## Notes for implementation (advisory, not blocking)

- The cursor `anchored` expectation and the "fixture selection is load-bearing / pass `--names`" lesson are logged as harness observations (`INS-001`, `INS-002`) and embedded in the dossier's Context Brief.
- AC-05 is only *partially* closed in Phase 2 (fixture + E2E join); its runbook half (plan task 3.3, cursor capture docs) lands in Phase 3 — correct per the plan's Coverage Map, not a gap.
