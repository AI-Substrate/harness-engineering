# FX001 execution log — flow friction fixes

**Date**: 2026-07-10  
**Status**: Complete  
**Delegation**: dlg-0003

## RED evidence

- `doctorEnvelope(report, clock, true)` retained verbose extension objects instead of `{name,status,verbs}`.
- `flow create` and `flow status` left the sibling `.md` absent/stale, and an injected `.md` write failure emitted no warning.
- Focused RED: 4 failed, 38 passed across the doctor and auto-render tests.

## Implementation

| Task | Evidence |
|---|---|
| FX001-1 | `scripts/gen-docs.mjs --check` compares generated bytes, prints `check:docs FAIL — ...; diff follows` before the diff, or `check:docs OK — no drift`; `package.json` routes `check:docs` through it. Both branches were exercised consecutively (first exit 1, second exit 0). |
| FX001-2 | Root `vitest.config.ts` reuses the CLI config with `root: harness/cli`. Full root `npx vitest run --coverage=false --reporter=dot` passed 180 files / 2,283 tests without sweeping `scratch/**`. |
| FX001-3 | `doctor --quiet` now returns terse layer identities plus extension `{name,status,verbs:[names]}` entries. The default envelope is exact-byte pinned; the built repo payload is 1,356 bytes. |
| FX001-4 | The model-to-task rationale now links to the absolute GitHub `rules-of-why.md` URL; the stale `../../../harness-foundations` target is absent. |
| FX001-5 | A shared warn-only post-write helper renders the sibling `.md` after `create`, append-only `event`, and the common mutation pipeline. Tests prove byte equality with manual render, automatic drift-free state, unchanged default stdout, retained `render`, and non-fatal render-write failure. The builder cadence and bundled flow docs no longer require a second render call. |
| FX001-6 | `.harness/engineering-harness.md` names `just test` as the canonical full-suite/coverage command while preserving what it runs. |

## Validation

- Focused GREEN: 64/64 tests across doctor, auto-render, render, and frozen flow-quiet contracts.
- TypeScript no-emit check: clean.
- Biome on changed TypeScript: clean.
- `npm run build`: clean; regenerated `docs-content.ts`.
- `just test`: 180 files / 2,283 tests passed with coverage.
- Root `npx vitest run --coverage=false --reporter=dot`: 180 files / 2,283 tests passed.
- `harness checks`: exit 0; all hard gates green. Expected warn-launch only: `arch-check` (2 findings) and `markdown-lint` (199 findings).
## Review fix (orchestrator, post-FIX_REQUIRED)

- Reviewer HIGH (review.fx001.md): `docs/how/telemetry-fixtures.md:178` still carried the stale `cd harness/cli && npx vitest run …` invocation. **Root cause: the dlg-0003 packet's allowed-paths did not include that file**, so the coder's fence — not its sweep — bounded FX001-6; the miss is the orchestrator's packet, and the fix was applied by the orchestrator (mechanical, matching the reviewer's prescribed remedy).
- Fix: line 178 → `npx vitest run test/services/telemetry/fixture-privacy-scan` (root-safe; **proven live**: 1 file / 35 tests green from repo root via FX001-2's new root vitest config). File is not a gen:docs source (absent from docs-manifest.json) — no regen needed.
- Scoped re-grep (live guides: docs/how/, .harness/engineering-harness.md, skills/, harness-foundations/, AGENTS.md, README.md): clean. Historical plan/review/execution records intentionally exempt.
