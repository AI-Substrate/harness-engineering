# Original ask — flow-token-efficiency
**Captured**: 2026-07-10T01:40+10:00  ·  **By**: /the-flow

> get the new flow up. dogfood our new rules even before we right them. run explore. consider eng-harness-flow skill also in scope for works please. … (so builder and eng-harness-flow) are in scope

Accreted verbatim asks from the same session (the full requirements record is `spine.md` beside this file):

> so something the flow doesnt do well is treat every token as gold, and we dont wat to make that worse. our next task after htis is wording in the flow on write what you ahve to, but consider every token (in planning, implemetiaton too - the flow can sometimes go crazy during impl and burn tokens).

> there is another efficienty domain - using the correct model for the task. e.g. sonnet for searching codebase, but opus or evn fable for code reviews if its deemd hard enough. unles there are overriding commands in the context then the builder flow sould suggest that it does things with differnt sub agents, like code review, rather than just in window. also for git commits and codebase search etc and other chores it soudl just do it using subagents rather thna a) clog up parent window and b) use the espensive model.

> we dont want ponytail itself, but the builder flow should consider if its a good method to help token efficenticy. Tokens hould be treated like gold again, and also token efficiency improvemnt (by movign thing sot detemrinisic layer and perhaps other maans liek subagents etc) is gold too
