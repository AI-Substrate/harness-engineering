<!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. -->
# Phase 2: Round one — the consumer-ranked noise removal

**Schema**: builder/plan · **Source**: tasks.dd.json · **Sections**: 4

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Phase 2: Round one — the consumer-ranked noise removal |
| slug | phase-2 |
| ordinal | — |
| status | draft |
| complexity | — |
| summary | — |
| backpressure | — |
| log | — |
| mode | — |
| plan_version | — |
| created | — |
| confidence | — |
| complexity_breakdown | — |

<a id="summary"></a>

## Summary

Round one as ranked by the consumer. Timeout first — it is the instrument everything else is measured with.

<a id="tasks"></a>

## Tasks

| id | title | domain | phase | state | note | receipt | done | success | notes | satisfies |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| tk-0101 | Raise the test timeout so a stopwatch is not reported as an assertion. THE CONSUMER'S #1 and the highest-leverage change available: ~46 phantom failures, and it is a config line. Do this FIRST — while it stands no other Windows measurement is trustworthy, and it invented three whole files of 'new' failures on the s077 branch. Prefer a global raise over a win32-only one unless there is a reason not to; state which and why. | — | — | [x] checked | — | GLOBAL raise to 30s testTimeout+hookTimeout in vitest.config.ts (not win32-only: the cost is process-spawn contention, which bites loaded Linux/CI too - two files' own measurements say so). Removed three vi.setConfig 20s calls that became DOWNGRADES below the new floor, keeping their docblocks. Controls: 7s case passes (7003ms); same case at --testTimeout=5000 fails; 35s case still reported 'timed out in 30000ms' so the instrument still bites. Expected up to 46 of their FAIL lines, unverified on win32. | — | — | — | [ac-0009](../../../plan.dd.md#acceptance-criteria) |
| tk-0102 | Add platform: 'linux' to the shared deps() helper in nudge.test.ts (~line 92). ~40 failures, one line. Do NOT change nudge.ts — the guard is correct and the composition root passes platform explicitly. Verify the count actually drops rather than assuming; the consumer's grouped signatures are the oracle. | — | — | [x] checked | — | NOT one line - 80% of the class. Shared deps() helper (~92) plus TWO more helpers with the identical defect (nudgeDeps ~801 R2, ~1079 R4), found by negative control not by reading. Also repaired the control at ~1605 that the fix would have broken: it asserted deps().platform undefined AND the POSIX outcome while depending on the host being POSIX - now destructures platform away and asserts per-host, so it proves the fallback on Windows instead of failing over it. nudge.ts untouched. Measured under a process.platform simulation: 39 failed without injection, 8 with the shared helper only, 62/62 with all three. 62/62 on darwin. They counted 40; I measured 39 and am not guessing at the last one. | — | — | — | [ac-000a](../../../plan.dd.md#acceptance-criteria) |
| tk-0103 | Fix the bash CAPABILITY defect in test/support/external-binary.ts, shipped in #118. hasBinary() probes PRESENCE; on Windows 'bash' resolves to WSL bash which cannot run a Windows-path script (exit 127), and 'which node' inside it also fails. Either probe capability or skip post-commit-hook on win32 with a named reason. A presence-only guard that lets a test proceed to fail is worse than none — the declaration makes the gap look handled. | — | — | [x] checked | — | ROUND 2 after terra's P1. The round-1 fix was itself incomplete AND SILENTLY SO: the probe proved native-path execution + shim-on-PATH, but the hook resolves git FIRST and bails with 'exit 0' twice, so a git-blind shell ran the hook to a SUCCESSFUL exit that did nothing and the case failed on the missing marker. Level 1 failed loudly (127); level 2 failed silently. probeShell(shell,{requires}) now proves every resolution property reached BEFORE the observable - native path, each required command resolving/running/capturable via command substitution, a GIT-PRODUCED path surviving [ -f ], and the shim off PATH - in one spawn against a real temp git repo, and returns WHICH step failed so the skip names it. Deliberately does NOT run the hook: a probe that executes its subject reports a broken subject as an environment gap and skips itself green. Controls: git-blind shell ACCEPTED by the old probe (false positive reproduced), the REAL hook under it exits 0 with no marker (consequence demonstrated), the new probe REJECTS it, a git-seeing shell still passes (not merely stricter), and a drift guard fails naming 'jq' when jq is added to the tracked hook. 12/12 in the file; post-commit-hook 4/4. Also fixed a defect I introduced in my own control - it listed the SHARED os.tmpdir and went red under parallelism; now a private TMPDIR namespace, verified non-vacuous by disabling cleanup. | — | — | — | [ac-000b](../../../plan.dd.md#acceptance-criteria) |
| tk-0104 | Skip exec-remote-telemetry-git on win32 with a NAMED reason — 16 failures. The consumer explicitly deprioritised this AS COVERAGE: a real-git-daemon suite that Linux CI covers properly, and on Windows the daemon holds handles past SIGTERM. Name what is unproven on win32 in the skip text, in the shape the consumer praised. | — | — | [x] checked | — | SCOPE NARROWED and PM-RULED (pij-respectable-clam: keep the narrow scope; the brief's 'skip the file' is superseded). Skipped the daemon-driving describe (18 cases) not the file (91). The other 73 are in-process credential/lease/config cases that pass on Windows today and are where the next Windows product defect would announce itself; skipping 91 to quiet 16 makes the instrument blinder, not the signal cleaner. Skip text names the transport, the cause (daemon holds handles past SIGTERM), why it is not reimplementable, and the three claims that stop being checked - while saying Linux CI proves all three, so only the WINDOWS behaviour is unmeasured. Measured: 91/91 on darwin; 73 passed / 18 skipped under simulated win32 with the reason printed. Cost stated rather than absorbed: 18 declared vs 16 they measured failing = 2 cases of coverage genuinely given up. | — | — | — | [ac-0009](../../../plan.dd.md#acceptance-criteria) |
| tk-0105 | Re-measure and hand the consumer a branch to re-run. Report the three columns (fixed / declared / recovered); never a total that mixes them. Expected ~140 -&gt; ~40. | — | — | [x] checked | — | Three columns, never mixed. FIXED: 39-40 (nudge, measured under simulation) + up to 46 (timeouts, their own FAIL-line count; 'up to' because a genuine hang still fails) + 3 (EPERM teardown that stops occurring once the describe skips). DECLARED: 16 (daemon) + 4 (post-commit-hook) - coverage we do NOT have. RECOVERED: 0. Trajectory ~140 -&gt; ~35-40, not claimed: their re-run is the measurement. Denominator 5096 -&gt; 5103 (+7 new control cases). Local gate: full suite 5103/5103 green, just checks every HARD gate ok; the 3 degradeds are pre-existing warn-launch and windows-check's 6 findings are all in html-snap, none in a file this phase touched. | — | — | — | [ac-0009](../../../plan.dd.md#acceptance-criteria), [ac-000a](../../../plan.dd.md#acceptance-criteria) |

<a id="done-when"></a>

## Done when

_No fields._
