# Original ask — harness-bypass-change-records
**Captured**: 2026-06-15T22:24:02Z  ·  **By**: /the-flow

> Need a new flow run. This one is we're gonna add some record types. We also need to prompt our harness flows on this as well, so we need to find a good home in the harness flow, engineering harness flow to record some things. So basically we're adding two new record record types. One is a record of harness bypass. So for example, if the coding agent for whatever reason could not or would not use the harness to do the work, we take a record. This record must be recorded. The next record is harness change, which basically is a rec record of where a piece of work was done that improved the harness. And these coupled with the retro form set of measures that we can then scan all of our company's Git repos for to find out measures that we're going to collect called harness bypass rate, harness change rate. And then we can then basically upstream plug them into other metrics like Dora and figure out if the harness is creating value and if we see a meaningful change in value when the harness is used by teams.
