# Execution log — Phase 2 (Deterministic render + CI parity + docs)

> Plan: [`first-class-flow-system-plan.md`](../../first-class-flow-system-plan.md) · Phase 2 of 3 · TDD-first.
> Mode: plain implement (no companion — a parallel task already holds the `code-review-companion` run; concurrent companions would contend for the same slug/run state). Review stage applies (no Graph supersede this phase).
> Branch: `024-first-class-flow-system` (shared — other agents active; staging is surgical, nothing pushed).

## Pre-flight
- Boot baseline (CLI vitest suite): **719 green / 72 files / exit 0** before a line of code.

## Tasks

### T001 — golden-file render parity tests + fixtures ✅
- `test/services/flow/fixtures/render/flight-plan-024.json` (CLI-shaped 024 flow, provenance + fixed timestamps) and `kitchen-sink.json` (every type/status, decision, harness seams, agents, adversarial chars).
- `flow-renderer.test.ts` golden-parity harness: render each `<name>.json` → assert byte-identical to committed `<name>.md`; drift + idempotency assertions. Goldens regenerated by `gen:flow-fixtures`, never hand-edited.

### T002 — render-rule + safety tests ✅
- Every rule covered: classDefs, spine + solid edges, excursions (dotted), harness-node conditional (`:::harness`), status→class, one 🗣 genesis bubble per node, `comments[]` → `💬N` badge + body-log (source/kind/refs), `decision` rhombus class, agents subgraph (companion) + side node (worker), legend, rail, unknown-type/status fallback, adversarial-input escaping.

### T003 — flow-renderer.ts (pure leaf) ✅
- `FlowDoc → string`; no I/O/Date/process; imports only `flow-events` types. Two escapers (`escapeMermaid` entity-form, `escapeMd` markdown-safe); deterministic ordering (append order). Renders every T002 rule.

### T004 — wire `harness flow render` + `--check` ✅
- `acts/flow.ts` render subcommand after the `event` block: `--path`/`--input` (alias) / `--slug` via `resolveFlowPath`; `--output` (in-repo containment) / stdout (JSON `data.rendered`; human raw passthrough via `emitRawAndExit`); `--check` re-renders + diffs the committed sibling `.md` (`.json`→`.md` same dir, `--against` override) — **read-only**, non-zero on drift via new `E310 FLOW_RENDER_DRIFT`.
- `test/acts/flow-render.test.ts` (10): exit codes, `--output` file-matches-stdout, `--check` match/drift/no-committed, file-unchanged on `--check`, `--against`, path-escape (E303), no-path (E301), human passthrough.

### T005 — check:flows CI step + gen:flow-fixtures + build-order rule ✅
- `scripts/flow-fixtures.mjs` (regenerate default; `--check` verifies via the built CLI). `package.json`: `gen:flow-fixtures` + distinct `check:flows` (`gen:flows` + `git diff --exit-code schemas-content.ts` + `flow render --check` over fixtures). `ci.yml`: "Flows drift guard" step after `check:docs`. Build-before-regen rule documented in the guide + script header.

### T006 — docs/how/harness-flow.md + README pointer ✅
- Full verb surface incl. `render` + `--check`; custom-flow authoring (`--schema` precedence); the `E308` clean-break note; the golden-regeneration / build-first rule. README "What's in this repo" pointer added.

### T007 — reconcile §4 distribution ✅
- `constitution.md` §4 (lines 45 + 164) and `docs/project-rules/architecture.md` §4 (lines 13 + 99): stale GitHub-Packages + `read:packages` token wording → shipped **public-npm / zero-auth** reality (plan 019). Doc-only. Direction locked GitHub-Packages → public-npm; README untouched (already current).

## Self-review remediation (post-implementation gate)
Running the render output through review (tests assert substrings, not mermaid-parse validity) surfaced two real defects — both fixed:
1. **`end` is a mermaid reserved keyword** — a bare `end` node id breaks the diagram. Added `MERMAID_RESERVED` guard in `buildIdMap` (suffixes `end`→`end_`, etc.); test added.
2. **Mermaid labels left `| [ ] { }` raw** — entity-escaped them in `escapeMermaid` (`#124;`/`#91;`/`#93;`/`#123;`/`#125;`) for a clean "cannot corrupt" guarantee; test added.

## Final gate
- Build ✓ · typecheck ✓ · `check:docs` ✓ · `check:flows` ✓ · full vitest **753 passed (74 files)** · `biome check` **170 files clean**.
- Suite delta: 719 → 753 (+34: 21 renderer + 11 act + 1 error-codes table + 1 reserved-word).

## Status vs acceptance criteria
- **AC-06** (byte-identical render + `--check` drift + all render rules incl. genesis bubble, comment badge + body-log, decision class, agents subgraph, unknown-type fallback, injection-safe): ✅
- **AC-12** (distinct `check:flows` CI step; no `check:docs` regression): ✅
- **AC-13** (docs guide + README pointer; §4 reconcile): ✅

> Not committed — held pending user review (user flagged problems to confirm). Nothing pushed.
