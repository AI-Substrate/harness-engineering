# Cursor prompt 1 of 3

You are working in `/Users/jordanknight/temp/attrib-probe` on branch `main`.

`seed.mjs` currently exports a single `one` constant.

Do exactly this:

1. Create `AI.md` — three or four lines describing what this repo contains.
2. Add `export const two = 2;` to the **end** of `seed.mjs`.

**Leave every other file exactly as it is** — do not reformat, reorder, tidy, or "fix"
anything already in this repo, including anything that looks unfinished or oddly styled.
Do **not** touch `HUMAN.md`. Do **not** commit yet.

Use your normal file-editing tools. Do **not** set or export any environment variables.

Do **not** run any `git-ai`, `harness`, or telemetry command, and do **not** inspect,
check, or try to fix anything about authorship attribution or git notes — a collector is
being observed and touching it invalidates the run.

When you are done, report back:

- **which files you personally created or edited, and which line numbers you wrote** —
  this is the ground truth the run is scored against, so be precise
- whether any command or tool call was refused, blocked, or produced a permission error
  — and if so, report the refusal **verbatim** rather than working around it
- anything you noticed that seemed unusual, however minor
