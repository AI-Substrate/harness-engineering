❌ **NEEDS ATTENTION** — 0 critical, 2 high, 0 medium.

| Severity | Finding | Evidence | Impact | Smallest fix |
|---|---|---|---|---|
| HIGH | `harness update` can reinstall stale baked skills after updating the CLI binary. | The plan changes the default skill source to a packaged local dir and says bare `harness update` reconciles recorded targets (`vendor-builder-baked-skills-plan.md:13`, `:19`, `:108`, `:157-158`). Current `harness update` first runs the binary update (`update.ts:350`) and then calls `skillsOutcome` in the same running process (`update.ts:358`). Today that skills refresh uses `DEFAULT_SKILLS_SOURCE` (`update.ts:185`), which is currently a GitHub source (`contract.ts:15`); the plan changes that to the current package's baked local `skills` dir. A running Node process cannot see the newly installed package's files after `npm i -g ...@latest`; resolving `<root>/skills` from the current process would point at the pre-update installation. | AC-04's core promise is false on the most important path: a user can run bare `harness update`, get a newer CLI installed, and have skills reconciled from the old package's baked tree. This is a functional readiness blocker for "self-reconcile on update." | Change the update design so skills reconciliation after a binary update uses the newly installed package's skill source, or defers/re-invokes after update. Examples: run skills reconcile before binary update only when already latest; after successful binary update, instruct/re-exec the newly installed `harness skills update` with the lock targets; or keep update-time skill refresh on an explicit remote/versioned source while install stays local. Add tests for old-version -> new-version update proving the skills source is from the new install, not the running process. |
| HIGH | The READY gate matrix falsely marks constitution and architecture checks N/A. | The plan says `G2 | Constitution | N/A | no docs/project-rules/constitution.md` and `G3 | Architecture | N/A | no docs/project-rules/architecture.md` (`vendor-builder-baked-skills-plan.md:100-101`). Both files exist in this checkout: `docs/project-rules/constitution.md` and `docs/project-rules/architecture.md`. This matters for this plan because it changes CLI acts/services/adapters, packaging, and docs, all governed by those files. | The artifact claims implementation readiness while skipping active upstream constraints. This already hid at least one material lifecycle/contract risk in update behavior, and it prevents a downstream implementer/reviewer from knowing which architecture/constitution constraints were actually checked. | Re-run G2/G3 against the existing files and replace the N/A rows with PASS/FAIL evidence. At minimum, record checks for P2/P3/P4/P8/P12 and architecture layering for `acts/skills.ts`, `acts/update.ts`, `services/skills/*`, fs ports/adapters, package files, and external write `~/github/tools/skills/SDD/MOVED.md`. If any deviation is intentional, add the plan-level deviation/decision explicitly. |

**Thesis**: partial; the vendoring/offline-install design is mostly buildable, but the update self-reconcile promise is not proven against the current update lifecycle.

**Consumers**: `/plan-6` implementer → implementation-ready tasks → ❌ until the update-time source/version ordering is specified; CLI consumers of `harness update` → last-recorded skill reconcile → ❌ on binary-update runs; packaging/offline install consumers → mostly covered by AC-01/AC-02.

**Proof**: read target plan; checked current `package.json#files`, `DEFAULT_SKILLS_SOURCE`, `harness skills install/update`, `harness update` sequencing, fs port/fake/node adapters, and active `docs/project-rules/{constitution,architecture}.md`; checked relevant history in plans 010 and 019.

**Open decision**: choose the authoritative skill source for `harness update` after the binary has just been updated: old running package, newly installed package, explicit remote/versioned source, or re-exec/deferred reconcile.
