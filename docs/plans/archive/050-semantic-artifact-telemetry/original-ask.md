# Original ask — semantic-artifact-telemetry
**Captured**: 2026-07-04T00:00:00Z  ·  **By**: /the-flow

> set up a new flow, and please thenr un a workshop that details all the telemetry elements and where / how we get them (shoudl get them prtty much at save time,if hte harness is importing telem as it does, it will grab thatinfo then when the file is changed. THis means files will change over time etc, with updated telem attached.
>
> (context from the preceding /thesis ask:) new telemetry update, this is semantics from paricular files. the-flow code review collects "fixes" we should count them. We should count phases form plans. I itnk there is probably a bunch that we can emit in to telemetry from harness flow / the-flow.json. Workshop length, research length. probalby other?
