# Original ask — harness-skills-install
**Captured**: 2026-06-09T13:25:00+10:00  ·  **By**: /the-flow

> this repo -> ~/github/tools/skills/harness has skills for the harness. these skills need to be installable by the harness core. Itw ill use vercel npx to instal them just as the readme in that other repo i just listed tells you can. It can install -g for globl or local, it can install them to claude, codex, cursor, copilot etc... it shoul ask that too. So as part of our installer, we should offr to install the skills to the current env using npx skills github install command as offered. this "install harness skills" should be a first class cli command on the harness, and the installer skill should offer to run it (but not force it, then it shoul say, to install later run: blah). our cli command should mirror and pass through to the vercel one and install the skills. be aware that without -y the vercel skills will show a UI that blocks, we don't want that. But it should tel them what it is about to run befor eit does it and explain where they can find more information ion the vercel skills npx. copy those skills to here please, then use npx to install them alongside our other exisitng skills ot hey are installed ot the repo. Also we need to refactor our skills as part of this plan to be eng-harness-<slug> so they are all properly grouped. before we being the flow playback to me what your understanding is of this task
