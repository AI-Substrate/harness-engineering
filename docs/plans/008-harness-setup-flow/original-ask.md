# Original ask — harness-setup-flow
**Captured**: 2026-06-08T21:55:13Z  ·  **By**: /the-flow

> next up we need to update our engineering-harness-setup skill. right now the skill comes with templates and things which are not needed (stuff like setting up the retro and other similar concepts etc will move out into the harness itself as deterministic code, so is not needed inside this bit. The skill should just focus on getting the harness installed from npx, and guiding the user if there are problems. The skill is a "flow" that will call out to other skills. documentation should represent it as a simple dag in mermaid diagrams. it should first get the harness installed, then run the harnessability-assessment if one has not already been run (.harness/reports/harnessability - im just updating this skill now, and it will save to this new location once updated). Then it will walk the user through setting up an initial extension - something identified from harnessability - like one for building the codebase and one for running tests.
