{
  "schema_version": 1,
  "kind": "flight-plan",
  "slug": "first-class-flow-system",
  "plan_dir": "docs/plans/024-first-class-flow-system",
  "mode": "Full",
  "cursor": "p3",
  "recommended_next": "p3",
  "now": "Phase 2 COMPLETE 7/7 + COMMITTED (full suite 753 green, biome 170 clean; renderer + `harness flow render`/--check E310 + check:flows CI + docs/how guide + §4 public-npm reconcile; self-review added a mermaid reserved-keyword `end` guard + `|[]{}` escaping). Code is entangled in commit bdbcfce (a concurrent agent's commit swept the shared git index — NOT rewritten, work intact); docs in 98ad84d. [build record] tasks PRODUCED + VALIDATED. tasks/phase-2-deterministic-render-ci-parity-docs/tasks.md: 7 T-tasks mapping plan 2.1-2.6 1:1 (T001/T002 RED render-rule + golden tests -> T003 flow-renderer.ts pure leaf service -> T004 wire `flow render` + `--check` -> T005 check:flows CI step + gen:flow-fixtures + build-before-vitest rule -> T006 docs/how/harness-flow.md + README -> T007 reconcile constitution.md + project-rules/architecture.md §4 GitHub-Packages->public-npm). Grounded by an Explore pass against real Phase-1 source (FlowDoc/FlowNode/FlowEvent/FlowComment contract quoted exact; acts/flow.ts idiom; package.json/ci.yml/docs/how confirmed). validate-v2 BROAD (4 agents) = VALIDATED WITH FIXES, 0 HIGH: routing-creep clean (renderer = pure data->string, `decision` = rendered data), all 13 render rules + AC-06/12/13 traceable, Forward-Compat to Phase 3 ✅; 3 MEDIUM (render input-flag precedence, committed-sibling-.md rule, gen:flow-fixtures regen command) + LOWs all folded in. Phase 1 stays COMPLETE 16/16 green.",
  "next": "Phase 3 tasks: `/the-flow 5 tasks --phase \"Phase 3: the-flow migration onto the CLI\" --plan \"docs/plans/024-first-class-flow-system/first-class-flow-system-plan.md\"`. the-flow consumes `harness flow` (capability/version-floor precheck -> 'run harness update'), ships + points at its own flight-plan schema via --schema, deletes the hand-crank cadence + 8 render rules + leaves a legacy note. Riskiest phase (CD-02). Recommend /compact first.",
  "_comment": "Plan pass complete + first build-prerequisite workshop done. workshops/001-harness-flow-cli-surface.md (Contract Ready) settled the 4 CLI questions: D1 single `flow` core act + Commander subcommand group (first nested group; all other acts stay flat); D2 `new`=author a flow TYPE/schema, `create`=instantiate an instance (matches existing `harness new`); D3 fine-grained atomic primitives (cursor/status/add-node/set-node/comment/event), NOT a coarse advance (routing policy stays in the prose Graph per plan Non-Goal); D4 flow-file precedence --path > --plan-dir > discovery (isWithin containment) and schema precedence --schema > .harness/schemas/flows/ > bundled > E304 (flows/ subdir keeps flow-type schemas separate from other schema kinds — user steer). E3xx refined to E300-E307. Hand-crank->CLI mapping table seeds Phase 3. RE-PLANNED to v1.1.0 (Full, CS-4 down from CS-5, READY) folding BOTH workshops + 8 grill decisions: dropped onboarding/adopt (-> separate plan) + flow agent (-> v2) -> 3 phases (was 4); two built-ins = the-flow (skill-shipped schema, via --schema) + harness-loop (CLI-bundled); clean break (E308, no back-compat); capability/version-floor precheck; renderer supplants the prototype (fresh-fixture golden). validate-v2 broad (4 agents) = VALIDATED WITH FIXES: code-facts verified (E300-E308 free, flow registers cleanly); applied ~20 fixes incl. the FC ❌ fix (overlay-declared status vocab + reserved optional authority slot so the staged adopt plan extends the core without reshaping it), E308 positive-signature, --schema isWithin-exempt, flow-Envelope snapshot, render escaping, reverse version-skew + --pin rollback, workshop errata. milestones_total recomputed 2+3+1=6. cursor stays plan; recommended_next=p1 (Phase 1 tasks). the-flow.md generated FROM this file.",
  "agents": [],
  "nodes": [
    { "id": "research", "type": "research", "label": "Research", "status": "done",
      "command": "/the-flow 1a explore", "ran_at": "2026-06-17T21:05Z",
      "note": "6-lens read-only fan-out -> research-dossier.md. Key: existing flight-plan schema is the blueprint; CLI slots a `harness flow` verb family cleanly; shared-core + custom-overlay validated by two built-in flows; event log greenfield; consumers must not break (CD-02); CD-01 cursor-vs-substrate authority.",
      "artifacts": ["research-dossier.md", "original-ask.md"], "next": ["plan"] },

    { "id": "plan", "type": "plan", "label": "Plan (spec + impl)", "status": "done",
      "command": "/the-flow 1b plan", "ran_at": "2026-06-17T21:38Z",
      "user_input": "the flow has been sligtly adjusted, the plan phase is a littel differnt on how it talks to phases. re-read it then do the plan please.",
      "note": "One atomic pass -> first-class-flow-system-plan.md (## Business Specification + ## Implementation Plan). Mode Full, CS-5, Status READY (G1/G2/G3/G5/G6/G7 PASS, G4 N/A). Round 1: Full / Full TDD / real-fixtures / hybrid docs. 4 phases, kept tight per the updated 'fewest phases that hold' principle (dossier's 7 -> 4; P0 sensor folds into 1.1, event log into Phase 1 data model, docs into Phase 4).",
      "comments": [
        { "at": "2026-06-18T01:25:00Z", "source": "agent", "kind": "validation",
          "text": "validate-v2 (NARROW, 3 agents) on the v1.2.0 amendment = VALIDATED WITH FIXES. Coherence+Code-fact: E309 verified FREE (error-codes.ts <=E204); 'additive/no structural change' verified TRUE (3 phases, CS-4, gates unchanged); AC<->task<->coverage-map closure clean; found 1 banner nit (delta list said 1.3 not the new 1.3a) -> fixed. Thesis Alignment: NO ISSUES -- no routing creep (decision = data construct; cursor moves only via explicit agent `flow cursor`), no non-goal creep, render surface = real Knowability value (not proxy optimization), proof level holds at Implementation; evidence Strong. Forward-Compat: all 4 consumers (tasks stage, the-flow migration, adopt plan, hook-contract) PASS; one Position risk (body-log becoming a parsed contract) -> HARDENED: AC-06 + workshop 002 now state the markdown body-log is render-only, never parsed back (comments[] JSON is source of truth). 2 fixes applied, both mechanical. v1.2.0 Validation Record appended to the plan. Status READY for Phase 1 tasks.",
          "refs": ["first-class-flow-system-plan.md"] },
        { "at": "2026-06-18T01:10:00Z", "source": "agent", "kind": "note",
          "text": "Plan AMENDED to v1.2.0 by DIRECT EDIT (user chose 'just amend, no full re-plan' — additive deltas, no structural change, avoids regenerating + re-churning the ~20 v1.1.0 validate-v2 fixes). Folded: workshop 003 (AC-01 template-create/--bare, AC-06 decision-render, NEW AC-15 insert-node edge algebra, tasks 1.2/1.3a/1.6/1.7/1.11/2.2, E309, 2 risks, Domain Manifest + Phase 1/2 Delivers + Phase 3.3) + the render-surface decision (AC-06 genesis-bubble + comments[] badge + markdown body-log; task 2.2). Also amended workshop 002 (new § Render surface) + workshop 001 (errata: create template/--bare + insert-node + decision type). E309 verified free (error-codes.ts <=E204). NOT yet validated — a narrow validate-v2 (coherence + AC-15 + coverage-map) is offered as optional insurance before Phase 1 tasks.",
          "refs": ["first-class-flow-system-plan.md", "workshops/003-templates-insertion-decision-points.md"] },
        { "at": "2026-06-18T00:55:00Z", "source": "user", "kind": "decision",
          "text": "RENDER-SURFACE decision (raised at the re-plan seam looking at the rendered the-flow.md; refined with the user). Node box = label + STATUS colour only (done/known/unstarted -- already render rule 5 + classDefs; preserved). ONE 🗣 bubble per node = the GENESIS directive: the user_input that created the node / set its direction the FIRST time (e.g. 'do this workshop', 'lets do the cli surface workshop') -- this is the existing user_input field + render rule 6, UNCHANGED. The accumulating comments[] (later steers, grill decisions, agent validation notes) are NOT bubbled -- they stay in comments[] and the node shows a `💬N` COUNT badge only; the full timestamped text renders in the .md's MARKDOWN BODY log (per-node, source/kind tagged), which fills AC-06's 'markdown' half (today the .md is only diagram+legend). So: user_input = genesis bubble (one per node, as today); comments[] = counted on the node + body-logged below. Rationale: keeps exactly today's clean one-bubble-per-node look (which the user likes) while making comments[] discoverable (count) + auditable (body) instead of JSON-only. Baked in one way, NO flag (a render flag is additive later if ever needed). GAP this closes: no render rule covered comments[] (00-routing rules 1-8 + Phase 2 task 2.2 rendered only user_input + status). FOLDS INTO: workshop 002 (add a 'Render surface' section -- 002 owns comments[] but wrote no render rules) + Phase 2 render rules (comment-count badge + markdown body log) + AC-06 (specify the markdown-body content, not just 'markdown') + task 2.2 (badge + body-log render tests). No new phase, no new workshop." },
        { "at": "2026-06-17T22:58:56Z", "source": "user", "kind": "decision",
          "text": "NEW REQUIREMENT (workshop-bound per user request): flows START FROM A TEMPLATE (the default shape research->plan->phases->review->merge) but are DYNAMIC -- the agent must be able to robustly + easily INSERT records/nodes mid-flow: insert phases, insert a workshop that's been run, insert decision points. Three things to pin: (1) TEMPLATES -- `flow create <type>` scaffolds the default node skeleton, not a bare flow (flight-plan.template.json precedent); per-type template ships with its schema (the-flow template with the skill, harness-loop bundled). (2) Dynamic INSERTION that splices edges FOR the agent -- e.g. `insert-node --after/--before/--branch-of` rewires next[] deterministically so the agent never hand-recomputes edges (this session inserted ws-cli/ws-events/ws-insert by hand 3x = the exact use case; must be a first-class verb). (3) DECISION POINTS -- a `decision` node type with multiple next[] -> serial sub-flows (original brief). -> Gets its own workshop (ws-insert); folds into Phase 1 (schema + mutations) + workshop 001 (verb additions). No new phase." },
        { "at": "2026-06-17T22:49:08Z", "source": "agent", "kind": "validation",
          "text": "RE-PLAN v1.1.0 + validate-v2 (broad, 4 agents) = VALIDATED WITH FIXES. Folded both workshops + all 8 grill decisions; CS-5->CS-4, 4 phases -> 3 (onboarding/adopt cut to a separate plan; flow agent -> v2). Code-facts verified by the Risk agent: E300-E308 free in error-codes.ts; `flow` not in RESERVED_NAMES (registers cleanly). Applied ~20 fixes; the load-bearing one is the Forward-Compat ❌->✅: the shared core would have frozen a closed status enum + dropped CD-01's authority tag, forcing the staged adopt plan to reshape the core -> fixed by making the status vocabulary overlay-declared + reserving an optional `authority` field, both proven by a Phase-1 fixture overlay (no adopt features ship). Also: E308 keyed on positive legacy signature (not empty events[]); --schema exempt from isWithin (out-of-repo skill schema); 2nd contract snapshot over flow Envelope shapes; mermaid-safe render escaping; reverse version-skew + `harness update --pin` rollback; workshop 001/002 errata reconciling pre-grill residue. One judgment-call surfaced for user veto (the FC extension-room fix). All gates PASS, Status READY." },
        { "at": "2026-06-17T22:31:48Z", "source": "user", "kind": "decision",
          "text": "Grill decision (8): the new system SUPPLANTS the old hand-cranked flow (a 'first manual attempt') -- it is NOT a compatibility target. The renderer + schema are free to DRIFT: add fields (created_at/events[]/comments[]/provenance/etc.) and change the render shape as current requirements need; best-fit, not byte-match. Phase 2 byte-parity golden = FRESH fixtures authored by the NEW CLI (incl. a re-rendered 024), committed + --check-guarded; DROP task 2.3's 'match today's hand-rendered shape' criterion. 024's current hand-written the-flow.md is a visual reference only, not a byte target." },
        { "at": "2026-06-17T22:27:30Z", "source": "user", "kind": "decision",
          "text": "Grill decision (7): the-flow TELLS the CLI where its schema is (passes --schema pointing at its skill-shipped flight-plan copy). The CLI bundles ONLY harness-owned schemas (the harness loop) via gen:flows; flight-plan is NOT a second CLI-bundled copy -> single owner per schema, no skill-vs-CLI drift. Workshop 001's --schema>.harness/schemas/flows/>bundled precedence already supports this. Assumption (correct me): the harness-loop schema is SHIPPED bundled but has NO driver in 024 (proves the split + ready for the later eng-harness-flow work) -- schema-only, like we agreed for adopt." },
        { "at": "2026-06-17T22:26:24Z", "source": "user", "kind": "decision",
          "text": "SCOPE CHANGE (grill, folds into re-plan): DROP the onboarding/adopt-flow built-in from 024 entirely -> adopt is a separate, already-staged plan (user forked the conversation). The TWO dogfood default schemas for 024 become (1) the-flow's flight-plan schema, shipped WITH the the-flow skill (travels with the skill, not necessarily CLI-bundled), and (2) the harness loop / eng-harness-flow schema, shipped BUNDLED in the harness CLI. These two prove the shared-core + per-flow-custom split (replacing onboarding as the second built-in). Effect: Phase 4 onboarding/adopt-flow + CD-01 dual-authority resume + the 4.5 adopt-flow signal are CUT from 024 (move to the adopt plan); Phase 4 shrinks to the docs sweep (likely folds into P2/P3 -> ~3 phases). Distribution model SPLITS: the-flow schema with the skill; harness flow schema(s) bundled via gen:flows. Open: does the CLI also bundle flight-plan, or does the-flow pass --schema to its skill-shipped copy? (next grill q)." },
        { "at": "2026-06-17T21:38:46Z", "source": "agent", "kind": "validation",
          "text": "validate-v2 broad (4 agents) = VALIDATED WITH FIXES. Convergent finding: the two workshops (CLI surface, event taxonomy) were deferred but Phase 1 criteria assume their output. Fixes applied: workshops made explicit build-prerequisites that fold back before Phase 1; contract sensor = two-checkpoint blocking gate; CD-01 per-field authority tag + resume re-verify; gen:flows/check:flows + build wiring; path-escape containment; E3xx block; the-flow cross-repo deploy task; eng-harness-flow signal path specified. Declined: splitting Phase 1 (would breach 'limit phases'; root cause was workshop sequencing, fixed directly)." },
        { "at": "2026-06-17T21:38:46Z", "source": "user", "kind": "note",
          "text": "the-flow source is user-controlled at ~/github/tools/skills/SDD/the-flow/ (deploys to ~/.claude/skills/the-flow) — Phase 3 migration is in-scope (cross-repo + deploy), not a third-party coordination." }
      ],
      "artifacts": ["first-class-flow-system-plan.md"], "next": ["p1"] },

    { "id": "ws-cli", "type": "workshop", "label": "Workshop · harness flow CLI surface", "status": "done",
      "branch_of": "plan", "command": "/the-flow 2c workshop \"harness flow CLI surface\"",
      "ran_at": "2026-06-17T21:48:08Z",
      "user_input": "lets do the cli surface workshop.",
      "note": "DONE (Contract Ready) -> workshops/001-harness-flow-cli-surface.md. Pinned the full `harness flow` surface: single core `flow` act + subcommand group; new=type/create=instance; fine-grained atomic mutations (cursor/status/add-node/set-node/comment/event); --path>--plan-dir>discovery + --schema>.harness/schemas/flows/>bundled precedence; E300-E307. Cross-checked against all 20 prior the-flow.json in this repo -> confirmed comments[]/events[]/created_at/modified_at are 100% greenfield, flagged renderer-tolerates-unknown-types for Phase 2. (`flow agent` was considered from the corpus but DROPPED to v2 per user grill -- pure bookkeeping; agents[] stays in schema, unpopulated by v1.) Folds into Phase 1/2 task criteria on the next re-plan.",
      "comments": [
        { "at": "2026-06-17T21:48:08Z", "source": "agent", "kind": "decision",
          "text": "4 CLI questions resolved: D1 single act+subcommands (vs many top-level acts); D2 new authors a flow type / create instantiates (matches existing `harness new` precedent); D3 fine-grained primitives not a coarse advance (CLI = primitives, prose Graph keeps routing policy per plan Non-Goal); D4 dual precedence chains for flow-file vs schema, isWithin containment (E303). Refined E3xx to E300-E307 (added E305 node-not-found/illegal-transition, E306 schema-version, E307 ambiguous-target). Built a hand-crank->CLI mapping table seeding Phase 3 task 3.3." },
        { "at": "2026-06-17T21:54:29Z", "source": "user", "kind": "decision",
          "text": "User steer: flow-type schemas default to .harness/schemas/flows/<type>.schema.json (a flows/ subdir), keeping them separate from other .harness/schemas/ kinds (records, configs). Workshop D4 + examples updated; folds into plan AC-11 + task 1.2b on the next re-plan." },
        { "at": "2026-06-17T21:58:00Z", "source": "agent", "kind": "note",
          "text": "Corpus review (user-prompted): reviewed all 20 prior the-flow.json in this repo for real field usage. Findings -> (1) agents[] populated in 8/20 flows (companion+render:wrap, entry shape {slug,kind,run_id,covers[],render,result} from 004) -> proposed a `flow agent` verb (later dropped, see next comment). (2) comments[]/events[]/created_at/modified_at/custom-metadata = 0/20 -> genuinely greenfield (workshop 002 designs freely). (3) only ran_at is a real timestamp; created_at/modified_at auto-stamping is pure CLI value-add. (4) ad-hoc node type 'validation' seen 3/20 -> renderer must tolerate unknown types (Phase 2 render rule). (5) note fields are append-style history blobs = same anti-pattern as 021 state keys, reinforcing comments[]. Added a 'Real-corpus grounding' section to workshop 001." },
        { "at": "2026-06-17T22:19:45Z", "source": "user", "kind": "decision",
          "text": "Grill decisions (4): (1) GOAL reframed -> deterministic flow MECHANICS (mutation/history/render), NOT automated routing; the agent must drive it well, accepted. (2) NO external enforcement of good usage -- near-enough is good enough; mechanical integrity (auto-events/timestamps/schema/render-check) enforced, narrative quality is agent judgment, risk accepted. (3) NO back-compat: pre-CLI hand-written flows just ERROR with a good honest message (E308 FLOW_LEGACY_FORMAT, next_action hedges 'likely legacy, could be a bug'); Phase 3 DELETES the-flow's hand-crank cadence + 8 mermaid render rules (CLI owns them), leaving a short legacy note; the 20 old files are NOT migrated. (4) `flow agent` DROPPED to v2. All fold into the next re-plan." },
        { "at": "2026-06-17T22:22:40Z", "source": "user", "kind": "decision",
          "text": "Grill decision (5): the-flow precheck is a CAPABILITY check, not presence-only. Probe `harness flow --help` / a min-version floor; 'CLI present but too old for flows' is the E308 cousin -> honest actionable error 'run `harness update`'. the-flow + CLI ship independently (skills add vs npm) -> runtime capability probe + remediation, no lockstep release. Sharpens AC-09 + Phase 3 precheck (task 3.4) + the E3xx block on the next re-plan." }
      ],
      "artifacts": ["workshops/001-harness-flow-cli-surface.md"],
      "next": ["plan"] },

    { "id": "ws-events", "type": "workshop", "label": "Workshop · event + comment taxonomy", "status": "done",
      "branch_of": "plan", "command": "/the-flow 2c workshop \"event and comment taxonomy\"",
      "ran_at": "2026-06-17T22:04:20Z",
      "user_input": "do next workshop",
      "note": "DONE (Contract Ready) -> workshops/002-event-and-comment-taxonomy.md. The greenfield piece (CD-03, 0/20 prior usage). Pinned: two surfaces (root events[] = flow-scoped machine facts; per-node comments[] = node-scoped narrative); 3 event tiers (built-in engine-fired {created,cursor-moved,status-changed,node-created,node-updated} / public-manual {build-run,test-run,deploy,checkpoint} / custom duck-typed); duck-typing algo (bool/int/float/date/string, leading-zero->string, --type override, type stored explicitly); durations DERIVED at read time (never stored); provenance stamped ONCE at root reusing record 7-key block (branch = created_from_branch); id format <PREFIX>-<NNN> reusing observe nextId; comment shape {at,text,source?,kind?,refs?} (already dogfooded in 024's own flow); tolerant/append-only (observe principle). Built the 021+004 'born badly -> born well' migration map.",
      "comments": [
        { "at": "2026-06-17T22:04:20Z", "source": "agent", "kind": "decision",
          "text": "Grounded in real precedents (not just schema): observe buffer-codec (id <PREFIX>-<NNN>, nextId padStart(3), tolerant parse never drops) + record provenance.ts (7-key block: harness_version/branch/repo/created_at/agent/plan_id) reused directly -> created_from_branch IS the provenance branch key. Evidenced against 021/.the-flow-state.json (13 prose keys, dates trapped in sentences) + 004 note mega-blobs -> every blob mapped to an event or comment. comment shape already in use on 024's own plan/ws-cli nodes (self-validating). Folds into plan task 1.3/1.4/1.7/1.8 + ### Clarifications on the next re-plan." }
      ],
      "artifacts": ["workshops/002-event-and-comment-taxonomy.md"],
      "next": ["plan"] },

    { "id": "ws-insert", "type": "workshop", "label": "Workshop · templates, node insertion + decision points", "status": "done",
      "branch_of": "plan", "command": "/the-flow 2c workshop \"templates, node insertion, and decision points\"",
      "ran_at": "2026-06-18T00:42:00Z",
      "user_input": "run the workshop",
      "note": "DONE (Contract Ready) -> workshops/003-templates-insertion-decision-points.md. The 3rd build-prerequisite (user-requested: 'flows start from a template but are dynamic -- need a robust, agent-easy way to insert records + decision points'). Pinned 3 mechanics, all ADDITIVE (no new phase, no 001/002 decision reversed): (T) `flow create` scaffolds a minimal TEMPLATE skeleton (research->plan->[assumed phase]->review->merge), `--bare` opts out; template resolves as a SIBLING of the schema (`<type>.template.json`), stamp-root-not-substitute (no templating engine); the existing flight-plan.template.json worked-example is NOT the create-seed. (I) `insert-node` = a DISTINCT verb from add-node (it mutates EXISTING nodes' edges; add-node never does); fully-specified edge algebra: --after X moves X's OUT-edges to N (N.next=old X.next, X.next=[N]); --before X moves X's IN-edges to N (each predecessor X->N, N.next=[X]); --branch-of X adds a dotted excursion (N.branch_of=X, N.next=[X]/--rejoin, X.next UNCHANGED); DAG re-checked before write -> E309 FLOW_EDGE_INVALID (new, verified free); audit = node-created + per-edge node-updated{edge_op} (REUSES 002's kinds, no new built-in kind). (D) `decision` node type = >=2 next[] heading serial sub-flows, ZERO new schema fields (chosen-ness via successor status; un-taken stays assumed, or `declined` via overlay-declared status = first consumer of the FC fix); singular cursor keeps sub-flows serial (parallel nodes stay deferred). Grounded in the DOGFOOD: this flow's own ws-cli/ws-events/ws-insert (3x --branch-of plan) + the plan->p1/p2/p3 phase reveal (--after) are exactly the hand-cranked edits insert-node makes first-class. E309 verified free (error-codes.ts <=E204); flight-plan.template.json confirmed present.",
      "comments": [
        { "at": "2026-06-18T00:42:00Z", "source": "agent", "kind": "decision",
          "text": "Workshop 003 settled the 3 mechanics. Folds into the next re-plan: AC-01 (create from template/--bare), AC-06 (renderer handles `decision` type), a NEW AC-15 (insert-node edge algebra + DAG re-check + audit events); tasks 1.5 (+`decision` enum + edge_op convention), 1.6 (template resolve/copy/stamp + --bare/--template + ship the create-seed), 1.7 (insert-node 3-mode algebra), 1.11 (+E309); test tasks 1.2/2.2 (edge-algebra incl. multi-successor/predecessor + cycle-rejection; decision render). Workshop 001 gets an errata note (create gains template/--bare; insert-node added). Phase 3: the-flow ships the create-seed + calls insert-node for phase-reveal/excursions instead of hand-editing next[]. Reconciles with 002 (no new built-in event kind; edge_op rides in details{}). Convergence surfaced: `decision` pruning is the first real consumer of the plan's overlay-declared status vocabulary (Finding 02b) -- the FC extension-room fix wasn't speculative. NO new phase; CS unchanged." }
      ],
      "artifacts": ["workshops/003-templates-insertion-decision-points.md"],
      "next": ["plan"] },

    { "id": "p1", "type": "phase", "label": "Phase 1 · Flow engine (schema, state, mutations, event log)", "status": "done",
      "phase": 1, "command": "/the-flow 6 implement --phase \"Phase 1: Flow engine — schema, state, mutations, event log\" --plan \"docs/plans/024-first-class-flow-system/first-class-flow-system-plan.md\"",
      "ran_at": "2026-06-18T01:35Z",
      "user_input": "continue with phase 5 taks then validate please",
      "note": "COMPLETE 16/16 on branch 024-first-class-flow-system, all committed + green (full suite 639 -> 719). T001-T006 (foundation+schema, prior session) + T007-T016 (this resume): FsPort.rename + flow-service (create/new/show/list + atomic temp+rename + E308) + flow-events (3-tier taxonomy + duck-typer + comments + datetime) + flow-mutations (cursor/status/add/set/comment + insert-node edge algebra + pre-write DAG E309) + acts/flow.ts (first nested subcommand group) + app wiring (flow reserved) + checkpoint-2 flow-Envelope snapshot + E306 version gate. Commits: 38dd47b 126bc7f 11c3ee2 c727994 43a650a 51947ba (+ earlier). Companion (run …-93ae) reviewed all 4 boundaries: 1 MEDIUM + 2 HIGH all addressed. Renderer = Phase 2; the-flow migration = Phase 3.",
      "comments": [
        { "at": "2026-06-18T01:35:00Z", "source": "agent", "kind": "note",
          "text": "RESUME build session (implement --companion, after /compact). User: 'resume and implemetn with companion'. Built T007-T016 TDD-first, commit-per-task: 126bc7f (T007-T009 service+atomic I/O+E308), 11c3ee2 (T010-T014 mutations+events), c727994 (T015 acts/flow.ts+app+ckpt2 snapshot), 43a650a (T016 version-gate E306), 51947ba (companion-finding fixes). code-review-companion run 2026-06-18T01-05-49-449Z-93ae reviewed all 4 commit boundaries -> 3 findings: 1 MEDIUM (anti-reinvention: FlowProvenance/nextEventId duplication -> FlowProvenance now reuses ProvenanceFields; nextEventId kept, shared-helper deferred to P2) + 2 HIGH (modified_at not bumped on new/rewired nodes -> FIXED; runMutation missing post-mutation validateFlowDoc -> FIXED with E300 gate + act tests). Full suite 719 green; npm run build clean (zero gen drift). Companion magicWand (backlog): auto-derive farewell retro from the coordination ledger.",
          "refs": ["tasks/phase-1-flow-engine-schema-state-mutations-event-log/execution.log.md"] },
        { "at": "2026-06-18T01:55:00Z", "source": "user", "kind": "decision",
          "text": "Ran the tasks stage for Phase 1 ('continue with phase 5 tasks then validate please'). Produced tasks/phase-1-flow-engine-schema-state-mutations-event-log/tasks.md: 16 T-tasks mapping plan 1.1-1.12 (incl. 1.2a/b/c/1.3a) 1:1, grounded in CONFIRMED reuse precedents (provenance.ts ProvenanceFields+spliceProvenance:62, buffer-codec <PREFIX>-<NNN>:42, posix-path isWithin:82, temp.ts ensureTemp, record/registry.ts + core-types/*, observe.ts act shape, gen-docs.mjs parity). E300-E309 re-confirmed free (highest=E204); E108 reused for insert-node mutual-exclusivity. Renderer correctly deferred to Phase 2." },
        { "at": "2026-06-18T02:10:00Z", "source": "agent", "kind": "validation",
          "text": "validate-v2 (BROAD, 4 agents) on the Phase 1 dossier = VALIDATED WITH FIXES. Source Truth: NO ISSUES (every symbol/path/line independently re-verified). Cross-Reference+Completeness: 16/16 plan tasks mapped 1:1, AC linkage correct, ordering sound (T001 first, T003<T006, TDD pairs), scope clean; 1 MINOR fixed. Thesis Alignment: Partially->Advanced (proof Hybrid->Implementation after sharpening T001/T003/T006/T009/T005 done-when). Forward-Compat: 2 HIGH + 3 MED -> fixed -- the schema task now ENUMERATES the shared-core node field set (user_input/comments[]/decision/datetime + tolerated agents[]/output/error) Phase 2 renders, and checkpoint 2 (flow-Envelope snapshot) is committed at Phase-1 end (T015) so Phase 3 can't consume a drifted shape; bundled-template sharp edge resolved with a stated default. All CRITICAL/HIGH fixed; Validation Record appended; Ready for GO.",
          "refs": ["tasks/phase-1-flow-engine-schema-state-mutations-event-log/tasks.md"] },
        { "at": "2026-06-18T02:55:00Z", "source": "agent", "kind": "note",
          "text": "BUILD SESSION (implement --companion). User: 'implemetn with code review companion please' (after /compact). Branched 024-first-class-flow-system; pre-flight boot HEALTHY (639 green); booted+briefed code-review-companion (--no-skills per repo note). Built 6/16 TDD-first, commit-per-task, all green (full suite 659+1todo): T001 contract snapshot (skill-owned --hook bytes frozen; 38dd47b), T002 E300-E309 (bf694f6), T003 gen:flows pipeline (c7b3194), T004/T005/T006 schema resolution+validation -- hand-rolled validator over a CLI-owned descriptor, NOT full JSON-Schema/ajv (f7fc71f). Companion reviewed T001-T003: T001/T003 clean, T002 1 MEDIUM (E307/E108 comment overlap) -> FIXED 7533e1a; it stood down before the schema group (T004-T006 companion-unreviewed -> re-boot on resume or review stage). Companion magicWand (backlog): `minih report draft` farewell-skeleton helper. CHECKPOINTED at the foundation+data-model boundary for /compact; resume = re-run implement from T007.",
          "refs": ["tasks/phase-1-flow-engine-schema-state-mutations-event-log/execution.log.md"] }
      ],
      "artifacts": ["tasks/phase-1-flow-engine-schema-state-mutations-event-log/tasks.md", "tasks/phase-1-flow-engine-schema-state-mutations-event-log/execution.log.md"],
      "next": ["p2"] },

    { "id": "p2", "type": "phase", "label": "Phase 2 · Render + CI parity + docs", "status": "done", "ran_at": "2026-06-18T12:40Z",
      "phase": 2, "command": "/the-flow 6 implement --phase \"Phase 2: Deterministic render + CI parity + docs\" --plan \"docs/plans/024-first-class-flow-system/first-class-flow-system-plan.md\"",
      "user_input": "next. also what is git status",
      "note": "`harness flow render [--check]` (all render rules + unknown-type tolerance + mermaid-safe escaping) + distinct check:flows CI step + docs/how guide + README + stale npm-vs-GitHub-Packages reconcile. Golden = fresh CLI-authored fixtures (renderer supplants the prototype). Tasks dossier produced + validated (awaiting implement GO).",
      "comments": [
        { "at": "2026-06-18T02:00:00Z", "source": "agent", "kind": "note",
          "text": "Ran the tasks stage for Phase 2 (user: 'next … yes run it then validate'). Produced tasks/phase-2-deterministic-render-ci-parity-docs/tasks.md: 7 T-tasks mapping plan 2.1-2.6 1:1, TDD-first (T001/T002 RED -> T003 renderer GREEN -> T004 act-wiring -> T005 CI parity -> T006/T007 docs). Grounded by an Explore pass on real Phase-1 source: the FlowDoc/FlowNode/FlowEvent/FlowComment input contract is quoted field-exact, the acts/flow.ts subcommand idiom (resolveFlowPath/readFlowDoc/emit/summary) is the slot for `render`, and package.json (gen:docs/gen:flows/check:docs at 35-38) + ci.yml (check:docs step at :81) + the 7 docs/how/*.md house style are confirmed. Caught + corrected the §4 direction: README is CURRENT (public-npm, zero-auth per plan 019); constitution.md + project-rules/architecture.md §4 are the STALE docs (GitHub-Packages+token) T007 fixes.",
          "refs": ["tasks/phase-2-deterministic-render-ci-parity-docs/tasks.md"] },
        { "at": "2026-06-18T02:05:00Z", "source": "agent", "kind": "validation",
          "text": "validate-v2 (BROAD, 4 agents) on the Phase 2 dossier = VALIDATED WITH FIXES, 0 HIGH / 0 FAIL. Source Truth: PASS w/fixes -- type contract + act idiom verified EXACT against flow-events.ts/acts/flow.ts; §4 direction confirmed correct; architecture.md real path = docs/project-rules/architecture.md (not docs/architecture.md). Cross-Reference+Completeness: PASS w/fixes -- 2.1-2.6 -> T001-T007 mapped 1:1 (no dropped/invented scope), all 13 render rules + AC-06/12/13 traceable, TDD+dep chain sound; 3 MEDIUM (render input-flag naming, committed-sibling-.md location, un-pinned regen command) -> ALL FIXED. Thesis Alignment: PASS (Implementation, Strong) -- NO routing creep (renderer = pure leaf; decision = rendered data), no non-goal creep, injection-safety backed by a real adversarial test task. Forward-Compat: PASS -- Phase 3 the-flow migration satisfied across T002/T003/T004/T006; body-log render-only locked; cross-repo .md mirror correctly deferred to Phase 3 (manual step). Fixes folded: --path/--slug via resolveFlowPath (--input alias, precedence stated), sibling-.md rule (.json->.md same dir, --against override), gen:flow-fixtures regen script pinned, confirmed architecture.md path, line-citation + soft-phrase + shared-test-file nits. Validation Record appended to the dossier; READY for implement GO.",
          "refs": ["tasks/phase-2-deterministic-render-ci-parity-docs/tasks.md"] }
      ],
      "next": ["p3"] },

    { "id": "p3", "type": "phase", "label": "Phase 3 · the-flow migration", "status": "known",
      "phase": 3, "note": "the-flow consumes `harness flow`; ships + points at its own flight-plan schema (--schema); capability/version-floor precheck (error-and-stop + 'run harness update'); deletes hand-crank cadence + 8 render rules + legacy note; deploy CLI-first; rollback = revert skill + harness update --pin. Riskiest phase (CD-02).",
      "next": ["review"] },

    { "id": "review", "type": "review", "label": "Review", "status": "known", "next": ["merge"] },

    { "id": "merge", "type": "merge", "label": "Merge", "status": "known",
      "command": "/the-flow 8 merge", "next": [] }
  ]
}
