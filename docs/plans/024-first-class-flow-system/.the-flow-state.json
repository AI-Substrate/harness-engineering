{
  "schema_version": 1,
  "slug": "first-class-flow-system",
  "plan_dir": "docs/plans/024-first-class-flow-system",
  "mode": "Full",
  "current_stage": "awaiting-6",
  "pending_command": "/the-flow 5 tasks --phase \"Phase 3: the-flow migration onto the CLI\" --plan \"docs/plans/024-first-class-flow-system/first-class-flow-system-plan.md\"",
  "intent": "Make the 'flow' (a cursor-spine DAG of timestamped nodes the agent visits, tracked across compaction) a first-class DETERMINISTIC concept inside the harness CLI (a `harness flow` verb family) — deterministic MECHANICS (mutation/history/render), not automated routing. Shared-core schema + per-flow custom overlays; deterministic render; embedded event log + per-node timestamped comments (datetime queryable). Two built-in schemas: the-flow (skill-shipped) + harness-loop (CLI-bundled). the-flow consumes the CLI (capability/version-floor required, no adoption needed); clean break (old flows error, no back-compat). Onboarding/adopt + flow agent deferred. Full, CS-4, 3 phases. Re-planned v1.1.0 + amended v1.2.0 + validated. PHASE 1 COMPLETE 16/16 on branch 024-first-class-flow-system — all committed + green (full suite 639->719); the harness flow engine (schema+overlay, atomic state I/O, mutations+insert-node, event log+comments+datetime, E300-E309, gen:flows, 2 contract snapshots, E308/E306) stands up. code-review-companion reviewed all 4 commit boundaries (3 findings: 1 MEDIUM + 2 HIGH, all addressed). Renderer = Phase 2; the-flow migration = Phase 3. Phase 2 tasks DONE + validate-v2 broad VALIDATED WITH FIXES (0 HIGH) — tasks/phase-2-deterministic-render-ci-parity-docs/tasks.md (7 T-tasks, plan 2.1-2.6 1:1, TDD-first). PHASE 2 COMPLETE 7/7 (flow-renderer pure leaf + `harness flow render`/--check E310 + check:flows CI step + gen:flow-fixtures + docs/how/harness-flow.md + §4 public-npm reconcile); full suite 753 green, biome 170 clean. Self-review remediation: mermaid reserved-keyword (`end`) id guard + `|[]{}` entity-escaping. COMMITTED — code entangled in bdbcfce (a concurrent agent's commit swept the shared index; NOT rewritten, work intact) + docs in 98ad84d. NEXT = Phase 3 tasks (the-flow migration onto the CLI), the riskiest phase (CD-02).",
  "milestones_total": 6,
  "milestones_done": 4,
  "last_checkpoint_at": "2026-06-18T12:45:00Z",
  "compacted_seams": [],
  "status": "active"
}
