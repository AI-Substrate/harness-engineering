<!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. -->
# Backpressure coverage — deterministic documents (dd)

**Schema**: builder/backpressure · **Source**: backpressure.dd.json · **Sections**: 3

## meta

| Field | Value |
| --- | --- |
| title | Backpressure coverage — deterministic documents (dd) |
| plan | [meta](plan.dd.md#meta) |
| basis_sha | — |
| certainty | Partial |

## rows

| id | criterion | phase | mode | tier | proof | state | note | receipt | probe |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bp-0201 | Schema resolution honours precedence across all four roots and reports every shadowed duplicate with its path. | ph-0002 | EXTEND | computational | Fake-fs multi-root fixture corpus with one package planted in each root, asserting first-hit-wins order and the full shadow chain. | [x] checked | — | — | npx vitest run test/services/dd/schema |
| bp-0801 | The baked docs corpus cannot silently drift from its markdown sources. | ph-0002 | BUILD | computational | A regenerate-and-diff gate: the generator runs in-process and the committed module is compared byte-for-byte, so an edited source with a stale module fails. | [x] checked | — | — | npm run check:dd-docs |
| bp-0901 | A plan authored as a deterministic document validates, renders byte-stably, and answers questions through stock jq rather than through reading. | ph-0005 | EXTEND | computational | This corpus is the subject: validate it at depth 3, prove the rendered siblings do not drift, sweep it with the doctor, and answer a real question with jq. | [ ] unchecked | — | — | harness dd validate docs/plans/065-deterministic-documents/exemplar/plan.dd.json --depth 3 |
| bp-1201 | dd-core stays a pure library: no envelope vocabulary, no act imports, no Node adapters. | ph-0001 | EXTEND | computational | Dependency-cruiser rule sets plus an architecture test that reads the import graph — a boundary a reviewer cannot forget to check. | [x] checked | — | — | harness arch-check --json |
| bp-0902 | The exemplar uses dd primitives cleverly and renders well for a human reader. | ph-0005 | ABSENT | human-judgement | No machine proves taste. This row exists so the judgement is visible as an open item rather than assumed — a human reads the rendered siblings and says yes or no. | [ ] unchecked | Deliberately human-judgement tier: the standing constraint is a person's call, and pretending otherwise would be the dishonest kind of green. | — | — |

## sensors

| id | name | command | dimension | found_in |
| --- | --- | --- | --- | --- |
| sensor-suite | full unit and architecture suite | just test | behaviour + architecture | root recipe -&gt; harness/cli |
| sensor-checks | composed quality gate | harness checks | all | .harness/extensions/checks/ |
| sensor-dd-doctor | dd corpus sweep at infinite radius | harness dd doctor --json | document health | harness/cli/src/acts/dd/doctor.ts |
