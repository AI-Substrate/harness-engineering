<!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. -->
# dd-native builder — the structural proof graph, wired end to end

**Schema**: builder/plan · **Source**: plan.dd.json · **Sections**: 20

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | dd-native builder — the structural proof graph, wired end to end |
| slug | dd-native-builder |
| ordinal | 70 |
| status | draft |
| complexity | CS-4 |
| summary | Make the builder pipeline dd-native, 100%: plans and per-phase task files are authored as .dd.json (humans read the generated sibling — there is no -plan.md), the flight-plan template and expander author the dd_link gates, and navigation is mechanically refused when the documents say the work is not done.<br><br>Around that spine: typed link relations (a frozen five with machine semantics), the per-item links bucket, mandatory pressure on every assertion, the satisfies edge from tasks to the ACs they serve, a semantic validation layer (harness plan validate) with contradiction warnings, scoped proof-tree reads, and a strict-zero --complete mode, plus a PR surface that renders closed ACs with their resolved evidence for the human checkpoint.<br><br>The thesis: the graph does not make claims true — it makes false claims expensive and visible. Agents run the journey autonomously; humans review claims-with-evidence at the PR. |
| backpressure | — |
| log | — |
| mode | Full |
| plan_version | 2.0.0 |
| created | 2026-08-04 |
| confidence | 0.8 |
| complexity_breakdown | S=2, I=2, D=1, N=1, F=1, T=2 (sum 9) |

<a id="research-context"></a>

## Research Context

Direct continuation of plan 065 (shipped, PR #87): the dd document layer, builder/* schemas, exemplar corpus, and flow-spine departure gate all exist and are tested — but nothing drives them (zero dd_links in any flight plan; builder has no dd knowledge).

Design authority: the 2026-08-04 Jordan/koala session, recorded as a RULED/LEAN/OPEN ledger in docs/plans/065-deterministic-documents/builder-tuning/notes.md and the design doc structural-proof-graph.md beside it. An independent Opus validation pass (NEEDS ATTENTION, 10 findings F1–F10) was folded in — all ten addressed, the load-bearing ones promoted to Key Findings.

Plan #90 (merged) moved builder in-repo (skills/builder/), introduced the assets/ layout, the 7b post-flight stage, and plan archiving — constraints this plan lands inside.

This plan is itself the first dd-native plan: this document IS the dogfood, and authoring it forced the schema extension it rides on (full builder spec as schema sections) plus one recorded difficulty (the CLI-frozen id-prefix registry).

<a id="goals"></a>

## Goals

- Typed link relations: semantics on the rel, not the field name; five built-ins frozen (pressure, proven_by, satisfies, derives, ref); unknown rels legal, treated as ref.
- Every list item can carry a links bucket (list-typed rel ref), auto-rendered as a final table column when present, traversed by dd graph map.
- Every done_when assertion names its instrument: pressure is a BP-row link or the literal not-applicable; absence is a validation ERROR. BP rows are a toolbelt — no state, no coverage reading, they gate nothing.
- evidence sections become done_when (the dw- prefix already says so).
- Task rows carry satisfies (always an array) back to the ACs they serve.
- harness plan validate grows the semantic layer: open-completables summary (info), rel-generic contradiction warnings (always), --address scoping (the proof-subgraph read), and --complete mode (per-row opens + orphan ACs + strict-zero green).
- The flow gains a check-kind gate: the last review node refuses departure until plan validate --complete is green; phase nodes gate on their task sections; the builder template/expander authors all of it.
- 1b plan and 5 tasks author .dd.json natively — the plan document is plan.dd.json with its generated sibling, no -plan.md exists; ship renders the dd-derived AC/evidence table into the PR body.
- The dd command surface is taught positionally: baked into node instructions[] and each stage module at its seam.
- Self-hosting: by ship, plan 070's OWN corpus is fully retrofitted — real gates hit during the journey, real validations, backpressure and log linked — indistinguishable from a plan authored entirely by the finished feature.
- The flow evaluates its own delivery: the flow-eval harness extension (and its prompting) gains a single-phase dd-native scenario — a blind subject runs the new builder flow end-to-end and telemetry-scored assertions prove it used the tooling correctly.

<a id="non-goals"></a>

## Non-goals

- No probe execution anywhere in dd/plan surfaces (ruled, twice): harness checks owns instrument execution; validate reads the record.
- No AC-state derivation — closure stays a performed act (agents close; the guard rails make it trustworthy).
- No code-drift freshness beyond the existing basis ledger (ruled: solve when it bites).
- No migration of existing markdown plans (058–069 stay as they are; the archive is institutional memory, not a conversion backlog).
- No per-assertion satisfies (ruled: task row, coarse).
- No allowlist/policy surface for --complete warnings (ruled: strict zero; --force is the bypass).
- No changes to the five lifecycle hooks, the harness-seam doctrine, or the E44x gate codes' meanings.
- No multi-phase flow-eval scenario in this plan — single-phase first; multi-phase considered later.

<a id="testing-strategy"></a>

## Testing Strategy

| Field | Value |
| --- | --- |
| approach | Hybrid — TDD for the new dd-core logic (rel parsing, contradiction engine, --complete/--address predicates, check-gate evaluator); lightweight validation for renderer columns, skill prompt-ware, and PR body assembly. |
| rationale | The correctness heart feeds gates other systems trust; wiring follows exercised patterns. |
| focus_areas | Control discipline (house rule: a check only ever run against good input has been demonstrated, not tested): every new warning/error class gets a planted-bad fixture proving it FIRES, plus a good twin; the pre-JIT target-invalid refusal pinned as intended behaviour; legacy markdown read path pinned over a legacy-folder fixture. Synthetic-corpus discipline: a shared fixture factory builds mock plans/phases/task files, and lifecycle-mutation suites drive them through the journey's state transitions (tick, untick, block, human-skip, JIT-birth of task files, basis drift) asserting validator and gate behaviour at every step — unit-level proof across the mutation space, not just point fixtures. |
| excluded | Pixel-level markdown cosmetics beyond structural goldens; probe execution (out of scope by ruling). |
| mock_usage | Fake ports only (injected fake-fs/clock; no vi.mock); real fixture corpora extending the exemplar. Prompt-ware behaviour proven by the scripted end-to-end dry-run (create → JIT → refuse → green → depart) at the P3+P4 join, with the CLI as the actor. |

<a id="documentation-strategy"></a>

## Documentation Strategy

| Field | Value |
| --- | --- |
| location | docs/how/dd/ chapters extended in place (relations, done_when, satisfies, links bucket, --complete/--address); structural-proof-graph.md promotes from builder-tuning/ into docs/how/dd/ once schemas land; baked dd docs regenerated via gen:dd-docs wherever the schema surface changes. |
| rationale | Agents discover dd through the CLI (dd docs, orient instructions[], refusal next_actions); humans get the deep reference in docs/how/dd/. ac-7016 makes the builder flow itself teach the commands positionally. |

<a id="acceptance-criteria"></a>

## Acceptance criteria

| id | claim | state | note | receipt | pressure | proven_by |
| --- | --- | --- | --- | --- | --- | --- |
| ac-7001 | Relations: a schema may declare rel on any link field (single or array items); the five built-in rels parse, resolve, and are exposed on the resolved schema; an unknown rel string is accepted and behaves as ref; dd graph map labels edges with their rel and can filter by it; the built-in set is frozen in the surface manifest (docs/plans/065-deterministic-documents/tasks/phase-1-dd-core-foundations/dd-surface.md, one-line renegotiation to extend); AND every existing builder/* link field declares its rel (pressure→pressure, proven_by→proven_by, done→derives, satisfies→satisfies), pinned by a schema test, so the engine is live on real plans from day one. | [ ] unchecked | — | — | — | — |
| ac-7002 | Links bucket: any list-item shape may carry a links bucket (array of rel-ref links) without per-schema bespoke fields; rendered as a final Links column only when at least one item in that list has entries; edges appear in dd links / dd graph map; absent bucket renders byte-identical to today. | [ ] unchecked | — | — | — | — |
| ac-7003 | Pressure mandatory: in the builder/done_when schema, an assertion without pressure is a validation ERROR (planted-bad fixture proves it fires); pressure not-applicable validates, renders honestly, and is queryable (a stock jq one-liner lists every not-applicable assertion — documented). | [ ] unchecked | — | — | — | — |
| ac-7004 | done_when rename: builder schemas and the exemplar use done_when (unchanged dw- ids); renderer titles read naturally; no evidence-named section remains in living builder schemas; frozen historical corpora are signposted, not rewritten. | [ ] unchecked | — | — | — | — |
| ac-7005 | satisfies: task rows carry satisfies as an always-array of rel-typed links to AC rows; dd graph map on an AC shows the incoming work-accounting; a non-array satisfies fails validation. | [ ] unchecked | — | — | — | — |
| ac-7006 | Contradiction engine: harness plan validate warns whenever a gate-terminal item links (via proven_by, satisfies, or derives) to a checkable target that is not gate-terminal — written once against rels, never field names; planted-bad fixtures prove each rel path fires; consistent-open and evidence-ready-unclaimed stay silent. | [ ] unchecked | — | — | — | — |
| ac-7007 | --complete mode: adds per-row warnings for every open completable and every orphan AC (no incoming satisfies); green means zero errors and zero warnings; without --complete, opens appear only as a summary info line; envelope semantics follow house degrade rules. | [ ] unchecked | — | — | — | — |
| ac-7008 | Scoped validate: --address &lt;address&gt; scopes the semantic checks to the address's reachable closure (outbound + incoming satisfies for AC rows), reusing the P7 graph-map traversal; scoped runs are always per-row (strict implied); green = in-scope terminal + no contradictions + no stale bases in scope; the phase gate itself stays on the cheap completion read. | [ ] unchecked | — | — | — | — |
| ac-7009 | Check-kind gate: a flow node may carry dd_link {check: plan-validate, address}; departure evaluates the semantic validator live, refuses on non-green quoting the findings verbatim, records --force as a defended override exactly as the completion gate does; the completion-kind gate is unchanged; both kinds share the untrusted-reading discipline. | [ ] unchecked | — | — | — | — |
| ac-7010 | Template authors the gates: the template + expander emit dd_links — each phase node gating on assets/tasks/phase-N/tasks.dd.json#tasks (bare ordinal — a stated dd-native amendment to #90's phase-N-&lt;kebab-title&gt; convention: a static template can bake an ordinal address before titles exist, and retitling a phase must never move its task-file address), the last review node carrying the check gate; a pre-JIT departure refuses target-invalid, pinned by test. | [ ] unchecked | — | — | — | — |
| ac-7011 | dd-native authoring: 1b plan emits plan.dd.json (+ built sibling plan.dd.md) as THE plan document — no -plan.md exists in a dd-native plan folder — and 5 tasks emits assets/tasks/phase-N/tasks.dd.json with done_when lists and satisfies edges, adding the phase row's tasks link in the same stroke; the flow discovers both; the scripted end-to-end dry-run (create → JIT → refuse → green → depart) runs as the wiring phase’s exit (ph-7002). | [ ] unchecked | — | — | — | — |
| ac-7012 | PR surface: ship renders the dd-derived AC table into the PR body — state marks + resolved proven_by / pressure / incoming-satisfies as clickable references — generated from the corpus under docs/plans/archive/&lt;ord&gt;-&lt;slug&gt;/ (7b has already archived; the #90 read-from-archive rule); refuses to fabricate on an unclosed corpus. | [ ] unchecked | — | — | — | — |
| ac-7013 | Layout & archive: the dd-native plan root holds plan.dd.json, plan.dd.md, the-flow.json, the-flow.md, plus original-ask.md where one exists (the stated amendment to #90's allow-list: the dd pair REPLACES &lt;slug&gt;-plan.md); task files under assets/tasks/phase-N/; after the post-flight git mv the corpus is doctor-clean AND the archive step rewrites the flow's repo-root-anchored dd_link addresses (fromPath null — they would otherwise silently go stale). | [ ] unchecked | — | — | — | — |
| ac-7014 | Exemplar & docs: the exemplar upgrades to the new schemas — 18 of 26 dw- rows currently lack pressure and each gets a real link or not-applicable; the exemplar keeps its teaching layout at docs/how/dd/exemplar/ (a corpus, not a plan folder — NOT re-homed); docs/how/dd/ + baked docs cover the new surface; just checks green; warn trio at baseline. | [ ] unchecked | — | — | — | — |
| ac-7015 | Legacy read path retained: dd-native is the only write path; the legacy markdown read/adopt/resume path stays unchanged — every in-flight markdown plan still detects, adopts, and resumes; pinned by a detection test over a legacy-folder fixture. | [ ] unchecked | — | — | — | — |
| ac-7016 | Tooling taught where it's needed: the dd/plan command surface is documented at BOTH builder layers — (a) node instructions[] baked by template/expander so harness flow orient (re-read every turn) surfaces the right command positionally (phase nodes: scoped rehearsal + state updates; last review: --complete, closure, verify-basis; gates print their own --force etiquette on refusal); (b) stage modules teach their seam's commands (5 tasks: authoring; 6/6a: state flips + rehearsal; 7: closure + --complete; 8: PR render). A dry-run reader following only orient output + the current stage module never lacks a command this plan introduced. | [ ] unchecked | — | — | — | — |
| ac-7017 | Self-hosting: by the final review, plan 070's own corpus is fully dogfooded — backpressure.dd.json and execution-log.dd.json exist with AC rows carrying pressure/proven_by links; per-phase task files exist with done_when lists (mandatory pressure honoured); tasks carry rel-typed satisfies; the flight plan carries real dd_link gates exercised by the actual journey with the evidence RECORDED where each side lives: at least one genuine refusal captured in telemetry (command_exit carrying the gate E-code — refusals write nothing to the flow by pinned invariant) and any force as the existing dd-gate-override flow event; plan validate --complete runs green at the last review node's gate; ship's PR renders this plan's own AC table. The corpus is indistinguishable from one authored entirely by the finished feature — the plan is its own acceptance fixture. | [ ] unchecked | — | — | — | — |
| ac-7018 | Flow-eval conformance: the flow-eval extension carries a single-phase dd-native builder scenario — a BLIND subject peer (pij) runs the new flow end-to-end on a scaffolded repo, and telemetry-scored assertions prove correct tool use: plan authored as plan.dd.json with no -plan.md, task file JIT-born with done_when + mandatory pressure + satisfies, at least one gate refusal evidenced in telemetry (command_exit gate E-code — tk-7069) followed by a pass, plan validate --complete green before ship-ward departure, states mutated through dd surfaces (never hand-edited generated md). Scenario prompting/instructions updated to match; scored via harness flow-eval score and ledgered. Multi-phase deferred. | [ ] unchecked | — | — | — | — |
| ac-7019 | dd read/write verb family: harness dd get returns the value at any address; dd set / dd add / dd rm mutate documents with schema validation BEFORE write, automatic sibling rebuild, and CLI-minted collision-free ids (add --mint &lt;prefix&gt;); the frozen dd surface manifest gains the four rows via the one-line renegotiation; agents never hand-edit .dd.json again — the CLI is the writer, matching the flow doctrine. | [ ] unchecked | — | — | — | — |

<a id="risks-assumptions"></a>

## Risks & Assumptions

Standing assumption: R-elegance — when a mechanism can be a convention, it is.

The dd/flow injection seams shipped in 065 accommodate the second gate kind without surface renegotiation beyond the E45x block. The #90 layout amendments (dd pair replaces -plan.md at root; bare-ordinal task dirs) are accepted as STATED amendments in the builder docs, never silent divergence.

Risk detail: see the risks section.

<a id="open-questions"></a>

## Open Questions

| question | state | note |
| --- | --- | --- |
| ac-7007 non---complete posture (open completables as one summary info line, per-row warnings only under --complete) is a carried LEAN — koala's warning-fatigue proposal, narratively walked with Jordan but not explicitly confirmed. One-line confirm or veto flips the wording; nothing else depends on it. | [x] checked | RULED (Jordan, 2026-08-04): "yeah we don't warn" — summary info line only mid-flight; --address (ac-7008) is the on-demand scoped read; per-row accounting reserved for --complete. |

<a id="workshop-opportunities"></a>

## Workshop Opportunities

| topic | type | why | key_questions | status |
| --- | --- | --- | --- | --- |
| none open | Other | The 2026-08-04 design session (one question at a time, narrative scenarios) played the workshop role; its rulings are the ledger in 065/builder-tuning/notes.md. | — | ledger complete |

<a id="clarifications"></a>

## Clarifications

| session | question | answer |
| --- | --- | --- |
| 2026-08-04 | Who closes ACs? | Agents — fully autonomous journeys; humans review ACs + linked evidence at the PR stage. |
| 2026-08-04 | BP coverage predicate — any incoming link, or only settled assertions? | Neither — BP is a toolbelt; the obligation flips to assertions (mandatory pressure), and the toolbelt gates nothing. |
| 2026-08-04 | Escape for assertions no instrument checks? | pressure: not-applicable — explicit and queryable; silence fails. |
| 2026-08-04 | Rel vocabulary governance? | Frozen core of five + open namespace (unknown rels behave as ref). |
| 2026-08-04 | satisfies granularity and cardinality? | Task row (coarse), always an array. |
| 2026-08-04 | Orphan ACs (no task claims them)? | Warn under --complete only. |
| 2026-08-04 | --complete green semantics? | Strict zero warnings; bypass via defended --force (human-authorized, recorded). |
| 2026-08-04 | Builder authoring format? | Fully dd-native, no dual write path; in future there will not be a -plan.md, just dd. Rebase first (#90 landed in main). |
| 2026-08-04 | PR surface scope? | Now — in this plan. |
| 2026-08-04 | Links bucket on list items? | Include in this plan. |
| 2026-08-04 | Probe linking (ACs run probes, go green)? | Keep away from it — harness checks fills that role for now; later iteration maybe, not now. |

<a id="planning-seam"></a>

## Planning Seam

| artifact | present | effect |
| --- | --- | --- |
| 065/builder-tuning/notes.md (ruling ledger) | y | Authoritative decisions; RULED items binding; never re-litigated here. |
| 065/builder-tuning/structural-proof-graph.md | y | The design doc; promotes to docs/how/dd in ph-7003. |
| 070/original-ask.md | y | Jordan's directives distilled. |
| Opus validation pass (10 findings) | y | Folded; the load-bearing findings promoted to key_findings. |
| Resolved: ac-7007 posture ruled | — | Confirmed — no mid-flight warns; summary line + --address scoped read; see open_questions receipt. |
| assets/research-flow-eval-history.md (subagent sweep) | y | The flow-eval/telemetry capability arc (034→069), current assertion vocabulary, the four-probe evidence inventory, and the reuse-vs-build delta for the eval scenario; sourced the refusal-evidence and SessionEvidence key findings. |

<a id="gate-matrix"></a>

## Gate Matrix

| gate | check | status | notes |
| --- | --- | --- | --- |
| G1 | Clarify | PASS | No unresolved NEEDS CLARIFICATION; one carried LEAN recorded in open_questions, non-blocking. |
| G2 | Constitution | PASS | docs/project-rules/constitution.md honoured; no conflicts introduced. |
| G3 | Architecture | PASS | dd-core isolation + flow barrel discipline preserved; new seams extend, never bypass. |
| G4 | ADR Compliance | N/A | No accepted ADRs bind this surface. |
| G5 | Structure | PASS | All required builder-spec sections present as dd sections (domain mode OFF per #90 — Target Domains/Manifest omitted). The schema extension carrying them ships with this plan. |
| G6 | Testing Alignment | PASS | Every phase carries planted-bad/control tasks per the testing strategy. |
| G7 | Domain Completeness | N/A | Domains off. |

<a id="implementation-summary"></a>

## Implementation Summary

The dd proof graph shipped in 065 but nothing drives it: no flight plan carries a dd_link, builder authors markdown, and the semantic questions (does this plan's story hang together?) have no surface.

This plan wires the loop end to end — schema features first (rels, buckets, mandatory pressure, satisfies), then the semantic validator, then the flow wiring (gates + dd-native authoring, exiting on the end-to-end dry-run), then one close-out phase: PR surface, corpus/docs upgrade, self-hosting audit, and the blind flow-eval.

The outcome: an autonomous journey whose phase departures are mechanically refused until the documents prove the work, and whose PR hands the human reviewer claims with their evidence one click away.

<a id="key-findings"></a>

## Key Findings

| impact | finding | action |
| --- | --- | --- |
| Critical | A static flight-plan template cannot bake gate addresses containing phase-N-&lt;kebab-title&gt; slugs it can't know (validation F1). | Bare-ordinal task dirs for dd-native plans (assets/tasks/phase-N/) — stated amendment to #90; ac-7010. |
| Critical | Shipped builder/* schemas declare NO rels; without declaring them the contradiction engine ships inert on every real plan (validation F2). | ac-7001 declare-clause; tk-7015; schema test pin. |
| High | E430–E439 AND E440–E449 are both complete allocations; dd-surface.test.ts pins exactly 50 E4xx codes against the manifest (validation F6). | E450–E459 via surface-manifest renegotiation, inside ph-7001 scope (tk-7025). |
| High | Flow dd_link addresses anchor at repo root (fromPath: null) — the post-flight archive git mv silently stales them; dd doctor cannot see it (scans only *.dd.json) (validation F8). | Archive step rewrites dd_link addresses; ac-7013; pinned in tk-7065. |
| High | 7b post-flight archives the plan folder BEFORE ship — ship must read the corpus from the archive path (validation F7). | ac-7012 reads docs/plans/archive/&lt;ord&gt;-&lt;slug&gt;/. |
| Medium | Builder detection keys hard on &lt;slug&gt;-plan.md (63 references); deleting the markdown read path strands every in-flight plan (validation F4). | ac-7015: dd-native is write-path-only; legacy reader retained + pinned (tk-7046). |
| Medium | The OD-8 parser allow-list silently drops unknown schema keys — rel will vanish without a parse-layer pin. | tk-7011; pin in declarations.test.ts/parse.test.ts, never the renderer suite. |
| Medium | 18 of 26 exemplar dw- rows lack pressure — the mandatory rule breaks the corpus as-is. | tk-7061; each row gets a real link or not-applicable; ledger walked leaf-first. |
| Medium | Dogfood (this document): the dd id-prefix registry is a CLI constant (ID_PREFIXES in core/constants.ts) — a schema cannot mint new item kinds, so full-spec sections (key findings, gate rows, open questions) were designed without id fields; and the builder/plan schema had to be extended with 15 new sections to carry the full builder spec. | ph-7001 revisits (same phase): either register prefixes in schema declarations or bless id-less rows as the pattern for non-addressable sections; the schema extension ships as part of this plan. |
| Critical | Gate refusals are currently unprovable from evidence — a triple loss: the refusal writes nothing to the flow (pinned P6 invariant, flow-mutations.ts:126-127), outcome-events never reads envelope.error.code, and flow failures collapse to verb 'flow'. Only a FORCE is durable (dd-gate-override flow event). ac-7017/ac-7018 depend on refusal evidence existing. | tk-7069: capture envelope.error.code in command_exit (fixed-vocab, privacy-precedented by checks.gates/mark.verdict) — refusal lands in telemetry, the nothing-written flow invariant survives untouched. |
| High | SessionEvidence deliberately excludes command_exit, file, artifact, and mark events (session-evidence.ts:311-333), and the flow-eval resolvers mirror must stay lock-step (a CLI test asserts it). Any new assertion keying on those needs the single seam widened on both sides. | tk-7069 widens SessionEvidence + resolvers lock-step; everything else in the dd scenario reuses existing assertion types (harness-verb-ran takes full two-token verbs like 'dd validate'; file-created/forbidden-state/file-content-matches/command-succeeds cover the fs lane — planted-friction A6-A9 is the exact precedent). |
| High | Dogfood (journey): dd documents have NO writer surface — every structural edit in this plan's own authoring was ad-hoc python read-modify-write, causing the dw-id collisions and stale-sibling risk; tk-7043's '6/6a mutate states through dd surfaces' implied verbs that do not exist. | ac-7019 / tk-7028: dd get/set/add/rm + minting, schema-validated before write, auto sibling rebuild; surface renegotiation. |

<a id="phases"></a>

## Phases

| id | title | brief | state | note | receipt | depends_on | tasks | objective | delivers | key_risks |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ph-7001 | dd core — relations, buckets, and the semantic validator | TASK ONE (Jordan): tk-7028 — the dd read/write verb family (get/set/add/rm + minting) lands FIRST, so every later task and the journey itself mutates documents through the CLI instead of ad-hoc JSON edits. Then: rel declaration parsing + frozen-five manifest row; bucket render column; done_when rename; satisfies; mandatory pressure + not-applicable; rel declarations on every existing builder link field; then the rel-generic contradiction engine, summary info line, --complete (strict-zero green), --address scoping, and the E450–E459 block via manifest renegotiation. Planted-bad fixtures per failure class; the synthetic corpus factory + lifecycle-mutation suites land here and serve the next phase too; dd validate regression-pinned byte-for-byte mechanical. | [ ] unchecked | — | — | — | [tasks](assets/tasks/phase-1/tasks.dd.md#tasks) | The schema layer learns rel, the renderer learns the links bucket, the builder/* schemas gain every field this design needs, and harness plan validate grows its opinions. | dd get/set/add/rm + CLI id minting — built first, used by everything after, rel on link shapes: parse, allow-list, resolved-schema exposure; unknown rel behaves as ref, Frozen-five manifest row + surface test; links bucket + rel labels/filter in graph map, builder/* schema upgrades: done_when, satisfies (array), mandatory pressure + not-applicable, rel declarations everywhere, Contradiction engine, summary line, --complete, --address; E450–E459, Synthetic corpus factory + lifecycle-mutation suites; dd validate regression pin | The OD-8 allow-list silently dropping rel — first task pins it at the parse layer. --address closure semantics — settle in tests before wiring. |
| ph-7002 | Wire the flow — check-kind gate + dd-native authoring | DdLink check variant behind deps.gate (live evaluation, verbatim-findings refusal, --force records); template/expander emit per-phase completion gates (bare-ordinal addresses) + last-review check gate; pre-JIT target-invalid refusal pinned as intended; barrel discipline extended; renderer/orient/rail surfacing. Then the prompt-ware: 1b plan authors plan.dd.json (no -plan.md), 5 tasks authors the task file + phase-row link in one stroke, 6/6a mutate states through dd, node instructions[] baked, stage modules teach their seam's commands, legacy read path retained. EXIT: the scripted end-to-end dry-run (create → JIT → refuse → green → depart), CLI as actor — this phase does not close without it. | [ ] unchecked | — | — | ph-7001 | — | Departure gates on the semantic validator, the template authors every gate, and the builder pipeline authors and mutates dd documents natively while teaching its own tooling. | Check-variant model + sanitize + untrusted-reading rules; evaluator behind deps.gate; act wiring, Template/expander gate authoring; pre-JIT refusal pin; dep-cruiser + seam test extension; badge/rail surfacing, 1b plan → plan.dd.json + sibling; 5 tasks → task file + link, one stroke; 6/6a dd state mutation, Node instructions[] baked + stage modules teach per-seam commands; legacy read/adopt path retained, EXIT: the end-to-end dry-run green | Barrel discipline (flow reaches the validator through an exported seam, never past one). Prompt-ware cannot be compile-checked — the exit dry-run is the proof; keep every module change behind it. |
| ph-7003 | Close-out — PR surface, corpus, docs, self-hosting audit, flow-eval | Ship renders the dd-derived AC table from the archive path (refuses an unclosed corpus; head-SHA links). Exemplar upgraded (18 dw rows pressured; satisfies; done_when; leaf-first ledger walk); docs/how/dd chapters + justfile recipes; structural-proof-graph promoted with a signpost; baked docs regenerated; archive-move proof (doctor-clean + dd_link rewrite). The self-hosting audit (ac-7017) and the single-phase blind flow-eval scenario (ac-7018) close the plan. | [ ] unchecked | — | — | ph-7002 | — | The human checkpoint sees claims with evidence, the reference corpus and docs tell the truth, and the flow proves itself twice over — self-hosted and blind-evaluated. | Corpus → PR-body AC table renderer + ship integration (archive path, head-SHA links), Exemplar schema upgrade; docs/how/dd chapters + recipes; proof-graph promotion; baked docs regen, Archive-move proof (doctor-clean + flow links resolve), Self-hosting retrofit audit; flow-eval single-phase scenario, scored + ledgered, Full just checks + warn-trio baseline | Corpus upgrade ripples basis shas — leaf-first. Broken PR links would discredit the surface on day one. |

<a id="tasks"></a>

## Tasks

| id | title | domain | phase | state | note | receipt | done | success | notes | satisfies |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| tk-7031 | DdLink check-variant model + sanitize + untrusted-reading rules | — | ph-7002 | [ ] unchecked | — | — | — | Hand-edited check links refused/dropped per F004/F007 discipline. | — | [ac-7009](#acceptance-criteria) |
| tk-7032 | Check evaluator behind deps.gate; act wiring | — | ph-7002 | [ ] unchecked | — | — | — | Non-green refuses, findings quoted verbatim; nothing written; --force records. | — | [ac-7009](#acceptance-criteria) |
| tk-7033 | Template/expander author gates (bare-ordinal addresses) | — | ph-7002 | [ ] unchecked | — | — | — | Fresh flow carries phase gates + last-review check gate; pre-JIT departure refuses target-invalid. | key finding: static template | [ac-7010](#acceptance-criteria) |
| tk-7034 | Barrel/seam extension + dep-cruiser rule + seam test | — | ph-7002 | [ ] unchecked | — | — | — | Arch test refuses module-path reach. | — | — |
| tk-7035 | Renderer/orient/rail surfacing for check gates | — | ph-7002 | [ ] unchecked | — | — | — | Badge + rail callout render; legend only when present. | — | — |
| tk-7041 | 1b plan authors plan.dd.json + sibling; dd-native root stated | — | ph-7002 | [ ] unchecked | — | — | — | Fresh plan validates 0/0 and renders; no -plan.md emitted. | — | [ac-7011](#acceptance-criteria), [ac-7013](#acceptance-criteria) |
| tk-7042 | 5 tasks authors task file + phase-row link in one stroke | — | ph-7002 | [ ] unchecked | — | — | — | JIT file validates; plan link lands in the same change. | — | [ac-7011](#acceptance-criteria) |
| tk-7043 | 6/6a mutate dd states; execution-log linkage | — | ph-7002 | [ ] unchecked | — | — | — | State flips visible in dd; no hand-edited generated md. | depends on tk-7028 (dd mutation verbs are the surface 6/6a mutates through) | — |
| tk-7044 | Node instructions[] baked (template + expander) | — | ph-7002 | [ ] unchecked | — | — | — | orient at each node prints its dd commands. | ac-7016 layer a | [ac-7016](#acceptance-criteria) |
| tk-7045 | Stage modules teach their seam's commands | — | ph-7002 | [ ] unchecked | — | — | — | Dry-run reader gap check passes. | ac-7016 layer b | [ac-7016](#acceptance-criteria) |
| tk-7046 | Legacy read/adopt path retained + detection test | — | ph-7002 | [ ] unchecked | — | — | — | Legacy-folder fixture adopts/resumes. | key finding: 63 markdown refs | [ac-7015](#acceptance-criteria) |
| tk-7047 | Joint-exit dry-run: create → JIT → refuse → green → depart | — | ph-7002 | [ ] unchecked | — | — | — | Scripted, CLI as actor; exercises ph-7003's refusal through ph-7004's artifacts; neither phase closes before this is green. | PHASE EXIT for ph-7002 — the phase does not close without this green | [ac-7011](#acceptance-criteria) |
| tk-7051 | Corpus → PR-body AC table renderer | — | ph-7003 | [ ] unchecked | — | — | — | Goldens over the exemplar; refuses unclosed corpus. | — | [ac-7012](#acceptance-criteria) |
| tk-7052 | Ship stage integration (archive path, head-SHA links) | — | ph-7003 | [ ] unchecked | — | — | — | Links click through on a real PR. | key finding: archive-before-ship | [ac-7012](#acceptance-criteria) |
| tk-7061 | Exemplar schema upgrade (pressure, satisfies, done_when) | — | ph-7003 | [ ] unchecked | — | — | — | Doctor-clean 0/0; leaf-first re-verify. | key finding: 18/26 rows | [ac-7004](#acceptance-criteria), [ac-7014](#acceptance-criteria) |
| tk-7062 | docs/how/dd chapters + justfile recipes for new commands | — | ph-7003 | [ ] unchecked | — | — | — | Every new command has a runnable recipe. | — | [ac-7014](#acceptance-criteria) |
| tk-7063 | Promote structural-proof-graph into docs/how/dd | — | ph-7003 | [ ] unchecked | — | — | — | Links repaired; builder-tuning keeps a signpost. | — | — |
| tk-7064 | Baked docs regen + drift gate | — | ph-7003 | [ ] unchecked | — | — | — | check:dd-docs green both ways. | — | [ac-7014](#acceptance-criteria) |
| tk-7065 | Archive-move proof: git mv fixture + dd_link rewrite | — | ph-7003 | [ ] unchecked | — | — | — | Doctor-clean post-move; flow links resolve. | key finding: repo-root anchoring | [ac-7013](#acceptance-criteria) |
| tk-7066 | Full just checks + warn-trio baseline verification | — | ph-7003 | [ ] unchecked | — | — | — | Exit 0; trio byte-identical. | — | [ac-7014](#acceptance-criteria) |
| tk-7067 | Self-hosting retrofit + proof: plan 070's corpus fully dd-native | — | ph-7003 | [ ] unchecked | — | — | — | backpressure + execution-log docs linked from every AC; task files with done_when; rel-typed satisfies; flight-plan gate events show a real refusal and a real pass; plan validate --complete green; doctor-clean. | The journey itself produces most of this; the task is the final audit that nothing was hand-waved. | [ac-7017](#acceptance-criteria) |
| tk-7068 | Flow-eval scenario: single-phase dd-native journey (blind subject, telemetry-scored) | — | ph-7003 | [ ] unchecked | — | — | — | Scenario bundle (live-testing/scenarios/) + prompts pair authored; base-ref pinned to include 070's own code (else the telemetry lane goes blind); assertions reuse existing types (skill-called builder, harness-verb-ran dd/plan validate, file-created plan.dd.json glob, forbidden-state on *-plan.md, file-content-matches for pressure/satisfies, command-succeeds --complete, refusal via tk-7069's evidence); scored via harness flow-eval score, judged lane filled, rendered, ledgered; instructions.md + flow-eval-run SKILL updated. | Research report: assets/research-flow-eval-history.md — reuse list §6 | [ac-7018](#acceptance-criteria) |
| tk-7069 | Refusal evidence: envelope.error.code into command_exit + SessionEvidence/resolvers widening (lock-step) | — | ph-7002 | [ ] unchecked | — | — | — | A refused nav set lands in telemetry as command_exit with the gate E-code (fixed vocab, no free text); SessionEvidence exposes it and the flow-eval resolvers mirror stays lock-step (CLI test green); the flow file remains untouched on refusal (invariant pin unchanged). | Research: refusal-evidence triple loss; force already durable via dd-gate-override | [ac-7017](#acceptance-criteria), [ac-7018](#acceptance-criteria) |

<a id="acceptance-coverage"></a>

## Acceptance Coverage Map

| ac | covered_by | verified_in |
| --- | --- | --- |
| ac-7001 | tk-7011, tk-7012, tk-7014, tk-7015 | schema tests + graph-map tests |
| ac-7002 | tk-7013, tk-7014 | renderer goldens + traversal tests |
| ac-7003 | tk-7015 | planted-bad fixture + jq doc |
| ac-7004 | tk-7015, tk-7061 | schema tests + doctor sweep |
| ac-7005 | tk-7015 | schema tests + graph-map incoming |
| ac-7006 | tk-7021, tk-7027 | per-rel planted-bad fixtures |
| ac-7007 | tk-7022, tk-7023, tk-7027 | mode tests + strict-zero pin |
| ac-7008 | tk-7024, tk-7027 | scope-closure tests |
| ac-7009 | tk-7031, tk-7032, tk-7027 | gate suite (refuse/force/untrusted) |
| ac-7010 | tk-7033 | template tests + pre-JIT refusal pin |
| ac-7011 | tk-7041, tk-7042, tk-7047 | scripted end-to-end dry-run |
| ac-7012 | tk-7051, tk-7052 | goldens + live PR check |
| ac-7013 | tk-7041, tk-7065 | root allow-list + archive-move proof |
| ac-7014 | tk-7061, tk-7062, tk-7064, tk-7066 | checks green + baseline |
| ac-7015 | tk-7046 | legacy-fixture detection test |
| ac-7016 | tk-7044, tk-7045 | orient output + dry-run reader gap check |
| ac-7017 | tk-7067, tk-7047, tk-7069 | recorded flow events + --complete green + PR render of this plan's own AC table |
| ac-7018 | tk-7068, tk-7069 | flow-eval report + ledger from a real blind-subject run |
| ac-7019 | tk-7028 | verb suites: pre-write validation refusal, sibling rebuild, mint collision test, surface-manifest pin |

<a id="risks"></a>

## Risks

| risk | likelihood | impact | mitigation |
| --- | --- | --- | --- |
| Prompt-ware drift — P4 modules say one thing, the CLI does another. | Medium | High | Joint-exit dry-run with the CLI as actor; doc-drift checks. |
| --address closure semantics surprise (what counts as in scope). | Medium | Medium | Settle in tk-7024's tests first; document the closure rule in the chapter. |
| Corpus upgrade breaks downstream goldens broadly. | Medium | Medium | Phase-6 isolation; leaf-first ledger walk; goldens regenerated deliberately, never blindly. |
| Check-gate evaluation cost at every departure. | Low | Medium | Scoped to the last review node only; completion gates stay cheap. |
| Layout amendments diverge from #90 silently. | Low | High | Amendments STATED in builder docs (ac-7010/ac-7013) — divergence is documented, never discovered. |

<a id="execution-guardrails"></a>

## Execution Guardrails

- Worked in the s065 worktree/branch (Jordan's call); rebased onto main before drafting; rebuild dist after every rebase or CLI edit (the hook runs THIS worktree's dist — merged-is-not-running, per-checkout variant).
- No pushes; no commits until the prime clears the commit path (Jordan, 2026-08-04).
- just fix before any push (CI gates on biome).
- Frozen surfaces extend only via the one-line manifest renegotiation; the five rels and the E45x block join the manifest on landing.
- Historical corpora are never rewritten to satisfy new rules — living corpora upgrade; frozen ones get signposts.
