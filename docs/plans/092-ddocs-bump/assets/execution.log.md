# Execution log — plan 092 ddocs bump

## Result

Pinned `@ai-substrate/dd` to `de01b77a67d0cff961e31ad8189054dd4b5c686b`, migrated the package-local executable from `node_modules/.bin/dd` to `node_modules/.bin/ddocs`, updated the runtime probe/tests/docs, and regenerated the deterministic-document siblings whose renderer banner changed.

## RED

- `npm ci` installed the pinned package; its prepare-time `npm run build` passed. There was no TypeScript RED.
- First `just test-all`: 15 failures. Nine were migration-relevant: old `runDd` binary resolution caused plan/fence/review/archive/semantics failures and the lockfile SHA assertion exposed the stale lock. Six PTY sensor failures were already present and unrelated to dd.
- After the binary/lock migration, the focused regression set reduced to one expected render-drift failure for `docs/how/dd/exemplar/review/round-1.dd.md`; regenerating it made the set pass.

## Changes and classification

- Binary sweep: 31 actionable files and 185 occurrences changed. The six live-consumer files were handled first-class: `doctor-service.ts`, generated flow schema content, two tests, `run-cli.ts`, and the live-testing assertion corpus. `AGENTS.md` and the 24 files in the packet's named roots completed the denominator.
- Historical plan-080 receipts and this packet retain the old spelling because they describe commands and migration facts at their recorded time; rewriting them would falsify evidence.
- `DdGraphNode` union: `npm run build` produced no diagnostics. The inspected `.sha`/`.schema` reads in `harness/cli/src/acts/plan/index.ts` are not graph-union reads: `check.sha` comes from `readPlanCheck`, `documentSet.schema` from the failed `planDocuments` result, and `pinned.sha` from `headBlobBase`. No narrowing was needed and no explicit file-node branch was reachable in this consumer.
- Additive `edge.kind` and `file_findings`: no output-shape assertion failed. The new `file_findings: []` field appeared in real `ddocs build` receipts without requiring consumer changes.
- E463/tally behavior: no E463 or tally-mismatch assertion exists in the live CLI/test tree, so no assertion moved.
- `@ai-substrate/dd/plan`: no live import exists; the only search hit is archived evidence that the export is intentionally blocked.
- Renderer change: all seven exemplar `.dd.md` siblings were regenerated through `ddocs build`; the two basis ledgers invalidated by the edited backpressure command were refreshed through `ddocs link verify-basis --update`.

## Verification receipts

- Final `npm run build` — PASS after migration.
- Focused migration regression set — PASS, 4 files / 63 tests (fast-scope exclusion omitted the slow archive file in this invocation).
- `npx vitest run test/acts/builder-dd-teaching.test.ts` — PASS, 24/24 after updating the escaped regex control.
- `just test` — PASS, 334 files / 5,204 tests; fast scope explicitly skipped 12 slow files.
- `just test-all` — ran the full scope after migration: 7 failures, comprising one binary-teaching expectation subsequently fixed and 6 unchanged PTY sensor failures also observed before migration.
- `just fix` — completed; three pre-existing unsafe unused-import suggestions remain warnings.
- `node scripts/flow-fixtures.mjs --check` — PASS, 6 fixtures.
- Seven `node_modules/.bin/ddocs build <doc> --check` probes — PASS with `drift:false`.
- Final `node_modules/.bin/ddocs doctor --json` — PASS: 46 documents swept, 0 errors, 0 warnings.
- `harness checks` — executed; failed on a transient hook-wrapper test and `check:flows`. The hook-wrapper rerun passed 5/5. `check:flows` regenerated byte-identical intended output, then its `git diff --exit-code` correctly saw this working tree's checked-in generated-file migration as an unstaged diff; the independent fixture check passed.

## Discoveries & Learnings

| Tag | Discovery | Disposition |
|---|---|---|
| Noteworthy | The packet's 27-file estimate omitted live consumers. The measured actionable denominator was 31 files / 185 occurrences. | Migrated all 31; reported the correction to prime before editing. |
| Noteworthy | The producer's union warning did not materialize in this tree; all candidate hash/schema reads were non-graph result types. | Classified from the compiler and source; made no speculative narrowing. |
| Noteworthy | The binary rename also changes the generated banner from `dd build` to `ddocs build`, invalidating exemplar siblings and basis hashes. | Regenerated all seven siblings and refreshed all three affected basis entries through the CLI. |
| Noteworthy | Flowspace semantic search found the correct file but ranked stale worktree copies ahead of main. | Treated hits as leads, verified this worktree's source, and reported the ranking variant to prime. |
| Deferred | Six real-PTY sensor tests time out/fail identically before and after this migration. | Outside the dd migration; preserved exact full-suite evidence for prime rather than masking it. |
| Deferred | The composite `check:flows` gate cannot be green on an unstaged intentional generated-file change because it compares the generated file to Git. | Independent generation plus the six-fixture check passed; prime can re-run the composite gate after composition/staging. |
