# Flight Plan: Fix FX004 — onboarding-experience test

**Fix**: [FX004-onboarding-experience-test.md](FX004-onboarding-experience-test.md)
**Status**: In flight — FX004-1…7 landed (`5fa96b0`…`7d705eb`); FX004-8 E2E smoke running

## What → Why

**Problem**: The worker prompt is a 256-line runbook (a 4th drifting copy of product knowledge); grading rests on worker self-report booleans; only 3 of 7 skills are mounted so the documented onboarding path is structurally unavailable; the clone never gets a project-local skills install. The test proves runbook execution, not onboarding.
**Fix**: Replace the runbook with a pure goal brief; de-leak the worker rules; drop governance from the test; mount all 7 skills; grade each clone with deterministic extension-side probes (skills-install graded, observe usage INFO); encode the `--prefix` trap product-side; refresh both operator docs; prove it with one lean E2E smoke.

## Domain Context

| Domain | Relationship | What Changes |
|--------|-------------|-------------|
| agents | owner | `prompt.md` → goal brief; `instructions.md` de-leak (8 rails stay); `output-schema.json` de-governance; `agent.json` descriptions |
| dogfood extensions | owner | `SKILL_FLAGS` ×7; post-collect deterministic probes + rollup de-governance; operator briefing |
| skills | modify | `eng-harness-0-setup` install step 2 gains the no-`package.json` guard |
| docs | modify | `docs/how/dogfood-harness-flow.md` matches the lean test |

## Stages

- [x] Stage 1: FX004-1 + FX004-6 — goal brief + `agent.json` + `--prefix` trap encode (same change set; no window where the trap lives nowhere) — `5fa96b0`
- [x] Stage 2: FX004-2 — de-leak worker rules (all 8 rails stay) — `469fc6b`
- [x] Stage 3: FX004-3 — output-schema de-governance (`minih check` green on PASS + ABANDONED samples) — `154dddc`
- [x] Stage 4: FX004-4 + FX004-5 — `SKILL_FLAGS` ×7 (exact `.minih.json` parity) + deterministic probes + rollup de-governance (fixture: drain ✗ + 2 discrepancy rows) — `9074a62`
- [x] Stage 5: FX004-7 — operator briefing + how-to guide (rich, updated; mount-suppression caveat) — `7d705eb`
- [~] Stage 6: FX004-8 — lean E2E smoke (fire 1 goal-briefed worker, collect, review; findings surfaced, never auto-implemented)

## Acceptance

The 10 bullets in the dossier's `## Acceptance` — headline checks: zero-product-knowledge grep on the brief; 7-skill mount parity; probes table in ROLLUP.md with discrepancy flags; skills-install probe (mount does not count); governance nowhere; `--prefix` trap product-side only; one collected E2E run.
