# Original ask — dogfood-harness-flow
**Captured**: 2026-06-09T11:36Z  ·  **By**: /the-flow

> Now look at refamiliarise wit the .harness/extensions/validate-harnessability.ts extension and the associated minih agent. We need a new one that is similar but it runs the entire "harness flow" skill from a freshly checked out git repo, is is to install the harness (its already installed on this machine etc so yeah skip that), then its to do the harnessbiliy assessment, then set up the harness md as descried int he flow (governance), then detect what would make a good boot extensions and then get that working. e.g. the full setup a new harness flow. If we find that the repo we have chosen does not support good harnessabiliy that is good, but we should choose another from our list of options on GitHub (popular git repos are good, node, python or go etc fine languages to use). Then we collect the retros from the harness properly. Also we should use hte harness flow ourselves! And dogfood it, so we can see it in action nd record our own retros properly as per the guide, on how using and dogfooding our very own harness extensions helps is work on testing out the actual harness flow using Minih agents. This plan will set that up and will culminate in us running the agents in parallel (3 repos at once please) and collecting all their outputs and records when finished so we might improve the system!. Also just making sure that whilst the flow harvests retros etc, we don't auto implement them, teh options are presented to the user and they may go deeper, but it never automatically imeplements. So during our work, we will record our run, harvest the data and store them as retros and hten report back at the end! That is unless if we try to store a record as part of our work and its planing not working, e.g. earlier work failed, we would fix that. But not wholesale improvements that come from retros.
