{
  "dd": {
    "schema": "builder/plan"
  },
  "sections": [
    {
      "name": "meta",
      "value": {
        "title": "Phase 1 — the hook runtime",
        "slug": "phase-1",
        "status": "draft",
        "summary": "Sensors first, then the runtime. The guard is the hard part and a first-parent check is not enough."
      }
    },
    {
      "name": "summary",
      "value": "Sensors first, then the runtime. The guard is the hard part and TWO mechanisms are already known to be insufficient: a `merge --squash` commit has ONE parent, its first parent IS the recorded HEAD, and its reflog message reads `commit: <msg>` — byte-identical to an authored commit. Measured across eight isolated repos: parent-count catches only `--no-ff` merges; the reflog discriminator catches ff-pull, cherry-pick, revert and amend; NOTHING yet catches squash-merge. Design for that, not for the cases that are easy to reject. Tasks 1-4 build the sensors and the missing ports before any feature code."
    },
    {
      "name": "tasks",
      "value": [
        {
          "id": "tk-0001",
          "title": "Add `node:net` to the architecture guard and prove it RED before it is green",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`no-direct-node-io.test.ts:23` currently forbids only `node:fs` and `node:child_process` — `grep -rn 'node:net'` across the arch tests returns ZERO, so a service importing node:net passes `just checks` today and the plan's G3 note is false. Add `/from ['\\\"]node:net['\\\"]/` to FORBIDDEN, then demonstrate the guard REFUSING a deliberate import and record that refusal in the execution log. A gate is not verified until it has refused.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0007"
          ],
          "done": "#done_when/tk-0001"
        },
        {
          "id": "tk-0002",
          "title": "Add a reflog read to the git port — it does not exist yet",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`grep -rn reflog harness/cli/src` returns NOTHING; `git-read-port.ts` exposes no reflog read. The transition classifier cannot be written without it. Port method + exec adapter + fake + tests. Split out of the classifier task deliberately: it is a hidden prerequisite that would otherwise block every provocation row.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "done": "#done_when/tk-0002"
        },
        {
          "id": "tk-0003",
          "title": "Build a PURE transition classifier, `classifyHeadTransition(prev, head, parents, reflogSubject, gitDirState) -> 'emit' | 'silent'`",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "A pure function with no IO, so the provocation suite has a callable target before any runtime exists. It must reject BOTH classes and it must not rely on parent-count plus reflog alone — those two are measured insufficient against squash-merge. The third discriminator is `.git` state (MERGE_HEAD / SQUASH_MSG presence) or an equivalent that distinguishes content authored elsewhere.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "done": "#done_when/tk-0003"
        },
        {
          "id": "tk-0004",
          "title": "Build the provocation suite against the pure classifier — every row, including the ones that defeat a naive guard",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "Class (a), HEAD did not advance by exactly one: checkout, reset --hard, rebase, amend, no-op, detached HEAD, no prior recorded state, AND a MULTI-COMMIT fast-forward pull (HEAD advances by more than one). Class (b), HEAD advanced by one whose first parent is the recorded HEAD but nothing was authored here: single-commit ff pull, --no-ff merge, cherry-pick, revert, `git am`, and **`merge --squash` + commit** — the last is byte-identical to an authored commit under parent-count AND reflog, and is the row that matters most. Plus the positive case (a genuinely authored commit) so the suite can see the opposite, and the CONCURRENT case: two POST fires reading state before either writes. Every class (a) and (b) row asserts SILENT; the positive asserts EMIT.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "done": "#done_when/tk-0004"
        },
        {
          "id": "tk-0005",
          "title": "Build the live-daemon note fixture in `test/support/hermetic-git.ts` — the ONLY file exempt from the hermetic-git guard — with a PAIRED positive control",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`vitest.config.ts:130-133` already sets GIT_TRACE2_EVENT='0' for the whole run, so 'no note appeared' is trivially true and a negative-only fixture proves nothing. `hermetic-git-fixtures.test.ts:52-70` also FAILS any test file other than hermetic-git.ts that mentions GIT_TRACE2_EVENT — so the helper must live there. Assert BOTH: no note when trace2 is discarded, AND the note IS written when the events reach a live daemon. Assert note IDENTITY — the file, the line range, AND the session id — never a note count. With no daemon present the positive records SKIPPED, never PASSED.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001",
            "../../../plan.dd.json#acceptance_criteria/ac-0002"
          ],
          "done": "#done_when/tk-0005"
        },
        {
          "id": "tk-0006",
          "title": "Implement the commit intercept over the classifier: PRE records HEAD, POST decides, with an ATOMIC state write",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "The POC writes state AFTER emitting, which is the read-then-write window that lets two concurrent fires both emit. Use an exclusive create or lock file so the concurrent provocation row goes green. All class (a) and (b) rows silent, the positive row emits.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "done": "#done_when/tk-0006"
        },
        {
          "id": "tk-0007",
          "title": "Implement the trace2 tickler over the EXISTING socket port — do not build a second one",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`NodeSocketProbe.send()` already does `sock.end(payload, cb)` and resolves {ok, bytes} — the identical call shape the POC uses, half-close included. Only the 750ms default timeout differs from the POC's 5000, and it is constructor-injectable. Six events sharing one sid, written through SocketRelayPort. The node:net guard from task 1 must pass with the rule ADDED.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001"
          ],
          "done": "#done_when/tk-0007"
        },
        {
          "id": "tk-0008",
          "title": "Wire `harness hooks fire <agent> --phase pre|post --hook-input stdin` as a core verb",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`src/app.ts:469-515` registers core verbs as a flat sequence; `registerCommitAct` is the precedent. Update the frozen verb lists in `test/app.test.ts` and `test/index.test.ts`. Core verb, not a repo extension — hooks are machine-scoped and doctor must call it.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001"
          ],
          "done": "#done_when/tk-0008"
        },
        {
          "id": "tk-0009",
          "title": "Make every failure path exit 0 and silent, proven by fault injection",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "Inject a dead socket, an unreadable config, a malformed payload, a non-git directory, and a thrown exception. Each exits 0 and writes nothing the agent can see. The deviation is scoped to `error`/`unconfigured` only — a reachable failure is `degraded`, which already maps to exit 0 under architecture.md:91 and needs no deviation.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0007"
          ],
          "done": "#done_when/tk-0009"
        },
        {
          "id": "tk-000a",
          "title": "Write every fire outcome to a Phase-1-owned journal file, so a silent failure is visible without waiting for Phase 2",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`harness hooks status` is a PHASE 2 deliverable and Phase 2 depends on Phase 1 — so Phase 1 cannot assert on it. The runtime writes its outcome (emitted / silent-with-reason / failed-with-cause) to a state file; this task asserts on THAT file with an unreachable socket. Phase 2's status verb merely reads it. Without this, the G2 deviation is unpurchased for the whole of Phase 1.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-000b"
          ],
          "done": "#done_when/tk-000a"
        }
      ]
    },
    {
      "name": "done_when",
      "value": {
        "tk-0001": [
          {
            "id": "dw-0001",
            "assertion": "The guard REFUSED a deliberate node:net import and the refusal is recorded in the execution log — not merely that the rule was added.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0002": [
          {
            "id": "dw-0002",
            "assertion": "A fake reflog adapter exists, and the exec adapter is covered by a test that reads a real reflog subject.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0003": [
          {
            "id": "dw-0003",
            "assertion": "classifyHeadTransition is pure — no IO, no git shell-out.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0004",
            "assertion": "It returns 'silent' for a squash-merge whose parent count is 1 and whose reflog subject reads 'commit: <msg>'.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0004": [
          {
            "id": "dw-0005",
            "assertion": "Every class (a) and class (b) row asserts SILENT and passes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0006",
            "assertion": "The squash-merge row passes — it defeats parent-count AND reflog, measured.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0007",
            "assertion": "The multi-commit fast-forward pull row passes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0008",
            "assertion": "The concurrent-fire row passes: two racing POSTs produce at most one emit.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0009",
            "assertion": "The positive row EMITS, so the suite can see the opposite.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0005": [
          {
            "id": "dw-000a",
            "assertion": "The helper lives in test/support/hermetic-git.ts and hermetic-git-fixtures.test.ts still passes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000b",
            "assertion": "The negative asserts no note when trace2 is discarded.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000c",
            "assertion": "The positive asserts the note IS written against a live daemon, recorded SKIPPED — never PASSED — when no daemon is present.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000d",
            "assertion": "Assertions name the file, the line range AND the session id. Nothing counts notes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0006": [
          {
            "id": "dw-000e",
            "assertion": "The concurrent provocation row is green against the real intercept, not just the pure classifier.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0007": [
          {
            "id": "dw-000f",
            "assertion": "No node:net import in any service; just checks passes WITH the rule added by task 1.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0010",
            "assertion": "SocketRelayPort was reused; whether it needed extending is recorded with the reason.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0008": [
          {
            "id": "dw-0011",
            "assertion": "harness hooks fire cursor --phase post --hook-input stdin runs and exits 0.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0012",
            "assertion": "The frozen verb lists in test/app.test.ts and test/index.test.ts are updated and green.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0009": [
          {
            "id": "dw-0013",
            "assertion": "Each of the five injected faults exits 0 and writes nothing the agent can see.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-000a": [
          {
            "id": "dw-0014",
            "assertion": "With an unreachable socket, the journal records the failure AND its cause.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0015",
            "assertion": "The assertion reads the journal file, not harness hooks status — that verb is Phase 2 and cannot be a Phase 1 dependency.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0010": []
      }
    }
  ]
}
