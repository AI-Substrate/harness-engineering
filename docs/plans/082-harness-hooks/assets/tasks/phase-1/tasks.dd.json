{
  "dd": {
    "schema": "builder/plan"
  },
  "sections": [
    {
      "name": "meta",
      "value": {
        "title": "Phase 1 — the hook runtime",
        "slug": "phase-1",
        "status": "draft",
        "summary": "Sensors first, then the runtime. The guard is the hard part and a first-parent check is not enough."
      }
    },
    {
      "name": "summary",
      "value": "Sensors first. THE DISCRIMINATOR IS THE INDEX STATE RECORDED AT PRE — measured, and it is the only mechanism that works. Three earlier candidates are measured DEAD: parent-count catches only --no-ff merges; the reflog subject catches ff-pull, cherry-pick, revert and amend; and `.git` state (MERGE_HEAD/SQUASH_MSG) DOES NOT EXIST when our hook runs — MERGE_HEAD is never written by --squash at all, and SQUASH_MSG is unlinked by git before even git's own post-commit hook fires. SEVEN transitions are byte-identical to a real authored commit on all three: merge --squash, cherry-pick -n, revert -n, git apply, checkout REF -- path, restore --source, and read-tree -m -u (which is how git subtree works). At PRE, every one of those has an ALREADY-STAGED index while a genuine agent edit has a CLEAN one. Its only error mode is a false NEGATIVE, which is the direction our doctrine demands. It does not solve a HUMAN committing inside the PRE/POST bracket — nothing does, because nothing in git records who typed. That case is known and accepted, not mitigated."
    },
    {
      "name": "tasks",
      "value": [
        {
          "id": "tk-0001",
          "title": "Add `node:net` to the architecture guard and prove it RED before it is green",
          "state": "checked",
          "phase": "phase-1",
          "success": "`no-direct-node-io.test.ts:23` currently forbids only `node:fs` and `node:child_process` — `grep -rn 'node:net'` across the arch tests returns ZERO, so a service importing node:net passes `just checks` today and the plan's G3 note is false. Add `/from ['\\\"]node:net['\\\"]/` to FORBIDDEN, then demonstrate the guard REFUSING a deliberate import and record that refusal in the execution log. A gate is not verified until it has refused.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0007"
          ],
          "done": "#done_when/tk-0001",
          "receipt": "npx vitest run test/architecture/no-direct-node-io.test.ts — REFUSED the planted src/services import (1 failed), green after removal (189 files, 0 offenders). Refusal verbatim in execution.log.md."
        },
        {
          "id": "tk-0002",
          "title": "Add a reflog read to the git port — it does not exist yet",
          "state": "checked",
          "phase": "phase-1",
          "success": "`grep -rn reflog harness/cli/src` returns NOTHING; `git-read-port.ts` exposes no reflog read. The transition classifier cannot be written without it. Port method + exec adapter + fake + tests. Split out of the classifier task deliberately: it is a hidden prerequisite that would otherwise block every provocation row.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "done": "#done_when/tk-0002",
          "receipt": "npx vitest run test/adapters/git/{fake-git,exec-git}.test.ts — 39 passed; real-git test reads 'commit (initial): seed: the first thing' and 'commit: squashed in'. Mutation check (subject truncation) went red."
        },
        {
          "id": "tk-0003",
          "title": "Extend the PRE hook to record the INDEX STATE alongside HEAD — the measured discriminator",
          "state": "checked",
          "phase": "phase-1",
          "success": "PRE records whether the index is clean or already-staged, plus HEAD. This is the ONLY mechanism that separates the seven defeaters from a genuine agent commit, and unlike .git state it is captured before the commit can erase it. Store it atomically (exclusive create or lock file) so two racing fires cannot both read a stale value.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "receipt": "GitPort.indexState() + FsPort.createExclusive() + HookStateStore (atomic temp+rename PRE write, O_EXCL claim, bounded claim prune CLAIM_KEEP=50). 14 tests incl. REAL-git survival proof and a real-filesystem prune bound; 6 mutation checks all caught."
        },
        {
          "id": "tk-0004",
          "title": "Build a PURE transition classifier over (prev, head, parents, reflogSubject, indexStateAtPre)",
          "state": "checked",
          "phase": "phase-1",
          "success": "Pure — no IO, no git shell-out — so the provocation suite has a callable target. `.git` state is NOT a parameter: it does not exist at decision time and including it would let a fabricated input make a broken system look green.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ],
          "receipt": "classifyHeadTransition(inputs) in src/services/hooks/classify-head-transition.ts — pure, type-only import. 29 tests. Purity + no-.git-state guards each PROVEN RED against a planted violation. 3 mutation checks caught."
        },
        {
          "id": "tk-0005",
          "title": "Build the provocation suite AGAINST REAL GIT REPOSITORIES, not against the pure classifier",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "Every row drives real git in an isolated repo and asserts on the RUNTIME's decision. A suite that feeds the pure function a fabricated state proves only that the implementation matches itself. Class (a) HEAD did not advance by exactly one: checkout, reset --hard, rebase, amend, no-op, detached HEAD, no prior state, multi-commit ff pull. Class (b) HEAD advanced by one, first parent == recorded HEAD, nothing authored here — ALL SEVEN MEASURED DEFEATERS: merge --squash, cherry-pick -n, revert -n, git apply, checkout REF -- path, restore --source, read-tree -m -u. Plus single-commit ff pull, --no-ff merge, cherry-pick, revert, git am. Plus the positive control (a genuine agent edit then commit) so the suite can see the opposite, and the CONCURRENT case: two POSTs racing produce at most one emit. Plus the KNOWN-BLIND row: a human committing inside the bracket is indistinguishable and MUST be asserted as emitting — documenting the limitation, not hiding it.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ]
        },
        {
          "id": "tk-0006",
          "title": "Build the live-daemon note fixture in test/support/hermetic-git.ts with a PAIRED positive control",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "vitest.config.ts:132 sets GIT_TRACE2_EVENT='0' run-wide, so a negative-only fixture is trivially true. hermetic-git-fixtures.test.ts walks EVERY .ts under test/ and allows only this exact path. Assert BOTH directions. Assert note IDENTITY — file, line range AND session id — never a count. Without a live daemon the positive records SKIPPED, never PASSED. IF NO DAEMON IS AVAILABLE IN CI, say so in the execution log: ac-0001 and ac-0002 then end Phase 1 UNPROVEN and that must be visible, not discovered later.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001",
            "../../../plan.dd.json#acceptance_criteria/ac-0002"
          ]
        },
        {
          "id": "tk-0007",
          "title": "Implement the commit intercept over the classifier, with an atomic PRE state write",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "The POC writes state after emitting — the read-then-write window that lets two concurrent fires both emit. Exclusive create or lock file. Every class (a) and (b) row silent against REAL git; the positive row emits.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ]
        },
        {
          "id": "tk-0008",
          "title": "Implement the trace2 tickler as ONE send() carrying all six newline-joined events",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "SocketRelayPort.send(path, payload) opens a connection and half-closes per call — six calls would give the daemon six sessions, not one. Join the six events with newlines into a single payload, as the POC does. No node:net in any service; `just checks` passes WITH the rule added by tk-0001.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001"
          ]
        },
        {
          "id": "tk-0009",
          "title": "Wire `harness hooks fire <agent> --phase pre|post --hook-input stdin` as a core verb",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "src/app.ts:469-515 registers core verbs flatly; registerCommitAct is the precedent. Update BOTH frozen lists in test/app.test.ts (two blocks) and test/index.test.ts, AND add the verb to RESERVED_NAMES in src/services/extensions/registry.ts — without it a repo extension can shadow a core verb.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001"
          ]
        },
        {
          "id": "tk-000a",
          "title": "Make every failure path exit 0 and silent, proven by fault injection",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "Dead socket, unreadable config, malformed payload, non-git directory, thrown exception — each exits 0 and writes nothing the agent can see. Because exit 0 is unconditional, NO assertion anywhere may rest on the exit code alone: it carries no information by design. Assert on the journal.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0007"
          ]
        },
        {
          "id": "tk-000b",
          "title": "Write every fire outcome to a Phase-1-owned journal, so a silent failure is visible without Phase 2",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "`harness hooks status` is a Phase 2 deliverable and Phase 2 depends on Phase 1, so Phase 1 cannot assert on it. The runtime journals emitted / silent-with-reason / failed-with-cause; this task asserts on THAT file with an unreachable socket. Phase 2's status verb merely reads it. Coverage of ac-000b is therefore PARTIAL by design, and Phase 1 must not close claiming that AC.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-000b"
          ]
        },
        {
          "id": "tk-000c",
          "title": "Prove the runtime on Linux in the OrbStack VM — the assumption everything rests on",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "The plan's own Key Finding says the unsandboxed-hook-runner premise is measured on macOS ONLY and 'the entire design rests on this'. Run the provocation suite and the live-daemon fixture inside the VM. Record the result as MEASURED for Linux with the invocation pasted. Windows stays UNVERIFIED and is written into the findings doc for the remote agent on #108 — a promise to escalate needs an artifact in the same action.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0001"
          ]
        },
        {
          "id": "tk-000d",
          "title": "Add a SECOND guard layer: scan the PRE payload's tool_input.command for content-importing operations",
          "state": "unchecked",
          "phase": "phase-1",
          "success": "MEASURED 2026-08-09 from /tmp/cursor-hook-pre.jsonl (76 records): Cursor issues chained git commands in ONE Shell tool call — the captured line is `git add -A && git commit --trailer ... -m ...`. So defeater-inside-one-bracket is the NORMAL shape, not an exotic case, and index-at-PRE alone does not catch it: PRE sees a clean index because the defeater has not run yet. The same PRE record carries tool_name and tool_input.command, so interrogate the bracket directly. Refuse to emit when the command names merge (with or without --squash), cherry-pick, revert, am, apply, rebase, read-tree, checkout/restore with a source ref or pathspec, reset, stash pop/apply, or subtree. PARSE PER SEGMENT split on && ; || and newline — the defeater and the commit are different segments of one line, so a scan of the first token misses it every time. This NARROWS, it does not close: a script, alias, shell function, Makefile target or heredoc hides the operation, and those rows are asserted as EMIT and labelled KNOWN-BLIND alongside the human-in-the-bracket row. It is a SECOND layer over index-at-PRE, never a replacement — mutate each layer separately AND both together, because a single-layer negation passes while the other layer silently covers for it. Like every other check it may only move the answer TOWARD silence, adding no new false-positive path.",
          "satisfies": [
            "../../../plan.dd.json#acceptance_criteria/ac-0003"
          ]
        }
      ]
    },
    {
      "name": "done_when",
      "value": {
        "tk-0001": [
          {
            "id": "dw-0001",
            "assertion": "The guard REFUSED a deliberate node:net import and the refusal is recorded in the execution log — not merely that the rule was added.",
            "state": "checked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0002": [
          {
            "id": "dw-0002",
            "assertion": "A fake reflog adapter exists, and the exec adapter is covered by a test that reads a real reflog subject.",
            "state": "checked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0003": [
          {
            "id": "dw-0003",
            "assertion": "PRE records index-clean vs already-staged, and the value survives the commit that follows.",
            "state": "checked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0004",
            "assertion": "The write is atomic: two racing PRE fires cannot both read a stale value.",
            "state": "checked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0004": [
          {
            "id": "dw-0005",
            "assertion": "classifyHeadTransition is pure — no IO, no git shell-out.",
            "state": "checked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0006",
            "assertion": "`.git` state is NOT among its parameters; it does not exist at decision time.",
            "state": "checked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0005": [
          {
            "id": "dw-0007",
            "assertion": "Every row drives real git in an isolated repo and asserts on the RUNTIME's decision, not on the pure function.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0008",
            "assertion": "All SEVEN measured defeaters assert SILENT: merge --squash, cherry-pick -n, revert -n, git apply, checkout REF -- path, restore --source, read-tree -m -u.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0009",
            "assertion": "Every class (a) row asserts SILENT, including the multi-commit fast-forward pull.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000a",
            "assertion": "The positive control EMITS, so the suite can see the opposite.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000b",
            "assertion": "The concurrent row passes: two racing POSTs produce at most one emit.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000c",
            "assertion": "The human-in-bracket row asserts EMIT and is labelled KNOWN-BLIND — the limitation is documented, not hidden.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0006": [
          {
            "id": "dw-000d",
            "assertion": "The helper lives in test/support/hermetic-git.ts and hermetic-git-fixtures.test.ts still passes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000e",
            "assertion": "The negative asserts no note when trace2 is discarded; the positive asserts the note IS written against a live daemon.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-000f",
            "assertion": "Assertions name the file, the line range AND the session id. Nothing counts notes.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0010",
            "assertion": "Without a daemon the positive records SKIPPED, never PASSED, and the execution log states that ac-0001 and ac-0002 end Phase 1 unproven.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0007": [
          {
            "id": "dw-0011",
            "assertion": "Every class (a) and (b) row is silent against REAL git, not against the pure classifier.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0012",
            "assertion": "The concurrent row is green against the real intercept.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0008": [
          {
            "id": "dw-0013",
            "assertion": "Exactly ONE send() call carries all six newline-joined events — the daemon sees one session, not six.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0014",
            "assertion": "No node:net import in any service; just checks passes WITH the rule added by tk-0001.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-0009": [
          {
            "id": "dw-0015",
            "assertion": "Both frozen verb-list blocks in test/app.test.ts and the one in test/index.test.ts are updated and green.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0016",
            "assertion": "The verb appears in RESERVED_NAMES in src/services/extensions/registry.ts so a repo extension cannot shadow it.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0017",
            "assertion": "The journal records a successful fire — the assertion does NOT rest on exit code, which is unconditional by design.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-000a": [
          {
            "id": "dw-0018",
            "assertion": "Each of the five injected faults exits 0 and writes nothing the agent can see.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0019",
            "assertion": "No assertion in this task rests on the exit code alone.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-000b": [
          {
            "id": "dw-001a",
            "assertion": "With an unreachable socket, the journal records the failure AND its cause.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-001b",
            "assertion": "The assertion reads the journal file, not harness hooks status — that verb is Phase 2 and cannot be a Phase 1 dependency.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-000c": [
          {
            "id": "dw-001c",
            "assertion": "The provocation suite and the live-daemon fixture both run inside the VM, with the invocation pasted into the execution log.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-001d",
            "assertion": "Windows is written into the findings doc for the remote agent in the same action — a promise to escalate needs an artifact.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ],
        "tk-000d": [
          {
            "id": "dw-001e",
            "assertion": "The scan splits the command on && ; || and newline and inspects EVERY segment — proven by a row where `git merge --squash X && git commit -m msg` is refused, which a first-token scan would pass.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-001f",
            "assertion": "Each layer is mutated SEPARATELY and both together: disabling the command scan alone must go RED on the defeater-inside-bracket rows, and disabling index-at-PRE alone must go RED on the defeater-before-bracket rows. A defence-in-depth guard that only passes a both-layers negation has not tested either layer.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0020",
            "assertion": "Obfuscated defeaters (wrapped in a script or alias) are asserted as EMIT and labelled KNOWN-BLIND in the test name — documenting the limit rather than hiding it, exactly as the human-in-the-bracket row does.",
            "state": "unchecked",
            "pressure": "not-applicable"
          },
          {
            "id": "dw-0021",
            "assertion": "The downstream consequence of an over-emit is RECORDED, not asserted: a row observes what a live daemon actually does with an over-emitted squash-merge and records SKIPPED without one. We have never measured whether it writes a wrong note, a note with no agent lines, or fails closed — so neither the plan nor the code claims it fabricates authorship.",
            "state": "unchecked",
            "pressure": "not-applicable"
          }
        ]
      }
    }
  ]
}
