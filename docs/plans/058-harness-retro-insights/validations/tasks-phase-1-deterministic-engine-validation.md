# Validation — tasks/phase-1-deterministic-engine/tasks.md

- **Validated**: 2026-07-12T14:05:00Z
- **Target**: `docs/plans/058-harness-retro-insights/tasks/phase-1-deterministic-engine/tasks.md`
- **Contract sources**: `harness-retro-insights-plan.md` (v1.1.0, READY); `research-dossier.md`; `validations/harness-retro-insights-plan-validation.md` (VALIDATED WITH FIXES — G1–G7 consumed, not re-derived); `skills/eng-harness-flow/references/stages/retro.md`; `retro.schema.json` (v1.2); `constitution.md` P2/P3/P10
- **Checks**: verified every code citation against source — `sweepBySuffix` telemetry.ts:335 ✓, `makeRow` insights.ts:174 ✓, `N_THRESHOLD` :46 ✓, `InsightRow`/`InsightSection` :70/:111 ✓, `FsPort` fs-port.ts:11-15 ✓, `FakeFs` ctor fake-fs.ts:39 ✓, `listObservations` observe-service.ts:224 ✓, `registerObserveAct` app.ts:283 ✓, `formatOk`/`formatUnconfigured`/`formatError` envelope.ts:72/114/129 ✓, `exitWithEnvelope` exit.ts:38 ✓, `record-service.ts` write-only + `contract.ts:30` "CLI never parses it" ✓, `FIELD_PATTERN`/`unquote` buffer-codec.ts:48/159 ✓, `core-instructions.ts` `CORE_INSTRUCTIONS` ✓, `gen-docs.mjs`+`docs-manifest.json`+`check:docs` pipeline ✓; doctrine anchors retro.md legacy :405, precedence :411, stale :416, ranking :417, recognizer :419, proof-gap 5-target list :178 ✓, compound-value refs :39/:466 ✓; retro schema frozen x-schema-version 1.2 ✓; live corpus scan of `.harness/records/retro/**` (35 records, 182 entries). No independent critic — direct proof settled the target.
- **Verdict**: NEEDS ATTENTION
- **Thesis / proof**: purpose advanced — the dossier is a faithful, TDD-ordered decomposition of plan Phase 1; every Phase-1 task 1.1–1.8 and every Phase-1 AC (AC-01..08, AC-12) maps to a task, tests precede impl throughout, read-only/no-new-deps/no-schema-change constraints are honoured, and all ~18 code/doctrine line citations are current. One verification criterion (T009) is pinned to a stale corpus baseline.
- **Consumers**: 2/2 addressable — implement verb (executes T001–T009 verbatim; the T009 baseline issue below would mislead it); Phase 2 (cluster `members[{record_path,retro_id,entry_id,status}]` provenance is specified in T004/T005 per AC-01/V-01).

## Findings
| Severity | Finding | Evidence | Status |
|---|---|---|---|
| MEDIUM | T009's Done-When gate "verify … headline open:encoded matches the known 128:22" is pinned to a stale, internally-inconsistent baseline. The live corpus now computes 182 entries = **133 open / 23 encoded / 26 suggested** (open:encoded **133:23**), not 128:22 (the dossier's own F-07 figure 128/24/22 sums to 174, not 182). An implementer running T009 verbatim against a *correct* engine will see a mismatch and either waste time debugging or falsely conclude AC-04 fails. The plan's own task 1.8 sets no fixed ratio — it requires only "zero crashes; skip counters explain every non-parsed file" — so tasks.md over-specified an extra gate against a drifting append-only corpus. | Live scan `grep -rhoE "status: (open\|encoded\|suggested)" .harness/records/retro` → 133/23/26 (=182); T009 row + Executive Briefing vs plan §Phase 1 task 1.8 | open — needs judgment (not auto-fixed) |

## Repairs
_None applied. The finding is MEDIUM and needs a design judgment (SKILL §9 bars auto-fixing MEDIUM). Recommended smallest fix: replace the frozen "128:22" anchor with an internal-consistency check — `open + encoded + suggested == entries` and every skip counter accounts for a non-parsed file — or, if a literal is kept, stamp it "as-of 2026-07-12: 182 entries, 133 open / 23 encoded / 26 suggested". All line-number and cross-reference citations were verified current, so no mechanical repair was warranted._
