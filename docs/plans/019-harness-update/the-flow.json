{
  "schema_version": 1,
  "kind": "flight-plan",
  "slug": "harness-update",
  "plan_dir": "docs/plans/019-harness-update",
  "mode": "Simple",
  "cursor": "impl",
  "recommended_next": "impl",
  "now": "Plan written + validated (READY, CS-4, Simple) — heading to the build",
  "next": "Implement (single Simple-mode phase)",
  "_comment": "Simple mode (user-directed). Router IS installed (~/.agents/skills/eng-harness-flow) so harness seam nodes are included. Spec + plan both done; plan auto-ran /validate-v2 (4 validators; CRITICAL banner-chokepoint fix applied; READY). the-flow.md is rendered FROM this file.",
  "agents": [],
  "nodes": [
    { "id": "spec", "type": "spec", "label": "Spec", "status": "done",
      "command": "/the-flow 1b specify",
      "ran_at": "2026-06-15",
      "user_input": "new plan, harness-update. yep, add those harness update commands as suggeted. also have it check for updates once a day and suggest to the user to update (give htem command to do it). once it detects an pdate is available, every call to cli shoudl have in its output somehere (json or hyuman) \"update availble to <version> from <version_installed>. comamnd to updaet is: blah. This way the agent will see it or the human will see it.",
      "note": "Simple mode (user-directed). CS-4. Four capabilities. Validated via /validate-v2.",
      "artifacts": ["harness-update-spec.md"],
      "next": ["bp"] },
    { "id": "bp", "type": "backpressure", "label": "Backpressure Check (post-spec seam)", "status": "assumed",
      "command": "/eng-harness-flow --event post-spec --spec docs/plans/019-harness-update/harness-update-spec.md",
      "branch_of": "spec",
      "note": "Optional post-spec harness seam — not run (user went spec → architect). Advisory; absence changes nothing.",
      "next": ["plan"] },
    { "id": "plan", "type": "plan", "label": "Plan (Simple — single phase)", "status": "done",
      "command": "/the-flow 3 architect",
      "ran_at": "2026-06-15",
      "user_input": "architect then valiadte",
      "note": "Status: READY. Gate Matrix 4 PASS / 0 FAIL / 1 N/A (G4 ADR — no docs/adr). 2 research subagents + 4 validate-v2 validators ran. CRITICAL applied: banner re-targeted from createOutputPort to the true universal chokepoint exitWithEnvelope (KF-09, T006B). 13 tasks + 2 harness-seam rows.",
      "artifacts": ["harness-update-plan.md"],
      "next": ["impl"] },
    { "id": "impl", "type": "phase", "label": "Implementation (single Simple-mode phase, 13 tasks)", "status": "known",
      "command": "/the-flow 6 implement --plan docs/plans/019-harness-update/harness-update-plan.md",
      "note": "Simple mode -> one phase with inline tasks (T001-T013 + T000/T0ZZ harness seams). Fires the pre-implement harness seam before task 1 (router installed).",
      "next": ["merge"] },
    { "id": "merge", "type": "merge", "label": "Merge", "status": "known",
      "command": "/the-flow 8 merge",
      "next": ["hh"] },
    { "id": "hh", "type": "harness-retro", "label": "Harness plan-complete (seam)", "status": "assumed",
      "command": "/eng-harness-flow --event plan-complete",
      "branch_of": "merge",
      "note": "Plan-complete seam — fired by the merge verb after execution; router installed.",
      "next": [] }
  ]
}
