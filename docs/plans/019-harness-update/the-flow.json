{
  "schema_version": 1,
  "kind": "flight-plan",
  "slug": "harness-update",
  "plan_dir": "docs/plans/019-harness-update",
  "mode": "Simple",
  "cursor": "merge",
  "recommended_next": "merge",
  "now": "Build done — implemented with a live companion (14 tasks, 611 tests green); review superseded",
  "next": "Merge",
  "_comment": "Simple mode. Build ran via `/the-flow 6 implement --companion`. The code-review-companion reviewed every per-task commit; 7 findings (3 HIGH) all fixed → the post-hoc review stage is superseded (Graph decoration), so awaiting-6 routes straight to merge. the-flow.md is rendered FROM this file.",
  "agents": [
    {
      "kind": "companion",
      "slug": "code-review-companion",
      "run_id": "2026-06-15T12-33-03-542Z-8339",
      "render": "wrap",
      "covers": ["impl"],
      "note": "Power-On-Mode; reviewed every commit live. Final verdict REQUEST_CHANGES → F001-F007 (3 HIGH, 4 MEDIUM) all fixed in 169aa5d. Supersedes the review stage."
    }
  ],
  "nodes": [
    { "id": "spec", "type": "spec", "label": "Spec", "status": "done",
      "command": "/the-flow 1b specify",
      "ran_at": "2026-06-15",
      "note": "Simple mode (user-directed). CS-4. Four capabilities. Validated via /validate-v2.",
      "artifacts": ["harness-update-spec.md"],
      "next": ["bp"] },
    { "id": "bp", "type": "backpressure", "label": "Backpressure Check (post-spec seam)", "status": "assumed",
      "command": "/eng-harness-flow --event post-spec --spec docs/plans/019-harness-update/harness-update-spec.md",
      "branch_of": "spec",
      "note": "Optional post-spec harness seam — not run (user went spec → architect). Advisory.",
      "next": ["plan"] },
    { "id": "plan", "type": "plan", "label": "Plan (Simple — single phase)", "status": "done",
      "command": "/the-flow 3 architect",
      "ran_at": "2026-06-15",
      "user_input": "architect then valiadte",
      "note": "Status READY. Gate Matrix 4 PASS / 0 FAIL / 1 N/A. 2 research subagents + 4 validate-v2 validators. CRITICAL applied (banner re-targeted to exitWithEnvelope, KF-09).",
      "artifacts": ["harness-update-plan.md"],
      "next": ["impl"] },
    { "id": "impl", "type": "phase", "label": "Implementation (single Simple-mode phase, 14 tasks)", "status": "done",
      "command": "/the-flow 6 implement --companion --plan docs/plans/019-harness-update/harness-update-plan.md",
      "ran_at": "2026-06-15",
      "user_input": "implement with companion please.",
      "note": "Built on branch 019-harness-update (off documentation-updates HEAD; other agent's WIP preserved, scoped commits only). T001-T013 + T006B. Pre-implement boot HEALTHY. 611 tests green, arch boundaries clean, envelope additive-only. Companion reviewed every commit → 7 findings all fixed (169aa5d). 15 commits c62f30b..169aa5d.",
      "artifacts": ["execution.log.md"],
      "next": ["review"] },
    { "id": "review", "type": "review", "label": "Review (superseded by companion)", "status": "assumed",
      "command": "/the-flow 7 review --plan docs/plans/019-harness-update/harness-update-plan.md",
      "branch_of": "impl",
      "note": "Superseded — the code-review-companion reviewed every commit inline; a separate post-hoc pass is redundant (Graph decoration). Run only if you want a second, independent review.",
      "next": ["merge"] },
    { "id": "merge", "type": "merge", "label": "Merge", "status": "known",
      "command": "/the-flow 8 merge --plan docs/plans/019-harness-update/harness-update-plan.md",
      "next": ["hh"] },
    { "id": "hh", "type": "harness-retro", "label": "Harness plan-complete (seam)", "status": "assumed",
      "command": "/eng-harness-flow --event plan-complete",
      "branch_of": "merge",
      "note": "Plan-complete seam — fired by the merge verb after execution; router installed.",
      "next": [] }
  ]
}
