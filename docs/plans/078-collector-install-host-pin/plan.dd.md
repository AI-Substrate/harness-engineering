<!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. -->
# The collector install path has never worked — drop the host pin, keep the digest (#124)

**Schema**: builder/plan · **Source**: plan.dd.json · **Sections**: 6

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | The collector install path has never worked — drop the host pin, keep the digest (#124) |
| slug | 078-collector-install-host-pin |
| ordinal | — |
| status | draft |
| complexity | — |
| summary | Drop the unsatisfiable release_host pin, keep the digest, and prove the happy path succeeds. |
| backpressure | — |
| log | — |
| mode | — |
| plan_version | — |
| created | — |
| confidence | — |
| complexity_breakdown | — |

<a id="summary"></a>

## Summary

harness doctor --install-collector cannot succeed on any platform. pin.ts declares release_host: 'github.com' and the downloader refuses any redirect leaving it, but GitHub ALWAYS redirects release-asset downloads to a CDN host. The guard fires on the happy path and the download never starts, so the only install path plan 073 shipped has never worked.

Proven universal by control rather than assumed: an unrelated repo (cli/cli v2.62.0) redirects to the same CDN host. The hostname changed recently; the redirect did not. A github.com host pin could never have matched — which is why the fix is to DROP the pin and rely on the SHA-256, not to allowlist the current CDN host that GitHub has just renamed once already.

The suite is green because the only redirect test fakes a redirect to cdn.example.com — a host that SHOULD be refused. It proves the guard fires when it should; nothing proves it does not fire when it should not. That asymmetry IS the defect, and adding another negative case would deepen it while looking like more coverage.

<a id="goals"></a>

## Goals

- harness doctor --install-collector actually installs the pinned collector — the happy path succeeds against GitHub's real redirect behaviour.
- Integrity is preserved by the property that actually provides it: the pinned SHA-256, verified before anything is placed on disk.
- The test suite proves the POSITIVE direction — a redirect to the real CDN host SUCCEEDS — not only that a bad redirect is refused. The missing direction is the whole defect.
- Whatever replaces the pin is asserted by a test, so a security-relevant field cannot silently stop being emitted by regenerate the way release_host could.

<a id="non-goals"></a>

## Non-goals

- Allowlisting release-assets.githubusercontent.com. Ruled out by prime: it buys a fix that breaks the next time GitHub renames the host, which it has just done. A host allowlist that cannot be satisfied on the happy path is not defence-in-depth, it is an outage.
- Adding more NEGATIVE redirect cases. The existing asymmetry is the defect; another refusal test makes it worse while reading as more coverage.
- Answering plan 075's named-pipe premise. Still open, now unblocked by an approved one-off side-load rather than by this plan.
- Sweeping the wider half-injected-seam pattern across the suite. Named on #124, not fixed here.
- Touching 077 / PR #118. Separate stream, waiting on the consumer's re-run.

<a id="acceptance-criteria"></a>

## Acceptance criteria

| id | claim | state | note | receipt | pressure | proven_by |
| --- | --- | --- | --- | --- | --- | --- |
| ac-0001 | The release_host pin is removed at all three live sites — the declaration (pin.ts), the manifest writer (regenerate.ts), and its only reader (install.ts) — with no orphan field left in the generated manifest. | [ ] unchecked | — | Blast radius measured by prime at exactly three sites; every other rg hit is a .git/ai/working_logs blob, not source. | — | — |
| ac-0002 | A test fakes a redirect to release-assets.githubusercontent.com and asserts the download SUCCEEDS. This is the load-bearing AC: the existing suite only ever proved the guard fires when it should, and no fake reproduced GitHub's actual redirect. | [ ] unchecked | — | Must FAIL against the pre-fix code. A positive-direction test that passes before the fix is not testing the fix. | — | — |
| ac-0003 | Integrity is unchanged: a payload whose SHA-256 does not match the pin is still refused, and nothing is placed on disk. Dropping the host pin must not weaken the digest check that was always the real control. | [ ] unchecked | — | Existing digest tests pass untouched, plus a case proving refusal survives a redirect to the real CDN host — the two must not be coupled. | — | — |
| ac-0004 | Whatever pinned fields remain are ASSERTED by a test that fails if regenerate stops emitting them. release_host had no such test, which is why it could ship unsatisfiable. | [ ] unchecked | — | Prime's finding: no test anywhere asserted release_host — not its presence, value, or survival through regeneration. | — | — |
| ac-0005 | The end-to-end install is exercised as far as this environment allows, and any step that could NOT be verified here is named rather than implied. | [ ] unchecked | — | A real network install is not reproducible in CI; state what was proven by fake, what by live probe, and what remains unverified. | — | — |

<a id="phases"></a>

## Phases

| id | title | brief | state | note | receipt | depends_on | tasks | objective | delivers | key_risks |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ph-2e37 | Phase 1: Drop the host pin and prove the happy path | — | [ ] unchecked | — | — | — | [tasks](assets/tasks/phase-1/tasks.dd.md#tasks) | — | — | — |
