# Original ask — dont-apologise-fix
**Captured**: 2026-07-09T10:35:00+10:00  ·  **By**: /the-flow

> kick off a new flow please, flow spine first, collect initial ask (you basicaly stated it above) then in to explore phase.

The "initial ask" is the decision record produced by the preceding grill-me session (agreed point-by-point by the user):

> **Objective** — Make "don't apologise — fix; the environment is a target of work for us, agents and humans together; pay every difficulty forward" a directive agents re-encounter mechanically mid-work, not prose they read once.
>
> **Decisions**
> 1. **Four layers, each independently removable**: canonical SKILL.md invariant · posture line in flight-plan template `instructions[]` (phase + observe nodes) · sub-skill posture lines · harness-seams.md echo.
> 2. **Discrimination test in the directive itself**: the inheritance test — "would tomorrow's agent hit this wall?" Own mistakes fix silently; hard walls and proof-gaps observe immediately; papercuts on second strike. No MUST-FIX neurosis.
> 3. **Rule narrowed, not repealed**: sub-skills may name `harness observe` (with execution-log fallback for direct-jump); orchestration stays flow-owned.
> 4. **New flows only** — no instruction migration into `sync`.
> 5. **Mid-flow fixes allowed** for small/reversible work, tracked as excursion nodes (intent + outcome notes via CLI) — no mini-plan docs.
> 6. **Every presented observation gets a disposition** (fixed-now/task/plan/command/declined/deferred), structured in the retro envelope; CLI schema + fingerprint done **now**, in this change-set.
> 7. **Retro presentation rewritten** — recommendation-led numbered paragraphs with a judgement-based "highest value, because…"; letter-code leaks purged from four builder files.
> 8. **Validated by flow-eval** with a planted-friction scenario scoring friction→observe and drain format.
> 9. **Anti-goals**: never a gate, never a runtime target, never a guilt ledger. The user still has to deliver their work; the harness assists. Out of scope: router vocabulary, minih, harvest UX (beyond code leaks), analysis tooling.
> 10. **Token discipline is a binding constraint**: every added line is channel-sized one-liners citing one canonical statement — this change must not worsen the flow's token profile (that's the *next* plan's whole subject).
> 11. **Tripwires measured from real telemetry** — and since telemetry is all we'll have, the plan gets an explicit task: *open the actual telemetry we collect today* (the field reference just landed in `docs/`), and verify it can answer both this change's tripwires and builder/flow performance generally; gaps become telemetry work inside this plan.
