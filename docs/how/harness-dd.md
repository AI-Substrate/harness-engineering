# Deterministic documents

The maintained deterministic-documents guide now lives in
[`docs/how/dd/`](dd/README.md).

This compatibility page remains so existing links have one stable route to the
new progressive reference instead of preserving a second copy that can drift.
